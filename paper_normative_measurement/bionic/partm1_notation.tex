% ===========================================================================
% PART -1 -- every symbol and every object, from arithmetic upward.
% Nothing in this paper is used before it appears here. An audit of the first
% draft found thirty-one objects used without definition; all are below.
% ===========================================================================
\section{\bi{E}{ve}{ry} \bi{sy}{mb}{ol}, from \bi{ari}{thme}{tic} \bi{up}{wa}{rd}}
\label{sec:notation}

This \bi{se}{cti}{on} \bi{de}{fin}{es} \bi{eve}{ryth}{ing} the \bi{p}{ap}{er} uses. It \bi{as}{sum}{es} \bi{ari}{thme}{tic} --- \bi{ad}{di}{ng},
\bi{mul}{tipl}{ying}, and the idea of a \bi{nu}{mb}{er} --- and \bi{no}{thi}{ng} else. A \bi{re}{ad}{er} who is
\bi{fl}{ue}{nt} may skip to §\ref{sec:math}; a \bi{re}{ad}{er} who is not \bi{sh}{ou}{ld} be able to \bi{fi}{ni}{sh}
this \bi{se}{cti}{on} and then \bi{c}{he}{ck} \bi{e}{ve}{ry} \bi{sub}{sequ}{ent} line.

\subsection{\bi{Nu}{mbe}{rs}, \bi{ine}{quali}{ties}, \bi{int}{erv}{als}}

$\R$ \bi{de}{not}{es} the real \bi{nu}{mbe}{rs}: \bi{eve}{ryth}{ing} on the \bi{nu}{mb}{er} line, \bi{inc}{lud}{ing} \bi{fra}{cti}{ons}
and \bi{irr}{atio}{nals} like $\sqrt 2$. $a<b$ \bi{m}{ea}{ns} $a$ lies to the left of $b$.
$[a,b]$ is the set of all $x$ with $a \le x \le b$ (\bi{end}{poi}{nts} \bi{in}{clu}{ded});
$(a,b)$ \bi{ex}{clu}{des} the \bi{end}{poi}{nts}. $|x|$ is the \emph{\bi{ab}{sol}{ute} \bi{v}{al}{ue}}: $x$ if
$x \ge 0$, and $-x$ \bi{oth}{erw}{ise}. It is the \bi{di}{sta}{nce} from $x$ to $0$, so it is \bi{n}{ev}{er}
\bi{ne}{gat}{ive}.

\step{One fact used \bi{rep}{eate}{dly}.} \bi{Mul}{tipl}{ying} an \bi{ine}{qual}{ity} by a \emph{\bi{po}{sit}{ive}}
\bi{nu}{mb}{er} \bi{pre}{ser}{ves} it; \bi{mul}{tipl}{ying} by a \emph{\bi{ne}{gat}{ive}} \bi{nu}{mb}{er} \textbf{\bi{re}{ver}{ses}}
it. $2<3$ but $-2>-3$. §\ref{sec:ci} \bi{de}{pen}{ds} on this at one \bi{sp}{eci}{fic} step, and
the step is \bi{ma}{rk}{ed} \bi{t}{he}{re}.

\subsection{\bi{Fun}{cti}{ons}}

A \emph{\bi{fu}{nct}{ion}} $f$ from a set $\mathcal{A}$ to a set $\mathcal{B}$, \bi{wr}{itt}{en}
$f:\mathcal{A}\to\mathcal{B}$, \bi{as}{sig}{ns} to each \bi{el}{eme}{nt} of $\mathcal{A}$ \bi{ex}{act}{ly}
one \bi{el}{eme}{nt} of $\mathcal{B}$. $\mathcal{A}$ is the \emph{\bi{do}{ma}{in}},
$\mathcal{B}$ the \emph{\bi{co}{dom}{ain}}. ``\bi{Ex}{act}{ly} one'' is the \bi{w}{ho}{le} \bi{co}{nte}{nt}: a rule
that \bi{som}{eti}{mes} \bi{g}{iv}{es} two \bi{an}{swe}{rs} is not a \bi{fu}{nct}{ion}.

\begin{definition}[Monotone]
$f$ is \emph{\bi{st}{ric}{tly} \bi{inc}{reas}{ing}} if $x<y$ \bi{im}{pli}{es} $f(x)<f(y)$. Such a \bi{fu}{nct}{ion}
\bi{pre}{ser}{ves} \bi{o}{rd}{er} and \bi{no}{thi}{ng} else: it can \bi{st}{ret}{ch} and \bi{sq}{uee}{ze} \bi{dis}{tan}{ces} \bi{fr}{ee}{ly}.
§\ref{sec:cardinal} uses \bi{ex}{act}{ly} this \bi{pr}{ope}{rty} to test \bi{wh}{eth}{er} the \bi{p}{ap}{er}'s
\bi{ce}{ntr}{al} data are \bi{or}{din}{al} or not.
\end{definition}

\subsection{Sum and \bi{pr}{odu}{ct} \bi{no}{tat}{ion}}

\[
\sum_{i=1}^{n} x_i \;=\; x_1+x_2+\cdots+x_n,
\qquad
\prod_{i=1}^{n} x_i \;=\; x_1 \cdot x_2 \cdots x_n .
\]
The \bi{le}{tt}{er} $i$ is a \emph{\bi{d}{um}{my}}: $\sum_{i=1}^{3}x_i$ and $\sum_{j=1}^{3}x_j$ are
the same \bi{nu}{mb}{er}. \bi{T}{hr}{ee} \bi{man}{ipula}{tion} \bi{r}{ul}{es} are used \bi{l}{at}{er} and each is just
\bi{or}{din}{ary} \bi{ari}{thme}{tic} \bi{wr}{itt}{en} \bi{com}{pac}{tly}.

\step{Rule 1 --- \bi{pu}{lli}{ng} out a \bi{co}{nst}{ant}.}
$\sum_i (c\,x_i) = c\sum_i x_i$, \bi{be}{cau}{se} $cx_1+cx_2+\cdots = c(x_1+x_2+\cdots)$.

\step{Rule 2 --- \bi{spl}{itt}{ing} a sum.}
$\sum_i (x_i+y_i) = \sum_i x_i + \sum_i y_i$, \bi{be}{cau}{se} \bi{ad}{dit}{ion} can be \bi{reo}{rde}{red}.

\step{Rule 3 --- \bi{reo}{rder}{ing}, and why it is safe here.} For a \emph{\bi{fi}{ni}{te}}
\bi{col}{lect}{ion}, \bi{ad}{di}{ng} the \bi{t}{er}{ms} in any \bi{o}{rd}{er} \bi{g}{iv}{es} the same \bi{t}{ot}{al}. \bi{E}{ve}{ry} sum in this
\bi{p}{ap}{er} is \bi{fi}{ni}{te}, so \bi{gr}{oup}{ing} \bi{t}{er}{ms} \bi{dif}{fere}{ntly} --- for \bi{in}{sta}{nce} \bi{gr}{oup}{ing} \bi{ou}{tco}{mes}
by the \bi{v}{al}{ue} a \bi{fu}{nct}{ion} \bi{t}{ak}{es} on them, as in the \bi{p}{ro}{of} of \cref{lem:lin} --- \bi{n}{ev}{er}
\bi{ch}{ang}{es} the \bi{an}{sw}{er}. (For \bi{in}{fin}{ite} sums this is \bi{f}{al}{se} in \bi{ge}{ner}{al}; the \bi{p}{ap}{er} \bi{n}{ev}{er}
\bi{n}{ee}{ds} one.)

\begin{definition}[Factorial]
$n! := 1\cdot 2\cdot 3 \cdots n$, with $0!:=1$ by \bi{con}{vent}{ion}. It \bi{co}{un}{ts} the \bi{nu}{mb}{er}
of ways to \bi{ar}{ran}{ge} $n$ \bi{di}{sti}{nct} \bi{ob}{jec}{ts} in a row: $n$ \bi{ch}{oic}{es} for the \bi{f}{ir}{st}, then
$n-1$, and so on. $3!=6$, and the six \bi{ord}{eri}{ngs} are \bi{li}{st}{ed} \bi{exp}{lici}{tly} in
§\ref{sec:toy}.
\end{definition}

\subsection{\bi{Po}{we}{rs}, \bi{r}{oo}{ts}, \bi{log}{arit}{hms}}

$x^2 := x\cdot x$; $\sqrt{x}$ is the non-\bi{ne}{gat}{ive} \bi{nu}{mb}{er} \bi{w}{ho}{se} \bi{sq}{ua}{re} is $x$;
$x^{1/2}=\sqrt x$. The \emph{\bi{log}{ari}{thm}} $\log x$ is the \bi{p}{ow}{er} to \bi{w}{hi}{ch} the base
must be \bi{ra}{is}{ed} to give $x$. Only two \bi{pro}{pert}{ies} are used:
$\log(ab)=\log a+\log b$ and $\log$ is \bi{st}{ric}{tly} \bi{inc}{reas}{ing}. The \bi{se}{co}{nd} is why
\bi{cor}{rela}{ting} $\log$-\bi{tra}{nsfo}{rmed} \bi{qua}{ntit}{ies} in \cref{rem:normalise} is a \bi{sta}{tem}{ent}
\bi{a}{bo}{ut} \emph{\bi{pro}{porti}{onal}} \bi{ra}{th}{er} than \bi{ab}{sol}{ute} co-\bi{mo}{vem}{ent}.

\subsection{Max, min, \bi{ar}{gm}{ax}, sup, inf}

$\max_i x_i$ is the \bi{la}{rge}{st} of the $x_i$; $\min_i x_i$ the \bi{sm}{all}{est}.

\begin{definition}[argmax]
$\argmax_{i} x_i$ is the \emph{\bi{i}{nd}{ex}} at \bi{w}{hi}{ch} the \bi{ma}{xim}{um} \bi{oc}{cu}{rs}, not the \bi{v}{al}{ue}.
If $x=(4,9,2)$ then $\max = 9$ and $\argmax = 2$. When two \bi{en}{tri}{es} tie, the
$\argmax$ is not \bi{un}{iq}{ue}; the \bi{p}{ap}{er}'s \bi{de}{cis}{ion} \bi{r}{ul}{es} \bi{b}{re}{ak} ties by \bi{i}{nd}{ex} \bi{o}{rd}{er}, and
\bi{e}{xa}{ct} ties in \bi{con}{tinu}{ous} \bi{sc}{or}{es} \bi{o}{cc}{ur} with \bi{pro}{babi}{lity} zero in the data.
\end{definition}

\begin{definition}[Supremum]
$\sup$ is the \bi{l}{ea}{st} \bi{u}{pp}{er} \bi{b}{ou}{nd}: the \bi{sm}{all}{est} \bi{nu}{mb}{er} that is $\ge$ \bi{e}{ve}{ry} \bi{el}{eme}{nt}
of a set. It \bi{coi}{nci}{des} with $\max$ when the \bi{ma}{xim}{um} is \bi{at}{tai}{ned}, and \bi{ex}{is}{ts} even
when it is not. $\sup\{1-\tfrac1n : n\ge1\}=1$ \bi{al}{tho}{ugh} $1$ is \bi{n}{ev}{er} \bi{at}{tai}{ned}.
\cref{thm:vetoinf} \bi{st}{at}{es} $\sup_X \Delta(X)=\infty$, \bi{me}{ani}{ng}: for any \bi{b}{ou}{nd} you
name, some \bi{can}{did}{ate} set \bi{ex}{cee}{ds} it.
\end{definition}

\subsection{Sets, and the four \bi{ope}{rati}{ons} used}

$\mathcal{A}\cup\mathcal{B}$ (\bi{u}{ni}{on}) \bi{co}{nta}{ins} \bi{eve}{ryth}{ing} in \bi{ei}{th}{er};
$\mathcal{A}\cap\mathcal{B}$ (\bi{int}{ersec}{tion}) \bi{eve}{ryth}{ing} in both;
$\mathcal{A}\subseteq\mathcal{B}$ (\bi{su}{bs}{et}) \bi{m}{ea}{ns} \bi{e}{ve}{ry} \bi{el}{eme}{nt} of $\mathcal{A}$
is in $\mathcal{B}$; $\varnothing$ is the \bi{e}{mp}{ty} set; $|\mathcal{A}|$ is the \bi{c}{ou}{nt}
of \bi{el}{eme}{nts}; $\mathcal{A}\setminus\mathcal{B}$ \bi{re}{mov}{es} from $\mathcal{A}$
\bi{eve}{ryth}{ing} that is in $\mathcal{B}$.

\begin{lemma}[Union bound, the pointwise version]
\label{lem:union}
$\mathbf{1}\{x\in\bigcup_i \mathcal{A}_i\}\;\le\;\sum_i \mathbf{1}\{x\in\mathcal{A}_i\}$.
\end{lemma}
\begin{proof}
If $x$ is in none of the sets, both \bi{s}{id}{es} are $0$. If $x$ is in at \bi{l}{ea}{st} one, the
left side is $1$ and the \bi{r}{ig}{ht} side is at \bi{l}{ea}{st} $1$ \bi{be}{cau}{se} it \bi{co}{un}{ts} the sets
\bi{con}{tain}{ing} $x$. \bi{T}{he}{re} is no \bi{t}{hi}{rd} case.
\end{proof}
\Cref{thm:bonf} is this \bi{l}{em}{ma} plus \bi{ta}{ki}{ng} \bi{exp}{ectat}{ions}, and \bi{no}{thi}{ng} else.

\subsection{\bi{Tu}{pl}{es} and \bi{ve}{cto}{rs}}

An \emph{\bi{or}{der}{ed} \bi{t}{up}{le}} $(x_1,\dots,x_k)$ is a list in \bi{w}{hi}{ch} \bi{po}{sit}{ion} \bi{ma}{tte}{rs}:
$(1,2)\neq(2,1)$. The set of all such \bi{l}{is}{ts} of $k$ real \bi{nu}{mbe}{rs} is \bi{wr}{itt}{en} $\R^k$,
and an \bi{el}{eme}{nt} of it is a \emph{\bi{ve}{ct}{or}}.

\bi{Thr}{ough}{out} this \bi{p}{ap}{er} $k=4$ and the four \bi{pos}{iti}{ons} are the four \bi{can}{did}{ate}
\bi{res}{pon}{ses} $A,B,C,D$. So a \bi{ve}{ct}{or} such as $(5.1,\,7.1,\,5.7,\,2.8)$ \bi{m}{ea}{ns}: this
norm \bi{sc}{or}{es} \bi{re}{spo}{nse} $A$ at $5.1$, $B$ at $7.1$, and so on.

\begin{definition}[Addition and scaling]
$(x_1,\dots,x_k)+(y_1,\dots,y_k) := (x_1+y_1,\dots,x_k+y_k)$ and
$c\,(x_1,\dots,x_k) := (cx_1,\dots,cx_k)$. Both act \bi{po}{sit}{ion} by \bi{po}{sit}{ion}.
\end{definition}

\step{Why this is the \bi{r}{ig}{ht} \bi{ope}{rat}{ion} here.} A \bi{cri}{ter}{ion} with \bi{we}{ig}{ht} $w$ and
\bi{sat}{isfac}{tion} \bi{pr}{ofi}{le} $s=(s_A,s_B,s_C,s_D)$ \bi{con}{trib}{utes} $w\,s$ to the \bi{s}{co}{re}, and
\bi{se}{ver}{al} \bi{cr}{ite}{ria} \bi{con}{trib}{ute} \bi{t}{he}{ir} sum. That is \bi{ex}{act}{ly} \cref{def:score}, and it is
the only \bi{p}{la}{ce} \bi{ve}{ct}{or} \bi{ari}{thme}{tic} \bi{en}{te}{rs} the \bi{con}{struc}{tion}.

\subsection{The dot \bi{pr}{odu}{ct}, the norm, and the \bi{a}{ng}{le}}

\begin{definition}[Dot product]
$x\cdot y := \sum_{i=1}^{k} x_i y_i$, a \bi{si}{ng}{le} \bi{nu}{mb}{er}.
\end{definition}

\begin{definition}[Norm]
$\|x\| := \sqrt{x\cdot x} = \sqrt{\sum_i x_i^2}$.
\end{definition}

\step{Why the norm is the \bi{le}{ng}{th}.} In two \bi{dim}{ensi}{ons}, $x=(x_1,x_2)$ is the \bi{co}{rn}{er}
of a \bi{r}{ig}{ht} \bi{tr}{ian}{gle} with legs $x_1$ and $x_2$; \bi{Pyt}{hago}{ras} \bi{g}{iv}{es} \bi{hyp}{oten}{use}
$\sqrt{x_1^2+x_2^2}$, \bi{w}{hi}{ch} is $\|x\|$. In $k$ \bi{dim}{ensi}{ons} the same \bi{fo}{rmu}{la} is
\bi{t}{ak}{en} as the \emph{\bi{def}{init}{ion}} of \bi{le}{ng}{th}, and it \bi{be}{hav}{es} as \bi{le}{ng}{th} \bi{sh}{ou}{ld}:
$\|cx\|=|c|\,\|x\|$, and $\|x\|=0$ only when \bi{e}{ve}{ry} \bi{coo}{rdin}{ate} is $0$.

\begin{theorem}[The dot product measures angle]
\label{thm:cosangle}
For non-zero $x,y$,
\[
\cos\theta \;=\; \frac{x\cdot y}{\|x\|\,\|y\|},
\]
\bi{w}{he}{re} $\theta$ is the \bi{a}{ng}{le} \bi{be}{twe}{en} $x$ and $y$.
\end{theorem}
\begin{proof}
\begin{steps}
\item The law of \bi{co}{sin}{es} for the \bi{tr}{ian}{gle} with \bi{s}{id}{es} $\|x\|$, $\|y\|$
and $\|x-y\|$ \bi{st}{at}{es}
$\|x-y\|^2 = \|x\|^2+\|y\|^2-2\|x\|\|y\|\cos\theta$.
\item \bi{Ex}{pa}{nd} the left side \bi{u}{si}{ng} the \bi{def}{init}{ion}:
\[
\|x-y\|^2=\sum_i (x_i-y_i)^2=\sum_i x_i^2-2\sum_i x_iy_i+\sum_i y_i^2
=\|x\|^2-2(x\cdot y)+\|y\|^2 .
\]
\item Set the two \bi{exp}{ress}{ions} \bi{e}{qu}{al}. The \bi{t}{er}{ms} $\|x\|^2$ and $\|y\|^2$
\bi{ca}{nc}{el} from both \bi{s}{id}{es}, \bi{le}{avi}{ng} $-2(x\cdot y) = -2\|x\|\|y\|\cos\theta$.
\item \bi{Di}{vi}{de} by $-2\|x\|\|y\|$.
\end{steps}
\end{proof}

\begin{definition}[Cosine similarity]
$\cos(x,y) := \dfrac{x\cdot y}{\|x\|\|y\|}$, a \bi{nu}{mb}{er} in $[-1,1]$.
$+1$ \bi{m}{ea}{ns} same \bi{dir}{ect}{ion}, $0$ \bi{per}{pendi}{cular}, $-1$ \bi{op}{pos}{ite}.
\end{definition}

\begin{remark}[The single most important consequence for this paper]
\bi{Co}{si}{ne} \bi{sim}{ilar}{ity} is \emph{\bi{b}{li}{nd} to \bi{le}{ng}{th}}: $\cos(x,y)=\cos(2x,y)$ for any
\bi{sc}{ali}{ng}. That is \bi{pre}{cis}{ely} why it is the \bi{r}{ig}{ht} tool once \cref{rem:normalise}
\bi{est}{abli}{shes} that the \bi{le}{ng}{th} of $\Nn_i$ \bi{me}{asu}{res} how many \bi{cr}{ite}{ria} a \bi{pe}{rs}{on}
\bi{ha}{ppe}{ned} to rate \bi{ra}{th}{er} than \bi{an}{yth}{ing} \bi{nor}{mat}{ive}.
\end{remark}

\step{A \bi{wo}{rk}{ed} \bi{co}{si}{ne}.} $x=(1,0)$, $y=(1,1)$. Then $x\cdot y = 1$,
$\|x\|=1$, $\|y\|=\sqrt2=1.4142$, so $\cos = 1/1.4142 = 0.7071$, \bi{w}{hi}{ch} is
$\cos 45^\circ$. \bi{Co}{rre}{ct}: $(1,1)$ \bi{po}{in}{ts} at $45$ \bi{de}{gre}{es} to $(1,0)$.

\subsection{\bi{Ce}{ntr}{ing}, and what it \bi{re}{mov}{es}}

\begin{definition}[Centred vector]
$\tilde x := x - \bar x\,\mathbf{1}$ \bi{w}{he}{re} $\bar x = \tfrac1k\sum_i x_i$ and
$\mathbf{1}=(1,1,\dots,1)$. \bi{Equ}{ivale}{ntly}, \bi{su}{btr}{act} the \bi{av}{era}{ge} from \bi{e}{ve}{ry} \bi{e}{nt}{ry}.
\end{definition}

\step{Two \bi{f}{ac}{ts}, both \bi{imm}{edi}{ate}.} $\sum_i \tilde x_i = \sum_i x_i - k\bar x = 0$;
and if $y = x + c\mathbf{1}$ then $\tilde y = \tilde x$, \bi{be}{cau}{se} the \bi{s}{hi}{ft} \bi{m}{ov}{es}
the \bi{av}{era}{ge} by \bi{ex}{act}{ly} $c$ as well. \emph{\bi{Ce}{ntr}{ing} \bi{er}{as}{es} the \bi{co}{mm}{on} \bi{l}{ev}{el} and
\bi{k}{ee}{ps} only the \bi{con}{tra}{sts}}, \bi{w}{hi}{ch} is \bi{ex}{act}{ly} what the \bi{de}{cis}{ion} uses.

\subsection{\bi{Ma}{tri}{ces}, rank, and the one \bi{ma}{tr}{ix} this \bi{p}{ap}{er} \bi{n}{ee}{ds}}

A \emph{\bi{ma}{tr}{ix}} is a \bi{rec}{tang}{ular} \bi{a}{rr}{ay} of \bi{nu}{mbe}{rs}; $M_{ij}$ is the \bi{e}{nt}{ry} in row
$i$, \bi{co}{lu}{mn} $j$. A \bi{ma}{tr}{ix} acts on a \bi{ve}{ct}{or} by
$(Mx)_i := \sum_j M_{ij}x_j$: each \bi{ou}{tp}{ut} \bi{coo}{rdin}{ate} is a \bi{we}{igh}{ted} sum of the
\bi{i}{np}{ut} \bi{coo}{rdin}{ates}.

\begin{definition}[Linear independence, span, rank]
\bi{Ve}{cto}{rs} $v_1,\dots,v_m$ are \emph{\bi{li}{nea}{rly} \bi{ind}{epen}{dent}} if the only way to make
$\sum_j c_j v_j = 0$ is to take \bi{e}{ve}{ry} $c_j = 0$ --- none of them is \bi{red}{und}{ant}. \bi{T}{he}{ir}
\emph{span} is the set of all $\sum_j c_j v_j$. The \emph{rank} of a \bi{ma}{tr}{ix} is the
\bi{nu}{mb}{er} of \bi{li}{nea}{rly} \bi{ind}{epen}{dent} \bi{ve}{cto}{rs} \bi{a}{mo}{ng} its \bi{ou}{tpu}{ts}: the \bi{dim}{ens}{ion} of the
\bi{s}{pa}{ce} it can \bi{r}{ea}{ch}.
\end{definition}

\begin{lemma}[The centring matrix and its rank]
\label{lem:centrank}
Let $C := I - \tfrac1k\mathbf{1}\mathbf{1}^\top$, so that $Cx = \tilde x$. Then
$C$ is a \bi{pro}{ject}{ion} ($C^2=C$) and $\operatorname{rank}(C)=k-1$.
\end{lemma}
\begin{proof}
\begin{steps}
\item $(\mathbf{1}\mathbf{1}^\top x)_i = \sum_j x_j$
for \bi{e}{ve}{ry} $i$, so $\tfrac1k \mathbf{1}\mathbf{1}^\top x = \bar x\mathbf{1}$, and
$Cx = x - \bar x \mathbf{1}$.
\item \bi{Ce}{ntr}{ing} an \bi{al}{rea}{dy}-\bi{ce}{ntr}{ed} \bi{ve}{ct}{or} \bi{ch}{ang}{es}
\bi{no}{thi}{ng}, \bi{s}{in}{ce} its \bi{av}{era}{ge} is $0$. So $C(Cx)=Cx$, i.e. $C^2=C$.
\item $C\mathbf{1} = \mathbf{1}-\mathbf{1}=0$, so the
one-\bi{dim}{ensi}{onal} \bi{s}{pa}{ce} of \bi{co}{nst}{ant} \bi{ve}{cto}{rs} is sent to zero.
\item If $Cx=0$ then $x=\bar x\mathbf{1}$, i.e. $x$ is
\bi{co}{nst}{ant}. So the set sent to zero is \emph{\bi{ex}{act}{ly}} the \bi{con}{sta}{nts}, of \bi{dim}{ens}{ion}
$1$.
\item The \bi{i}{np}{ut} \bi{s}{pa}{ce} has \bi{dim}{ens}{ion} $k$; a one-\bi{dim}{ensi}{onal}
\bi{su}{bsp}{ace} is \bi{des}{tro}{yed}; the \bi{i}{ma}{ge} \bi{the}{ref}{ore} has \bi{dim}{ens}{ion} $k-1$. For $k=4$:
rank $3$.
\end{steps}
\end{proof}

\Cref{lem:centrank} is the \bi{en}{ti}{re} \bi{co}{nte}{nt} of the \bi{f}{ir}{st} half of \cref{thm:probe}.

\subsection{\bi{Equ}{ival}{ence}, \bi{quo}{tie}{nts}, and the \bi{sp}{he}{re}}

\begin{definition}[Equivalence relation]
A \bi{re}{lat}{ion} $\sim$ that is \bi{ref}{lex}{ive} ($x\sim x$), \bi{sym}{met}{ric} ($x\sim y \Rightarrow
y \sim x$) and transitive ($x\sim y, y\sim z \Rightarrow x\sim z$). It partitions
a set into \bi{di}{sjo}{int} \emph{\bi{equ}{ival}{ence} \bi{cl}{ass}{es}}: $[x] := \{y : y\sim x\}$.
\end{definition}

\step{The two used here.} (i) $u \sim u+c\mathbf{1}$: \bi{uti}{lit}{ies} \bi{dif}{fer}{ing} by a
\bi{co}{nst}{ant}, \bi{w}{hi}{ch} \bi{de}{ci}{de} \bi{ide}{ntic}{ally}. (ii) $u \sim \lambda u$ for $\lambda>0$:
\bi{uti}{lit}{ies} \bi{dif}{fer}{ing} by \bi{po}{sit}{ive} \bi{s}{ca}{le}, \bi{w}{hi}{ch} \bi{o}{rd}{er} \bi{ide}{ntic}{ally}.

\begin{definition}[Quotient]
The set of \bi{equ}{ival}{ence} \bi{cl}{ass}{es}, \bi{wr}{itt}{en} $\mathcal{A}/\!\sim$. Its \bi{dim}{ens}{ion} is
the \bi{dim}{ens}{ion} of $\mathcal{A}$ \bi{m}{in}{us} the \bi{dim}{ens}{ion} of what was \bi{col}{lap}{sed}.
\end{definition}

\begin{definition}[Sphere]
$S^{m} := \{x \in \R^{m+1} : \|x\|=1\}$, the set of unit-\bi{le}{ng}{th} \bi{ve}{cto}{rs} in
$\R^{m+1}$. It has \bi{dim}{ens}{ion} $m$: one \bi{con}{stra}{int} \bi{re}{mov}{es} one \bi{de}{gr}{ee} of \bi{fr}{eed}{om}.
$S^1$ is a \bi{ci}{rc}{le} in the \bi{p}{la}{ne}; $S^2$ is the \bi{su}{rfa}{ce} of a ball in \bi{t}{hr}{ee}
\bi{dim}{ensi}{ons}.
\end{definition}

\step{\bi{Pu}{tti}{ng} the two \bi{to}{get}{her}, \bi{w}{hi}{ch} is \cref{thm:probe}.} \bi{St}{art}{ing} from
$\R^4$: \bi{qu}{oti}{ent} by \bi{con}{sta}{nts} \bi{le}{av}{es} \bi{dim}{ens}{ion} $3$; the unit-\bi{le}{ng}{th}
\bi{rep}{resent}{atives} of that $3$-\bi{dim}{ensi}{onal} \bi{s}{pa}{ce} form $S^2$, of \bi{dim}{ens}{ion} $2$. \bi{H}{en}{ce}
``two free \bi{par}{amet}{ers}''.

\subsection{\bi{O}{rd}{er} \bi{sta}{tist}{ics}: \bi{me}{di}{an}, \bi{qu}{ant}{ile}, \bi{per}{cent}{ile}}

Sort the \bi{va}{lu}{es} as $x_{(1)}\le x_{(2)}\le\cdots\le x_{(n)}$.

\begin{definition}[Median]
The \bi{mi}{dd}{le} \bi{v}{al}{ue}: $x_{((n+1)/2)}$ if $n$ is odd, and the \bi{av}{era}{ge} of
$x_{(n/2)}$ and $x_{(n/2+1)}$ if $n$ is even. For $(2,4,4,4,6)$ it is $4$; for
$(2,4,4,6)$ it is $(4+4)/2=4$.
\end{definition}

\begin{definition}[Quantile]
The $q$-\bi{qu}{ant}{ile} is the \bi{v}{al}{ue} \bi{b}{el}{ow} \bi{w}{hi}{ch} a \bi{fr}{act}{ion} $q$ of the data lies. The
\bi{me}{di}{an} is the $0.5$-\bi{qu}{ant}{ile}; $p_{25}$ and $p_{75}$ are the $0.25$- and
$0.75$-\bi{qua}{nti}{les} (\emph{\bi{qua}{rti}{les}}). ``$p_{95}=0.800$'' in §\ref{sec:est} \bi{m}{ea}{ns}
$95\%$ of \bi{pr}{omp}{ts} have a \bi{re}{lat}{ive} \bi{ma}{rg}{in} \bi{b}{el}{ow} $0.800$.
\end{definition}

\begin{remark}[Why the paper reports quantiles beside means]
A mean can be \bi{m}{ov}{ed} \bi{arb}{itra}{rily} by one \bi{ex}{tre}{me} \bi{v}{al}{ue}; a \bi{me}{di}{an} \bi{ca}{nn}{ot}. \bi{Rep}{ort}{ing}
both, as \cref{tab:vetoM} does with \bi{me}{di}{an} $3.64$ and \bi{ma}{xim}{um} $26.54$, \bi{ex}{pos}{es} a
skew that a mean \bi{a}{lo}{ne} \bi{w}{ou}{ld} hide --- and in that \bi{t}{ab}{le} the \emph{\bi{ma}{xim}{um}} is the
load-\bi{be}{ari}{ng} \bi{nu}{mb}{er}, \bi{be}{cau}{se} \cref{thm:vetoinf} is \bi{a}{bo}{ut} the \bi{w}{or}{st} case.
\end{remark}

\subsection{Rank \bi{cor}{rela}{tion} (\bi{Sp}{ear}{man})}

\begin{definition}[Spearman $\rho$]
\bi{Re}{pla}{ce} each \bi{v}{al}{ue} by its rank (\bi{sm}{all}{est} $=1$, next $=2$, \dots) in its own list,
then \bi{co}{mpu}{te} the \bi{or}{din}{ary} \bi{cor}{rela}{tion} of the \bi{r}{an}{ks}.
\end{definition}

\step{Why the \bi{p}{ap}{er} uses it for the \bi{g}{au}{ge} test.} It \bi{re}{spo}{nds} only to \emph{\bi{o}{rd}{er}},
so it \bi{an}{swe}{rs} ``do the two \bi{ins}{trum}{ents} rank the \bi{ope}{rat}{ors} the same way'' \bi{wi}{tho}{ut}
\bi{b}{ei}{ng} \bi{dis}{trac}{ted} by \bi{t}{he}{ir} \bi{dif}{fer}{ent} \bi{sc}{al}{es} --- \bi{w}{hi}{ch} is the \bi{e}{xa}{ct} \bi{qu}{est}{ion}
§\ref{sec:gauge} asks. A \bi{Sp}{ear}{man} of $+1.000$ \bi{m}{ea}{ns} \bi{ide}{nti}{cal} \bi{or}{der}{ing}; $0$ \bi{m}{ea}{ns}
\bi{unr}{ela}{ted} \bi{or}{der}{ing}.

\step{\bi{Wo}{rk}{ed}.} \bi{Ins}{trum}{ent} 1 \bi{g}{iv}{es} $(0.116, 0.051, 0.042, 0.023)$, \bi{r}{an}{ks}
$(4,3,2,1)$. \bi{Ins}{trum}{ent} 2 \bi{g}{iv}{es} $(0.139,0.061,0.035,0.025)$, \bi{r}{an}{ks} $(4,3,2,1)$.
\bi{Ide}{nti}{cal} \bi{r}{an}{ks}, so $\rho=+1.000$, even \bi{th}{ou}{gh} no \bi{v}{al}{ue} \bi{ma}{tch}{es}.

\subsection{Bits, and what \bi{qua}{ntisa}{tion} \bi{m}{ea}{ns}}

\begin{definition}[Bit]
One \emph{bit} \bi{dis}{tingu}{ishes} two \bi{pos}{sibil}{ities}. $b$ bits \bi{dis}{ting}{uish} $2^b$: one
bit \bi{g}{iv}{es} $2$ \bi{le}{ve}{ls}, two bits $4$, \bi{t}{hr}{ee} bits $8$, \bi{e}{ig}{ht} bits $256$.
\end{definition}

\begin{definition}[Quantisation to $b$ bits]
\bi{G}{iv}{en} \bi{va}{lu}{es} in $[\ell,h]$, \bi{re}{pla}{ce} each $x$ by the \bi{ne}{are}{st} of the $2^b$ \bi{ev}{en}{ly}
\bi{sp}{ac}{ed} \bi{le}{ve}{ls} $\ell + \tfrac{j}{2^b-1}(h-\ell)$, $j=0,\dots,2^b-1$.
\end{definition}

\step{\bi{Wo}{rk}{ed}, at one bit.} With \bi{we}{igh}{ts} \bi{ra}{ngi}{ng} over $[-2,8]$, one bit \bi{le}{av}{es} the
two \bi{le}{ve}{ls} $\{-2, 8\}$: \bi{e}{ve}{ry} \bi{ne}{gat}{ive} \bi{we}{ig}{ht} \bi{be}{com}{es} $-2$ and \bi{e}{ve}{ry} \bi{po}{sit}{ive} one
$8$. Only the \emph{sign} \bi{su}{rvi}{ves}. §\ref{sec:compile} \bi{re}{por}{ts} that this
\bi{rep}{rodu}{ces} $89.8\%$ of the \bi{sh}{ipp}{ed} \bi{dec}{isi}{ons}.

\subsection{Min--max \bi{nor}{malis}{ation}}

\begin{definition}
$\hat x_i := \dfrac{x_i - \min_j x_j}{\max_j x_j - \min_j x_j} \in [0,1]$,
with $\hat x = 0$ at the \bi{w}{or}{st} \bi{e}{nt}{ry} and $1$ at the best.
\end{definition}

\step{Why §\ref{sec:agg} uses it, and what it \bi{as}{sum}{es}.} \bi{Com}{par}{ing} \bi{ut}{ili}{ty} \bi{ac}{ro}{ss}
\bi{pe}{op}{le} \bi{re}{qui}{res} \bi{pu}{tti}{ng} them on a \bi{co}{mm}{on} \bi{s}{ca}{le}, and \bi{no}{thi}{ng} in the \bi{eli}{cita}{tion}
\bi{su}{ppl}{ies} one. Min--max \bi{m}{ak}{es} \bi{e}{ve}{ry} \bi{pe}{rs}{on}'s best \bi{op}{ti}{on} \bi{w}{or}{th} $1$ and \bi{w}{or}{st} \bi{w}{or}{th}
$0$ \emph{by \bi{con}{struc}{tion}}, \bi{w}{hi}{ch} is a \bi{sti}{pula}{tion}, not a \bi{mea}{sure}{ment}. It is
\bi{st}{at}{ed} as such: the \bi{Bla}{ckw}{ell} \bi{def}{icie}{ncy} of $0.1298$ is a \bi{nu}{mb}{er} \bi{re}{lat}{ive} to that
\bi{nor}{malis}{ation}, and a \bi{dif}{fer}{ent} \bi{int}{erper}{sonal} \bi{s}{ca}{le} \bi{w}{ou}{ld} give a \bi{dif}{fer}{ent} \bi{fi}{gu}{re}.

\subsection{\bi{Con}{diti}{onal} \bi{pro}{babi}{lity} and \bi{con}{diti}{onal} \bi{exp}{ecta}{tion}}

\begin{definition}[Conditional probability]
$\Prob(E\mid F) := \dfrac{\Prob(E\cap F)}{\Prob(F)}$ when $\Prob(F)>0$:
\bi{re}{str}{ict} \bi{att}{ent}{ion} to the \bi{ou}{tco}{mes} in $F$ and \bi{ren}{orma}{lise} so they sum to $1$.
\end{definition}

\begin{definition}[Conditional expectation]
$\E[X \mid F] := \dfrac{\sum_{\omega\in F} X(\omega)\Prob(\omega)}{\Prob(F)}$,
the \bi{av}{era}{ge} of $X$ over $F$ \bi{a}{lo}{ne}.
\end{definition}

\begin{lemma}[Law of total expectation, the two-event case]
\label{lem:tower}
For an \bi{e}{ve}{nt} $F$ with \bi{com}{plem}{ent} $F^c$,
\[
\E[X] \;=\; \E[X\mid F]\,\Prob(F) \;+\; \E[X\mid F^c]\,\Prob(F^c).
\]
\end{lemma}
\begin{proof}
\bi{S}{pl}{it} the \bi{de}{fin}{ing} sum by \bi{wh}{eth}{er} the \bi{ou}{tco}{me} lies in $F$:
\[
\E[X]=\sum_{\omega\in F}X(\omega)\Prob(\omega)+\sum_{\omega\in F^c}X(\omega)\Prob(\omega).
\]
\bi{Mu}{lti}{ply} and \bi{di}{vi}{de} the \bi{f}{ir}{st} sum by $\Prob(F)$ and the \bi{se}{co}{nd} by $\Prob(F^c)$,
then \bi{a}{pp}{ly} the \bi{def}{init}{ion} \bi{a}{bo}{ve} to each. (If $\Prob(F)=0$ the term is \bi{ab}{se}{nt} and
the \bi{id}{ent}{ity} is \bi{tr}{ivi}{al}.)
\end{proof}

\Cref{lem:tower} with $F=\{\text{the decision flipped}\}$ and $X = \Reg$ is
\bi{ex}{act}{ly} \cref{lem:coarsen}, \bi{s}{in}{ce} $\Reg=0$ on $F^c$ so the \bi{se}{co}{nd} term \bi{va}{nis}{hes}.

\subsection{\bi{Ind}{epen}{dent} and \bi{ide}{ntic}{ally} \bi{dis}{trib}{uted}}

\emph{\bi{Ide}{ntic}{ally} \bi{dis}{trib}{uted}} \bi{m}{ea}{ns} \bi{e}{ve}{ry} \bi{obs}{erva}{tion} is \bi{d}{ra}{wn} from the same
$\Prob$. \emph{\bi{Ind}{epen}{dent}} \bi{m}{ea}{ns} no \bi{obs}{erva}{tion} \bi{ca}{rri}{es} \bi{inf}{orma}{tion} \bi{a}{bo}{ut}
\bi{an}{oth}{er}. \bi{Wr}{itt}{en} \emph{iid}. §\ref{sec:cluster} \bi{ex}{is}{ts} \bi{be}{cau}{se} in this data the
\bi{obs}{ervat}{ions} are \bi{ide}{ntic}{ally} \bi{dis}{trib}{uted} but \textbf{not} \bi{ind}{epen}{dent}, and only
the \bi{se}{co}{nd} half \bi{f}{ai}{ls}.

\subsection{\bi{Ma}{rk}{ov} \bi{ke}{rne}{ls}}

\begin{definition}[Markov kernel]
A rule $K$ that, \bi{g}{iv}{en} an \bi{i}{np}{ut} $z$, \bi{pr}{odu}{ces} a \emph{\bi{pro}{babi}{lity} \bi{dis}{tribu}{tion}}
over \bi{ou}{tpu}{ts}, \bi{wr}{itt}{en} $K(\cdot\mid z)$. A \bi{det}{ermin}{istic} \bi{fu}{nct}{ion} is the \bi{sp}{eci}{al}
case that puts all the \bi{pro}{babi}{lity} on one \bi{ou}{tp}{ut}, \bi{wr}{itt}{en} $\delta_{f(z)}$.
\end{definition}

\step{Why the \bi{p}{ap}{er} \bi{n}{ee}{ds} it.} \Cref{thm:blackwell-proof} says a \bi{ga}{rbl}{ing} \bi{ca}{nn}{ot}
help. \bi{Agg}{rega}{tion} is \bi{det}{ermin}{istic}, \bi{h}{en}{ce} a \bi{ga}{rbl}{ing} with the \bi{deg}{ener}{ate} \bi{ke}{rn}{el}
$\delta_{\Agg(z)}$; the \bi{th}{eor}{em} then \bi{ap}{pli}{es} \bi{ve}{rba}{tim} \bi{wi}{tho}{ut} any \bi{pro}{babil}{istic}
\bi{agg}{rega}{tion} \bi{b}{ei}{ng} \bi{in}{vol}{ved}.

\subsection{\bi{Int}{egr}{als}, only as much as $\Phi$ \bi{re}{qui}{res}}

The \bi{p}{ap}{er} uses the \bi{no}{rm}{al} \bi{dis}{tribu}{tion} \bi{fu}{nct}{ion}
$\Phi(z)=\int_{-\infty}^{z}\tfrac{1}{\sqrt{2\pi}}e^{-t^2/2}\,dt$. The \bi{in}{teg}{ral}
sign \bi{m}{ea}{ns} \emph{area \bi{u}{nd}{er} the \bi{c}{ur}{ve} to the left of $z$}. The \bi{c}{ur}{ve}
$\tfrac{1}{\sqrt{2\pi}}e^{-t^2/2}$ is the \bi{fa}{mil}{iar} bell; its \bi{t}{ot}{al} area is $1$, so
$\Phi(z)$ is the \bi{fr}{act}{ion} of the bell \bi{l}{yi}{ng} left of $z$.

\begin{definition}[Quantile of the normal]
$z_q$ is the \bi{p}{oi}{nt} with $\Phi(z_q)=q$. The \bi{t}{hr}{ee} used are
$z_{0.975}=1.9600$, $z_{0.80}=0.8416$, $z_{0.99}=2.3263$. By the \bi{sy}{mme}{try} of the
bell \bi{a}{bo}{ut} $0$, $z_{1-q}=-z_q$; this \bi{sy}{mme}{try} is the step used in
\cref{thm:mde}.
\end{definition}

\begin{remark}[No calculus is needed beyond this]
The \bi{p}{ap}{er} \bi{n}{ev}{er} \bi{dif}{ferent}{iates} or \bi{int}{egra}{tes} \bi{an}{yth}{ing}. $\Phi$ \bi{en}{te}{rs} only as a
\bi{t}{ab}{le} of \bi{t}{hr}{ee} \bi{nu}{mbe}{rs}, and \bi{e}{ve}{ry} use of it is an \bi{in}{sta}{nce} of ``how far out in
the bell is this''.
\end{remark}

\subsection{\bi{Si}{gmo}{id} and \bi{l}{og}{it}, for the \bi{ins}{trum}{ent}}

The \bi{j}{ud}{ge} \bi{pr}{odu}{ces} a \bi{nu}{mb}{er} $\lambda$ (a \bi{dif}{fere}{nce} of log-\bi{pro}{babil}{ities}) and it
is \bi{ma}{pp}{ed} to $(0,1)$ by the \emph{\bi{si}{gmo}{id}}
$\sigma(\lambda)=1/(1+e^{-\lambda})$, \bi{w}{hi}{ch} is \bi{st}{ric}{tly} \bi{inc}{reas}{ing} with
$\sigma(0)=0.5$.

\mesoboundary{$\sigma(\lambda)$ lies in $(0,1)$ and is \bi{the}{ref}{ore} \bi{ea}{si}{ly} \bi{mi}{sta}{ken}
for a \bi{pro}{babi}{lity}. It is not one. It is a \bi{mo}{not}{one} re-\bi{exp}{ress}{ion} of a \bi{m}{od}{el}'s
\bi{in}{ter}{nal} \bi{s}{co}{re}, \bi{cal}{ibra}{ted} \bi{ag}{ain}{st} \bi{no}{thi}{ng}. \bi{E}{ve}{ry} use of $\sat$ in this \bi{p}{ap}{er}
\bi{ca}{rri}{es} that \bi{ca}{ve}{at}, and §\ref{sec:instrument} \bi{me}{asu}{res} its \bi{con}{sequ}{ence} \bi{ra}{th}{er}
than \bi{as}{sum}{ing} it away.}

\subsection{A \bi{t}{ab}{le} of \bi{e}{ve}{ry} \bi{sy}{mb}{ol}}

\begin{table}[H]\centering\footnotesize
\begin{tabular}{llll}
\toprule
\bi{sy}{mb}{ol} & \bi{me}{ani}{ng} & \bi{f}{ir}{st} \bi{de}{fin}{ed} & \\
\midrule
$\mathbf{1}\{P\}$ & \bi{ind}{ica}{tor}, $1$ if $P$ true & §\ref{sec:math} &\\
$\sum,\ \prod,\ n!$ & sum, \bi{pr}{odu}{ct}, \bi{fac}{tor}{ial} & §\ref{sec:notation} &\\
$\argmax$ & the \bi{i}{nd}{ex} of the \bi{ma}{xim}{um} & §\ref{sec:notation} &\\
$\sup,\ \inf$ & \bi{l}{ea}{st} \bi{u}{pp}{er} / \bi{gr}{eat}{est} \bi{l}{ow}{er} \bi{b}{ou}{nd} & §\ref{sec:notation} &\\
$x\cdot y,\ \|x\|$ & dot \bi{pr}{odu}{ct}, \bi{le}{ng}{th} & §\ref{sec:notation} &\\
$\cos(x,y)$ & \bi{co}{si}{ne} \bi{sim}{ilar}{ity} & §\ref{sec:notation} &\\
$\tilde x$ & \bi{ce}{ntr}{ed} \bi{ve}{ct}{or} & §\ref{sec:notation} &\\
$C$ & \bi{ce}{ntr}{ing} \bi{ma}{tr}{ix}, rank $k-1$ & \cref{lem:centrank} &\\
$S^{m}$ & unit \bi{sp}{he}{re}, \bi{dim}{ens}{ion} $m$ & §\ref{sec:notation} &\\
$\Prob,\ \E,\ \Var,\ \Cov,\ \Corr$ & \bi{pro}{babi}{lity}, mean, \bi{va}{ria}{nce}, \bi{cov}{aria}{nce}, \bi{cor}{rela}{tion} & §\ref{sec:math} &\\
$\se$ & \bi{st}{and}{ard} \bi{e}{rr}{or} $=\operatorname{sd}/\sqrt n$ & §\ref{sec:se} &\\
$s^2$ & \bi{sa}{mp}{le} \bi{va}{ria}{nce}, \bi{di}{vis}{or} $n-1$ & \cref{lem:bessel} &\\
$\Phi,\ z_q$ & \bi{no}{rm}{al} cdf and its \bi{qua}{nti}{les} & §\ref{sec:notation} &\\
$\rho$ & \bi{int}{racl}{ass} \bi{cor}{rela}{tion} \emph{or} \bi{Sp}{ear}{man} & §\ref{sec:cluster}, §\ref{sec:notation} &\\
$\mathrm{DEFF}$ & \bi{de}{si}{gn} \bi{ef}{fe}{ct} $1+(R-1)\rho$ & \cref{thm:deff} &\\
$\mathrm{MDE}$ & \bi{mi}{nim}{um} \bi{det}{ecta}{ble} \bi{ef}{fe}{ct} & \cref{thm:mde} &\\
$\mathrm{rel}$ & \bi{rel}{iabi}{lity} & §\ref{sec:atten} &\\
$\resp,\ X,\ k$ & \bi{re}{spo}{nse} \bi{s}{pa}{ce}, \bi{p}{ro}{be} set, its size ($=4$) & §\ref{sec:object} &\\
$\sat,\ \wt,\ V,\ \Pi$ & \bi{sat}{isfac}{tion}, \bi{we}{ig}{ht}, veto set, \bi{ra}{nki}{ng} & \cref{def:tensors} &\\
$Y_p,\ \Nn_i$ & \bi{col}{lect}{ive} and \bi{ind}{ivid}{ual} \bi{in}{duc}{ed} \bi{sc}{or}{es} & \cref{def:score}, \cref{def:ni} &\\
$\Agg,\ G$ & \bi{agg}{rega}{tion} rule and its \bi{ou}{tp}{ut} & \cref{eq:chain} &\\
$\Reg$ & \bi{nor}{mali}{sed} \bi{re}{gr}{et} & \cref{def:regret} &\\
$\pi$ & flip rate & §\ref{sec:est} &\\
$m$ & \bi{de}{cis}{ion} \bi{ma}{rg}{in} & \cref{prop:cap} &\\
$\Delta$ & the \bi{pe}{nal}{ty} a veto \bi{re}{qui}{res} & \cref{thm:veto} &\\
$\phi_i$ & \bi{Sh}{apl}{ey} \bi{inf}{lue}{nce} & §\ref{sec:c1} &\\
$v(S)$ & \bi{cha}{racter}{istic} \bi{fu}{nct}{ion} of the \bi{coa}{lit}{ion} game & §\ref{sec:c1} &\\
$K(\cdot\mid z)$ & \bi{Ma}{rk}{ov} \bi{ke}{rn}{el} & §\ref{sec:notation} &\\
$\sigma(\lambda)$ & \bi{si}{gmo}{id} & §\ref{sec:notation} &\\
\bottomrule
\end{tabular}
\caption{\bi{E}{ve}{ry} \bi{sy}{mb}{ol} used in the \bi{p}{ap}{er}. $\rho$ is \bi{ove}{rloa}{ded} by \bi{con}{vent}{ion}; the
two uses \bi{n}{ev}{er} \bi{ap}{pe}{ar} in the same \bi{exp}{ress}{ion} and are \bi{dis}{ambig}{uated} at each site.}
\label{tab:symbols}
\end{table}

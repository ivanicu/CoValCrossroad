# r04_rebuild_satisfaction

**Rebuild the withheld criterion x response layer and validate it**

```bash
python rounds/r04_rebuild_satisfaction/run.py
```

Results land in `rounds/r04_rebuild_satisfaction/results/`.

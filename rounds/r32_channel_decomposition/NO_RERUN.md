Comment-only edit (2026-07-28): the provenance caveat was upgraded after r48 showed the
seed/write-in split is structural rather than a threshold choice. No executable line changed.

**Verified rather than asserted:** the round was re-run and all **88** numbers in its results
file are bit-identical to the stored ones.

# A25 — is the population itself a choice?

**The decision this arc must make safe:** *may `DEFINITION.md` quote its per-rule admitted shares as
numbers, or must every one of them be reported as a band?*

R917 corrected every rate in A24 by restricting the population to candidates — apparatus out,
second judge out — and in doing so read **R906's typing as given**. R906 is where the population
comes from: R908, R909, R910, R911 and R917 all read its `RUBRIC_SELECTOR` list. So the arc has one
upstream object that decides who is even counted, and nobody has attacked it. If that object turns
on researcher choices, every number downstream of it inherits them.

**Per P16 this file is a table of contents only** — every finding, its interval and its scope live
in `E05/DEFINITION.md`.

| round | what it asked |
|---|---|
| `R918` the typing is a decision tree on two thresholds | are `exact > 0.95` and `lex > 0.25` degrees of freedom, or structural predicates wearing a threshold's clothes? |

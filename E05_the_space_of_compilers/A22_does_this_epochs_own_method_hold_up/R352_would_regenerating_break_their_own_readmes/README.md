# R352 — regenerating the seven is safe

**The decision this makes safe:** *may the seven drifted rounds be regenerated?* **Yes.** None of
their 27 differing leaves is quoted in its own round's README.

## Result — `W1_SAFE`

| round | leaves differing | quoted in its own README |
|---|---:|---:|
| `R34_global_rater_crossfit` | 13 | **0** |
| `R36_channel_shapley` | 9 | **0** |
| `R102` · `R107` · `R115` · `R121` · `R328` | 1 each | **0** |

**27 differing leaves. 0 quoted.** Regeneration is pure hygiene.

## ⭐ My pre-registered prediction failed, and that is the useful part

Written into the round **before** the run:

> *"I expect W2 for R34 and R36, which carry 13 and 9 of the 27 differing leaves, and W1 for the
> five that differ in a single leaf. Writing that down first, because a prediction I record only
> after seeing the table is not a prediction."*

**Both were wrong.** The two rounds holding four-fifths of the drift quote **none** of their
differing leaves. Drift volume does not predict prose exposure — the leaves that move are internal
intermediates, and what a round writes up is a different, smaller set.

## Why the order mattered

R351's remedy is to regenerate, and **regenerating rewrites the artifact a round's own README
quotes**. Checking afterwards would have destroyed the evidence needed to check. This is the same
population R351 named and deliberately excluded:

> *"a value quoted in a round's own README is not counted, which is the right scope for `published`
> and the wrong one for `written down somewhere`."*

## Controls

| | returned |
|---|---|
| **DIFF**, imported from R351 | identical → 0; one perturbed leaf → exactly 1 |
| **SEARCH**, imported, planted both ways | present → found; absent → clean |
| **REAL positive, on *this* population** | `R328.provenance.true_argmax = 0.55747530882624` **appears in its own README** |
| **ISOLATION** | 0 of 767 artifacts changed |

The real positive is the one that matters. R351's real control used `FORMULATION.md`; **a control
validated on a different document population than the claim's is the unit mismatch this session has
repeated in five costumes.** So it was re-fired here, on a round's own README, before the zero was
believed.

**The instrument is imported from R351, not re-typed**, so it cannot drift away from the version
whose controls were validated.

## Register

| criterion | status |
|---|---|
| **cross-citation** | **not covered, and it is a real gap** — a value one round quotes from *another* is exactly how a regenerated number would travel |
| why not | 345 READMEs at 3 significant figures collide constantly; **the wider population needs a tighter instrument, not this one** |
| precision floor | 3 significant figures, inherited from R351 — under-counts, so the zero is a bound |

## The sentence I can no longer write

> *"the rounds that drift most are the ones whose writeups are most at risk."*

**R34 and R36 hold 22 of the 27 differing leaves and quote none of them.**

Artifact: `results/r352_own_readmes.json`.

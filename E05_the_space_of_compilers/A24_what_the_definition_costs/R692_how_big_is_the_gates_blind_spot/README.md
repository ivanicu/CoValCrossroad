# R692 · how big is the gate's blind spot? — **72 literals, 38 sets, against 2 visible**

**⭐⭐⭐ The single overlap is the ③ extension itself: `{coval_core, topw_k3, topw_k4, topw_k6,
topw_k8}` appears **both** under `PUBLISHED_FIVE` and under neutral names elsewhere. **That is why
R676 found the count stable while the membership was not — the same set travels under two kinds of
name.****

## A CAVEAT MEASURED IS PRODUCTION; A CAVEAT ASSERTED IS DECORATION
R691's gate declared its own blind spot in prose. This round gives it a number, and the number is
now **in the gate's docstring** rather than in a round nobody opens.

| literals binding ≥2 arm names | |
|---|---|
| under a **release-asserting** name — the gate **sees** these | **2 distinct sets** |
| under an **our-arms** name — explicitly ours | 16 distinct sets |
| ⭐ under a **NEUTRAL** name — the gate **cannot see** these | **72 literals, 38 distinct sets** |
| ⭐ **blind sets that also appear under a release-asserting name** | **1** |

Commonest blind names: `USES_PROMPT_LABELS` ×5 · `FREE` ×5 · `FIVE` ×4 · `PAIRS` ×3 · `READERS` ×3.

Registered **A 45 [10,150] → 72, INSIDE (+27)** · **B 3 [0,20] → 1, INSIDE (−2)** · **directional
HOLDS.** 644 `run.py` scanned.

**Controls:** POSITIVE — R361's `FIVE` (neutral name, 5 arm names) found. **g=0** — `PUBLISHED_FIVE`
**not** counted as blind, *the classifier returns both values*. NEGATIVE — a literal of non-arm
strings not counted. PLACEBO — identical.

## ⚠ THE UNIT GAP IS THE WHOLE READING
A neutrally named arm list is **usually our own arm list**, which is legitimate. **72 is what the
gate cannot SEE, not a count of hidden false claims.** Instrument unit: *a neutrally named arm
literal*. Claim unit: *an unseen release claim*. **Not equal — so the alarming number is the
overlap, and it is 1.**

## ⚠ A DERIVATION, DECLARED BEFORE THE MEASUREMENT AND NOT COUNTED AS A RESULT
R689 established the release ships **one** core. It follows with no further evidence that **every
claim this arc has made about the definition was checked against one released object and N of our own
constructions.** Forced by the arithmetic — *could it have come out otherwise? No.* **It is the
deepest form of §4's "the definition describes the instance", and it is a derivation**, stated with
no evidential weight and outside this round's estimand.

## WHAT THIS DOES TO THE GATE
`release_names_resolve_to_one_set` **passing is a floor and weak evidence.** It certifies the names
that *announce* a release claim; **most arm literals here announce nothing.** The docstring now says
so with the number.

## NEXT
The overlap set is the ③ extension and it appears under both a release-asserting name and neutral
ones (`results/blind_spot.json`, field `overlap`). Read the neutral-named occurrences and record, per
occurrence, whether the surrounding code treats it as *the extension* or as *the published five* —
the two readings license different claims, and R676 found six five-member sets whose divergence
began exactly here.

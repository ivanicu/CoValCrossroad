R704 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #306 ON R703's NEXT LINE
  Counts CONFIRMED from results/load_bearing.json field `rows`: F1 4, F2 20, F3 2. R694's 95.2%
  confirmed at its README:26.
  ⛔ BUT R703's own artifact `world` string prints `str(r['unique'][:3])` (run.py:120) — three members
     per clause, no ellipsis, no count — beside `n_unique: 4` and `20` in the SAME file. Third
     occurrence of a display contradicting the artifact next to it (R690 set-indexing, R698 README
     79/44/8 vs 10). Recorded; correction annotated onto R703/README.md, gate deferred with a count.

⛔ THE ARITHMETIC TRAP, DECLARED BEFORE THE RUN.
  R360's ledger prints the (family,k) cells directly. From the 12 non-singleton cells I can DERIVE
  F2's answer by hand, so POINT A below is a check on MY ARITHMETIC, not a test of the world. It is
  labelled DERIVED and does not license a finding. The frontier moves to POINTS B and C.

ESTIMAND
  For each clause C ∈ {F1 provenance, F2 behaviour, F3 size(repaired)} and each arm C UNIQUELY
  excludes, whether C's label for that arm is recovered by the majority label of the arm's (family,k)
  cell WITH THE ARM HELD OUT, decomposed three ways:
     CELL-DETERMINED  held-out cell non-empty and not tied, and its majority is correct
     FALLBACK         held-out cell empty or tied → global majority used, and it is correct
     WRONG            prediction incorrect
  Headline = CELL-DETERMINED share, because it is the only category in which the NAME did work.

IDENTIFICATION
  Identified: every arm's (family,k) and every clause verdict are on disk (R360). ⚠ (family,k) is OUR
  parameterisation of arms WE built (carried from R694). LOO is used because in-sample purity is
  forced for singletons — 12 of 24 cells.

SCOPE
  population : the 42 arms of R360's ledger
  instrument : leave-one-out cell-majority predictor over a partition of the arm names
               instrument unit = AN ARM
               claim unit      = A CLAUSE
               ⚠ NOT EQUAL — a share over 4 or 2 arms is not a clause-level property; carried into
               the verdict as a resolution statement, not a footnote.
  baseline   : per-clause majority-class floor + label-permutation null (2000 draws × 3 seeds)
  regime     : this repository at HEAD

WORLDS
  A PARAMETERISATION  most of F2's 20 are cell-determined → F2's weight is our naming showing through
  B BEHAVIOURAL       few are → the clause carries content the name does not
  C REPLICATE ARTIFACT the cells that determine are seed/sham replicates WE shipped, so the
                      instrument measures our replicate count, not our naming
  D BLIND             the three clauses are inside each other's nulls → unanswerable here

PREDICTION MATRIX          A         B        C         D
  F2 cell-determined      high      low     high      any
  survives dedup-to-      yes       n/a     NO        n/a
    one-arm-per-cell
  F1/F3 separable          yes      yes     yes        no

REGISTERED
  POINT A  [DERIVED, not evidence]  F2 cell-determined = 13 of 20; F1 = 2 of 4; F3 = 0 of 2.
           interval F2 [6,18]. Tests my arithmetic against the code.
  POINT B  [MEASURED]  F2's cell-determined share minus the label-permutation null's 95th percentile,
           in share points: +0.35, interval [+0.05, +0.65].
  POINT C  [MEASURED]  over the 5-partition specification sweep, the number of specifications in
           which F2's cell-determined share exceeds F1's: 4 of 5, interval [1,5].
  DIRECTIONAL (separately failable)  F2's cell-determined share > F3's in a majority of the 5
           specifications.
  KILL (conditional — evaluated ONLY if POSITIVE fires and NEGATIVE is null):
     if F2's share does not exceed its own null's 95th pct in ANY specification → world B, the
       "recoverable from the name" premise dies;
     if no clause pair is separable in any specification → world D, UNVERIFIED.

CONTROLS
  POSITIVE  plant label = (family == 'random'), a pure function of the partition. Compute FLOOR (the
            permutation null) and CEILING (share of the arms in non-singleton cells) and require
            floor < threshold < ceiling — the threshold must be reachable AND refusable.
  g=0       plant an i.i.d. seeded coin-flip label; the positive control's threshold must NOT pass.
  NEGATIVE  shuffle the cell assignment preserving cell sizes and RECOMPUTE the majorities (refit,
            not relabel) → must fall to the null.
  SHAM      the same operation minus the ingredient: a single-cell partition. Isolates base rate.
  PLACEBO   two identical runs; the difference must be exactly 0.
  ARITHMETIC cell_determined + fallback + wrong == n_unique exactly, per clause, per specification.
  UNIT      assert the instrument-unit string != the claim-unit string, and carry it to the verdict.

SPECIFICATION SWEEP (partition axis, 5 cells)
  (family,k) · family only · k only · (family,k,sham-flag) · single cell [= the sham]
MULTIPLICITY  3 clauses × 5 partitions = 15, BH q=0.10 over all 15; non-survivors reported.
SEEDS  3, and the seed flag is verified to change the draws.
ARTIFACT  results/recoverability.json, with tree sha.
IMPOSSIBLE
  cross-release — the naming scheme is ours; a second release would need its own generator names.
  construct validity of "recoverable" — no external standard says what a generator name should encode.

R719 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #321 ON R718's NEXT LINE — IT HOLDS, AND FOLLOWING IT FOUND SOMETHING WORSE THAN IT ASKED FOR.
  ✓ R688's population is R360's arms, and R711/R712/R713 priced the three clauses on the same 42.
  ⛔⛔ BUT R688's `published` FIELD IS EXACTLY `PUBLISHED_FIVE` — ['coval_core','topabs_k4',
     'topvar_k4','topw_k4','topwvar_k4'] — AND R689 RETRACTED THAT SET ONE ROUND LATER. R688's whole
     finding is "the 3 published arms outside ③'s extension all fail ② anyway"; those 3 are
     topabs_k4, topvar_k4 and topwvar_k4, three of the four arms R689 showed the release does NOT
     name. ⭐ So a block R718 certified as STANDING rests on a set the very next round withdrew.
  ⛔ AND R718's INSTRUMENT COULD NOT SEE IT: R688's block on STATEMENT.md cites only R683 in its
     body, so "no body citation outranks the heading" returned UNAMENDED. The correction lives in a
     DIFFERENT block. An amendment test scoped to a block cannot see a retraction filed elsewhere.

ESTIMAND
  (i)  DEPENDENCE — does R688's verdict change when its population is corrected from PUBLISHED_FIVE
       to the set the release actually names? Recompute §4's falsifier on both.
  (ii) BLIND SPOT — over all claim blocks on STATEMENT.md, how many cite a literal or a set that a
       LATER round retracted, without citing that later round in their own body? R718 reported 3
       amended of 12; this is the count it structurally could not reach.

IDENTIFICATION
  (i) is exactly recomputable from R360's ledger and the release's card. ⚠ (ii) requires knowing
  which literals were retracted, which is read from the ledger's own retraction entries — a
  convention, so the search is positive-controlled against R688, whose status is now established.

SCOPE
  population : the 12 claim blocks of STATEMENT.md, and the 42 arms of R360's ledger
  instrument : set recomputation + a cross-block citation search
               instrument unit = A CLAIM BLOCK
               claim unit      = WHETHER A BLOCK'S SUPPORT SURVIVES
               ⚠ NOT EQUAL — a block whose SUPPORT was retracted is not thereby false; its verdict
               may survive on the corrected set, and that is exactly what (i) tests.
  baseline   : R688's committed verdict on PUBLISHED_FIVE
  regime     : this repository at HEAD

WORLDS
  A VERDICT SURVIVES   recomputed on the release's actual named set, ③ still excludes nothing that ②
                       did not, so R688's conclusion holds and only its wording needs the correction
  B VERDICT FALLS      the recomputation changes the count ⇒ a standing block is withdrawn
  C DEGENERATE         the corrected set is too small to run the test ⇒ UNVERIFIED, and the block is
                       downgraded to unevaluable rather than either kept or killed

PREDICTION MATRIX             A          B          C
  excluded-by-③-alone         0        > 0        n/a
  corrected set size       >= 2       >= 2        < 2

REGISTERED
  POINT A  arms excluded by ③ alone, on PUBLISHED_FIVE: 0 (reproducing R688), interval [0, 3]
  POINT B  the same, on the set the release's card actually names: 0, interval [0, 1]
  POINT C  claim blocks citing a retracted literal without citing the retracting round: 1,
           interval [0, 5]
  DIRECTIONAL (separately failable)  the corrected set has FEWER than 3 members, so the test that
           produced R688's "3 published arms" cannot be run on it at the same size.
  KILL (conditional — evaluated ONLY if the POSITIVE reproduces R688 and g=0 returns nothing):
     corrected count > 0 → world B, the block is WITHDRAWN from the statement;
     corrected set < 2   → world C, the block is downgraded to UNEVALUABLE, never kept.

CONTROLS
  POSITIVE   recomputing on PUBLISHED_FIVE must reproduce R688's committed `n_excluded_by_three_alone
             = 0` and its `published_excluded` list exactly. A recomputation that cannot reproduce
             the committed number is not recomputing the same thing.
  g=0        a literal that was never retracted (`coval_core`) must NOT be flagged by the cross-block
             search. A search that flags everything flags nothing.
  NEGATIVE   run the cross-block search for a literal that appears in NO block; it must return 0, not
             a silent empty pass — and an empty POPULATION exits 2.
  SHAM       the same cross-block search over DEFINITION.md, which is append-only: it should find the
             retraction recorded in place rather than filed elsewhere.
  PLACEBO    two identical runs differ by exactly 0.
  UNIT       instrument unit != claim unit, asserted and carried into the verdict.

SPECIFICATION SWEEP
  population ∈ {PUBLISHED_FIVE, card-named only, R360's 42 arms} × clause ∈ {③ alone, ③ given ②}
  = 6 cells, all reported.
MULTIPLICITY  6 cells; counts are exact, so no p-values are computed and none are implied.
SEEDS  not applicable — every operation is deterministic; stated rather than faked.
ARTIFACT  results/retracted_support.json — the recomputation on every population, and the blind-spot
          list with each block's cited rounds.
IMPOSSIBLE
  deciding whether ③ is a GOOD clause — that is construct validity and needs a standard outside here.
  cross-release.

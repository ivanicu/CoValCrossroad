# R719 · a block R718 certified as STANDING rests on a set the next round retracted

**R688's `published` field is exactly `PUBLISHED_FIVE`, and R689 retracted that set ONE ROUND LATER.
The card names `1` of the 5, and at n=1 R688's test cannot be run. `⭐ §4's FALSIFIER DOES NOT FIRE
AGAINST ③` is **downgraded to UNEVALUABLE** — not refuted, unevaluable. ⛔⛔ And R718's amendment test
was structurally blind to this: `2` of the 3 blocks touching the retracted literal never cite the
retracting round, so its "2 of 12 stand" is an overcount.**

## check #321 — it holds, and following it found worse than it asked for

✓ R688's population is R360's arms, priced by R711/R712/R713 on the same 42.

⛔⛔ **But R688's finding is *"the 3 published arms outside ③'s extension all fail ② anyway"*, and
those 3 — `topabs_k4`, `topvar_k4`, `topwvar_k4` — are three of the four arms R689 showed the release
does NOT name.**

## §4's falsifier on every population

| population | n | ③ excludes | ③ excludes ALONE |
|---|---|---|---|
| `PUBLISHED_FIVE` *(retracted)* | 5 | **3** | **0** |
| card-named only | **1** | 0 | **0** |
| R360's 42 arms | 42 | 37 | **4** |

⭐ **On the set the card actually names, n = 1 — too small to run R688's test at all.**

## ⛔⛔ the blind spot in R718's own instrument

| block | cites R689/R714? |
|---|---|
| `THE RELEASE SHIPS ONE CORE GENERATOR AND 986 INSTANCES` | ✓ yes |
| `⭐ §4's FALSIFIER DOES NOT FIRE AGAINST ③ (R688)` | ⛔ **no** — cites only R683, R688 |
| `⭐ LINEAGE OF THE EXTENSION — what supports the number 5` | ⛔ **no** — 62 rounds cited, none of them R689 |

**Both blocks R718 reported as standing touch the retracted literal without citing the retraction.**
**An amendment test scoped to a block cannot see a retraction filed in a different block.**

## controls — 6 PASS, 0 FAIL, two runs byte-identical

| control | returned |
|---|---|
| **POSITIVE** | recompute on `PUBLISHED_FIVE` → excluded `['topabs_k4','topvar_k4','topwvar_k4']`, alone **0** — **reproduces R688's committed list and number exactly** |
| **g=0** | `coval_core`, never retracted, appears in 7 of 12 blocks — the search does not flag everything |
| **NEGATIVE** | a literal in no block → **0** hits |
| **SHAM** | `DEFINITION.md`, append-only: 79 blocks, population non-empty |
| PLACEBO / UNIT | identical runs differ by 0 · instrument unit ≠ claim unit |

⛔ **The POSITIVE control caught a real defect before the verdict.** My first version reconstructed
③'s extension from **R703's** formula — *"everything ② rejects is unconstrained by ③"* — which admits
the three arms R688 says ③ excludes. The control failed, the round printed UNVERIFIED, and the fix is
to read ③'s extension from **R688's own committed artifact**. **Same error as R709: auditing a round
means using ITS object, not a sibling's algebra.**

## registered vs observed

| | registered | observed |
|---|---|---|
| **A** on `PUBLISHED_FIVE` | 0 [0, 3] | **0** — reproduces R688 |
| **B** on the card-named set | 0 [0, 1] | **0** |
| **C** blocks citing a retracted literal without the retracting round | 1 [0, 5] | **2** |
| directional | corrected set < 3 members | **HOLDS**, n = 1 |

## what this downgrades

⛔ `STATEMENT.md`'s **`⭐ §4's FALSIFIER DOES NOT FIRE AGAINST ③ (R688)`** → **UNEVALUABLE**. Its
population is `PUBLISHED_FIVE`, retracted one round after it was computed, and the corrected set is
too small to run the test.

⚠ **The clause is NOT shown to be decoration.** On R360's full 42 arms ③ excludes **37**, and **4**
that ② does not. What falls is **R688's evidence**, not ③.

⚠ **And R718's residue is an overcount** — the "2 of 12 standing" it reported first now needs
re-deriving with a cross-block retraction search.

## impossible here

| criterion | what it would require |
|---|---|
| deciding whether ③ is a *good* clause | an external standard |
| running R688's test on the card-named set | a release naming more than one core generator |

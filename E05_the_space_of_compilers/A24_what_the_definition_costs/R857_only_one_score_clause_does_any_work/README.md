# R857 · how many of the definition's clauses actually do work? — the bar ordering says ONE

**Arc A24.** ⭐ **World A. And the pattern was already committed for clause ① and never generalised.**

## ⭐ THE BARS, ON ONE SCALE

| clause | its reference bar | source |
|---|---:|---|
| **②** prompt-blind set | **0.5462** (`ref2_mean`, 41 arms) · **0.5404** (`genericpool16`, even half, 99 arms) | R347 · R856 |
| **①** random draw of the prompt's **own rubric** | **0.4922** (`ref1_mean`) | R347 |
| **④′** response-only rule max | **0.4820** | R849 |

⚠ **TWO values exist for ②'s comparator** — different constructions on different halves. **Both are
reported with provenance; neither is silently substituted for the other**, and the **KILL CHECK
requires the ordering to hold under both**. It does.

## ⭐⭐ ORDERING: **② > ① > ④′**

**MEASURED** — the ordering itself. Nothing forces a random-rubric draw to sit below a prompt-blind
set; it is an **empirical fact about this release** and could have come out otherwise.

⚠ **DERIVED** — that a higher bar **dominates** a lower one. That is algebra, exactly as in R856, and
it is **labelled rather than banked.**

## ⭐⭐⭐ SO OF THE THREE SCORE CLAUSES, ONLY ② DOES WORK

**① and ④ are both dominated: anything they would exclude, ② has already excluded.**

- **R347 said this for ① in its own committed verdict string — `W1_DERIVATION`** — with
  `ref_gap_min = 0.0470` (②'s reference exceeds ①'s on **every** arm) and `contingent: []`.
  **Nobody generalised it.**
- **R856 found it independently for ④**, one round ago, and read it as new.

⭐ **The definition therefore reduces to:**

> **② — the one working score clause**
> **+ ③ — PROVENANCE, a different kind of test with no bar at all**
> **+ the size floor**

⚠ **③ is outside this comparison BY CONSTRUCTION.** Forcing a provenance predicate onto a score
scale would be a category error — **named, not approximated.**

## ⚠ WHAT THIS DOES NOT SAY

- **Not that ① and ④ are wrong.** They are true of the object and would bind on a release where the
  bars ordered differently. **They are inert *here*.**
- **Not that the definition is "really two clauses".** ② and ③ test different kinds of thing, and
  R855 showed they are **not independent** — ③ covers ②'s permutation blind spot. **A chain of two
  is not the same as a conjunction of two.**
- **Nothing about construct validity.** No external gold standard for corehood exists.

## STRUCTURALLY IMPOSSIBLE HERE
| criterion | what it would require |
|---|---|
| placing ③ on the bar scale | it has no bar — a category error, not a missing measurement |
| construct validated | an external gold standard |
| cross-release | a second release, where the bars might order differently |

⚠ **N/A with what each would require — never "planned".**

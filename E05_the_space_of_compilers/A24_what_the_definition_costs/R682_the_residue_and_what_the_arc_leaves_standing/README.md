# R682 · the residue, and what the arc leaves standing — **CLOSURE**

**⭐⭐⭐ Labelled CLOSURE, and that label is the finding's honesty. No outcome here could change
R680's ceiling or R681's count, so calling it Frontier would inflate the word until it means
nothing. What it protects is the arc's claims; what it produces is their landing in STATEMENT.md.**

## THE RESIDUE — a third kind exists

| round | what the constant feeds |
|---|---|
| **R361** | **ANALYSIS POPULATION** — `np.mean`/`np.std` over its members, `LABELS + FIVE` |
| R360 | CONVERTED — `set(PUBLISHED_FIVE)` only |
| R676 | NO BOUND NAME — inline arguments to its **own control calls** |

Registered *2 comparison / 1 analysis* → **1 / 1** (the third has no bound name at all).
**Directional — at least one uses the set as an analysis population — HOLDS.** Kill did not fire.

**Controls:** POSITIVE — a constant consumed by `np.mean` over its members → `ANALYSIS`. **g=0** — a
constant only converted and compared → **not** `ANALYSIS`. NEGATIVE — bound and never loaded →
`DEAD`. PLACEBO — identical.

**Why R361 matters and the other two do not:** its hard-coded set is an **input assumption of its
statistics**, so any R361 number about *"the five"* inherits the hard-coding. ⚠ **Consumption, not
dependence** — computing statistics over a set does not prove the conclusion needs the set to be
right. Separating those needs the round re-run against a perturbed set.

## ⭐ WHAT THIS ARC LEAVES STANDING (§0.2 — the residue, not the ledger)

1. **The ③ extension is ONE set** — `coval_core, topw_k3, topw_k4, topw_k6, topw_k8` — with a
   **unique producing commit, R294** *(R678)*.
2. **Five other five-member sets denote other objects**: a publication list, two pre-③ sets, a rule-
   prefix list, an unrelated set *(R677)*.
3. **The deliverable's 7 extension-size rows cite 22 rounds, none of them a producer** *(R679)* —
   traceability, not correctness.
4. **At most 6 independent computations**, not 22 — a ceiling twice over *(R680)*.
5. **The hard-coding is fixtures, not contamination**: 1 of 12 reaches an output field *(R681)*.

**All five are now in `STATEMENT.md`** under *"Lineage of the extension"* — because a finding that
never reaches the deliverable is cost, not production.

## THE INSTRUMENTS THIS ARC BUILT, which outlive its claims
- a **producer finder** — earliest commit whose diff writes a set **as a value** (R678)
- a **literal classifier** with a docstring strip, so a count measures code and not prose (R680)
- a **use classifier** — fixture / output / analysis / dead (R681, R682)

## NEXT
`STATEMENT.md` now carries the lineage block, and `R361` is named there as consuming the set as an
analysis population (`results/residue.json`, field `rows`). Re-run R361 against a **perturbed** set —
one member swapped for an arm outside the extension — and compare its reported statistics. If they
move, the round's conclusion depends on the hard-coded membership; if they do not, the set was
descriptive there. That is the one measurement that separates consumption from dependence.

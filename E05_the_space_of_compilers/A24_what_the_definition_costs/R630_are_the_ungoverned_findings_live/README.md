# R630 · Mostly not superseded and mostly not live — 10 of 17 rest on rounds that never settled

**Decision this makes safe:** what to do with `FORMULATION.md`. **Neither archive nor gate.** The
dominant class is **unsettled work carried forward in prose.**

| status | n | meaning |
|---|---|---|
| **UNSETTLED** | **10** | the cited round has no artifact verdict, or an `UNVERIFIED` one |
| RETRACTED | 5 | the cited round appears in `RETRACTIONS.md` |
| **LIVE** | **2** | settled verdict, no retraction — genuinely ungoverned |

**Exactly one is both clause-naming and live:**
> **`③b ⛔ REGISTER — clause ③'s TESTABILITY, measured twice and bounded (R335–R338)`**

## ⛔⛔ I quantified over a truncated print, and was wrong in BOTH directions
R629 printed `uh[:12]`; I read the slice as the population.

| | claimed | measured |
|---|---|---|
| cite a round in the 280–360 band | 9 | **11** |
| name a clause | 6 | **3** |

**§4's "truncated string read as data", committed directly** — and *the same truncation produced an
understatement and a 2× overstatement at once*, which is why "eyeballing looked roughly right" is not
a defence. **The remedy is not care, it is arithmetic.**

## ⭐ The world set missed the modal outcome — third instance this arc
I pre-registered **A SUPERSEDED** vs **B LIVE DEBT**. The answer is **neither**: 10 of 17 are
**UNSETTLED**. R619 and R620 each retracted the same shape. **A binary world set does not merely risk
missing the answer — in this arc it has missed the MODAL outcome three times out of three.** That is
no longer a slip to note; it is a design default to change.

## What this makes of the file
- **not a history file** — only 5 of 17 are retracted, so archiving under L81 would bury live and
  unsettled material;
- **not a live-debt file** — only 2 are live, 1 clause-naming;
- **it is an unsettled-work file**, and the honest action is to mark the 10 rather than gate or
  archive the whole.

## Controls
| control | returned |
|---|---|
| **positive** — a round named in `RETRACTIONS.md` classifies RETRACTED | PASS |
| **g=0** — a round absent from the ledger (R100) does not | PASS |
| **negative** — a fabricated round (R997) has no artifact → UNSETTLED, never LIVE | PASS |
| **placebo** — a clause glyph that does not occur | **0** findings |

**MULTIPLICITY:** 17 findings × 3 status classes × 4 clause glyphs + 4 controls. **Full list printed,
no truncation** — the point of the round.

**IMPOSSIBLE, named — with its ledger:** `RETRACTIONS.md` membership means **something** about that
round was retracted, **not necessarily this finding**. So **RETRACTED overstates supersession and
LIVE is a LOWER bound on the debt.** And whether a live finding **contradicts** `② ∧ ③` needs a
reader; every one is printed with its rounds so one can judge.

## The sentence I can no longer write
> *"the ungoverned findings are either superseded or live."*

**Ten of seventeen are neither** — they cite rounds that never returned a settled verdict, and prose
carried them forward anyway.

## NEXT
The one live clause-naming finding is `③b`, and clause ③ is half the live definition — so its
testability register sitting outside every gate is the arc's sharpest single item. **Read R335–R338's
artifacts and check whether their bound on ③'s testability still holds under the current `② ∧ ③`**,
because if it does the gated pair is missing a limit on its own definition, and if it does not, a
retraction is owed to a document no gate has ever opened.

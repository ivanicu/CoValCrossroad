R677 · PRE-REGISTRATION — written after check #278 read the four rounds' ESTIMANDs, before any
classifier is built or run.

WHAT CHECK #278 ALREADY ESTABLISHED (not a prediction, an observation — no forecast scored on it)
  R676's NEXT asserted "each [of the four sets] was produced by a different ③ variant". Reading the
  four rounds' own ESTIMAND lines:
    · R470.P            = "the arms admitted by ①∧②∧④ — the extension BEFORE ③ is applied"  -> NOT a ③ reading
    · R442.published_five = the arms CoVal PUBLISHED; R442's own output says "and NEITHER is the
                          published five"                                                    -> NOT a ③ reading
    · R509.five         = a set inside a round whose stated answer is an extension of ONE     -> NOT the round's extension
    · the 22×-cited set = the ③-rank extension                                                -> IS a ③ reading
  So the attribution was asserted, not checked. Second time in this arc (ledger 745 was the first).

ESTIMAND    over every five-member arm set in the corpus, classify what its field DENOTES by reading
            its own round's ESTIMAND: (a) an extension under some ③ reading, (b) a set BEFORE ③,
            (c) an external publication list, (d) other. Then, among (a) only: how many distinct
            sets, and what is their intersection?
POINT       1 of the 4 "extension-claiming" sets survives as a genuine ③-reading extension.
INTERVAL    [1, 3]
DIRECTIONAL (separately failable) every surviving set contains `coval_core`.
KILL        if 0 survive, R676's finding is VOID rather than merely mis-framed, and the retraction
            must say so.
CONTROLS    ⚠ the classifier is a TEXT instrument over docstrings, so it needs its own controls:
  POSITIVE  the 22×-cited ③-rank set must classify as a genuine ③ extension (known member).
  g=0       the classifier must NOT classify R470.P as a ③ extension (known non-member) — if it
            does, it cannot separate and no count is admissible.
  NEGATIVE  R404.rubric_rules (rule prefixes, not arms) must classify as not-an-extension.
  PLACEBO   a synthetic field name in a round whose docstring never mentions ③ must not classify
            as a ③ extension.
IDENTIFICATION LIMIT  a docstring states what a round INTENDED; it does not prove the code computed
            it. This classifies STATED denotation, and that is an upper bound on agreement between
            field name and object.

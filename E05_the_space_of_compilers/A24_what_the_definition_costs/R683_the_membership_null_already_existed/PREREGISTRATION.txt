R683 · PRE-REGISTRATION — written after the prior-art gate found the answer, and saying so.

⚠ DISCLOSURE. P4's prior-art gate was run BEFORE any code, and it found that R361 already contains
  an EXACT null over all C(9,4)=126 label/five assignments, with `rank_null` and `rank_resolved`
  committed. So the headline is NOT a forecast — it was read off a committed artifact. This round
  therefore scores NO prediction on the finding itself. What it registers is the ARITHMETIC AUDIT of
  that artifact, which I have not performed.

WHY THE PROPOSED ROUND IS NOT RUN. R682's NEXT proposed re-running R361 against ONE perturbed set.
  R361 sweeps ALL 126. A single perturbation is strictly weaker than an exact enumeration, and
  re-running a round destroyed its artifact once already in this arc.

POINT A     the null's size is exactly C(9,4) = 126.                 (verifiable, not a guess)
POINT B     the two-sided p at 0.8B reproduces from `pct` under ties: 2 × min(0.9048, 1−0.9048+ties).
            Registered: the recomputation matches the committed 0.2857 to 4 decimals.
INTERVAL B  match / no-match — binary, and a mismatch is the finding.
DIRECTIONAL (separately failable) the 2B judge resolves the label/five rank split and the 0.8B judge
            does not — i.e. the separation is INSTRUMENT-DEPENDENT, not a property of the arms.
KILL        if the committed controls block does not show positive, g0 and placebo all true, the
            artifact's numbers are silence and nothing is read out of it.
CONTROLS
  POSITIVE  R361's own committed controls (positive / g0 / placebo) must all be true.
  ARITHMETIC the null size must equal C(9,4) exactly; a mismatch means the enumeration is not what
            the docstring says.
  g=0       a percentile of 0.5 must map to a two-sided p of 1.0 under the same formula.
  PLACEBO   recomputation run twice identical.
IDENTIFICATION LIMIT  this reads a committed artifact; it does not re-execute R361. A number that is
            wrong in the file is wrong here too. What it CAN establish is internal consistency, and
            that is what is claimed.

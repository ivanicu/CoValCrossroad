R675 · PRE-REGISTRATION — written before any contextual resolution is attempted.

WHY THE ROUND R674 PROPOSED CANNOT BE RUN
  R674's NEXT asked to split the 27 UNRESOLVABLE pairs into "never existed" vs "renamed since".
  Check #276: ALL 27 are a third cause -- a bare basename (`run.py`, `README.md`) matching 630 files.
  Zero are missing and zero are renamed. The proposed round has an empty population, and running it
  would have been a gate reporting success having examined nothing.

  ⭐ AND THE DEFECT IS NOT IN THE CORPUS, IT IS IN MY RESOLVER. A round README saying "run.py" means
  THAT round's run.py. My resolver globbed the whole tree, found 630, and discarded the pair. So the
  47.5% headline was measured on a population that systematically excluded every CONTEXTUAL citation.

ESTIMAND A  of the 27 under-specified pairs, what share resolve when the path is read relative to the
            CITING ARTIFACT's own directory rather than the repository root?
POINT A     85%          INTERVAL A [60%, 100%]
ESTIMAND B  the headline verification rate after those pairs are admitted.
POINT B     50%          INTERVAL B [40%, 62%]     (R674 measured 47.5% without them)
DIRECTIONAL (separately failable) admitting them RAISES the rate, because a round's own run.py and
            README contain that round's own numbers.
KILL        if fewer than 10 of 27 resolve contextually, the contextual reading is wrong and the
            bucket is genuinely unresolvable -- estimand B is not computed at all.
IDENTIFICATION LIMIT, STATED BEFORE THE RUN
            a README `## NEXT` section HAS a directory; a commit body does NOT. Contextual resolution
            is only available to README-sourced pairs. If the 27 are mostly commit-sourced, estimand
            A is bounded above by the README share and that bound is the result.
CONTROLS
  POSITIVE  a pair known to cite its own round's file must resolve to THAT file, not another round's.
  g=0       the same control must FAIL when pointed at a directory that has no such file.
  NEGATIVE  a bare basename from a commit body (no directory context) must stay UNRESOLVABLE.
  PLACEBO   a basename no file in the tree carries must stay UNRESOLVABLE, never resolve to anything.

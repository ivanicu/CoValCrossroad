# R645 · Every claim row is single-object-bound, and only one says so — the arc's close

**Decision this makes safe:** whether the deliverable states the scope its evidence supports.
**It did not. It does now.**

| | |
|---|---|
| rows with **instrument** scope — `home judge`, `968 prompts`, `41 arms` | **10 of 10** |
| rows with **OBJECT** scope — *holds on one release and no other* | **1 of 10** — row 7's *"one release"* |

⭐ **These are different claims.** The first scopes the **measurement**; the second scopes the
**definition**. The table stated the first everywhere and the second almost nowhere — so **nine rows
read as though they were about cores in general, when every one is about cores in this release.**

## ⛔ My own pattern under-matched, and the correction is the negative control
v1's OBJECT family lacked *"one release"* and reported **0 of 10**. Row 7 carries it. **Corrected:
1 of 10.** ⭐ *A pattern returning a rounder number than the truth is not more conservative — it is
the one that was not checked against the rows it claims to have read.*

## Why no second object can be found *(R644)*
| object | ① ② ③ ④ ⑤ |
|---|---|
| home release | **5/5** |
| second release | 3/5 — missing pool and core |
| best non-home file | 4/5 |
| **join of every non-home file** | **3/5** |

**The pool and the core exist in exactly one file and nowhere else** — the missing fields are missing
from **the disk**, not from a particular file. **The object is not merely un-found; it is not
constructible from what is here.**

## The edit
A scope block is now written into `STATEMENT.md` above the R628 bound, stating that **`② ∧ ③` is
evaluable on exactly one object**, that every row should be read as prefixed by *on the home release*,
and that **key presence is necessary but never sufficient** — R602 measured the second corpus as
disjoint in content, which no schema check can see.

## Controls
| control | returned |
|---|---|
| **positive** — every row must show instrument scope, else the column is decorative | **10 of 10** — PASS |
| **negative** — the pattern must be checked against the rows it claims to read | ⛔ **caught v1's 0 vs the true 1** |
| **placebo** — a phrase no row uses | **0** — PASS |

**MULTIPLICITY:** 10 rows × 2 scope families + 3 controls. **Every row printed verbatim.**

**IMPOSSIBLE, named:** that a row *should* carry object scope is **a judgement about what the claim
asserts, not a fact about the text.** The measurement is what the rows **say**; **the edit is a
decision, and it is recorded as one.**

## ⛔ Check #246
*"Every claim row **currently** reads as unconditional"* — **R601 measured that, and `STATEMENT.md`
has been appended to three times since.** A stale measurement in the present tense. ✓ The same line's
*"check first"* was right, which is why this round is the check rather than an act on a remembered
number.

## The sentence I can no longer write
> *"the claim table states the scope its evidence supports."*

**It stated the instrument's scope, in full, everywhere — and the object's scope in one row of ten.**
*Being scrupulous about one axis is what made the other invisible.*

## NEXT
The arc closes here on a stated bound rather than a finding, and the remaining debts are named and
small: **the four inert prohibition sites**, and `FORMULATION.md`'s **12 unsettled findings** (R632).
**Neither is a measurement.** The next genuinely open question is the one R644 makes precise —
**what would an object off this disk have to look like** — and that is a search rather than an
experiment, so it belongs to whoever provisions the next release, with R618's five fields as the
requirement they are checked against.

# R556 · Row 5 offered two alternatives and one was already on disk

**Decision this makes safe:** what independent replication is actually missing here.

**WORLD A.** Row 5 requires *"a second team **or** a second release"* and prices it at *"another
site"*. **The second release is present** — `data/utterances.jsonl`, 68.2 MB — and **3 of 375
artifacts** were computed on it: R398, R427, R433.

**The row is wrong in KIND, not in price.** What is missing is **a second designer**, which §2.5
says is dischargeable without another site. ⚠ **Not this session** — agent dispatch is disallowed
under the instructions in force, so the remedy is *named*, never *planned*.

## ⚠ Two axes I nearly conflated, and the register does too
- **second JUDGE** — `Qwen3.5-0.8B-Base` on **the same release** *(R536, R537)*
- **second CORPUS** — a **different release**

Only the second is what row 5 names. **They were written as separate strings before the control was
designed**, which is the only reason the judge rounds were not counted as corpus rounds.

## ⚠ The count was 4 until I read the hits
The fourth was **R550's gate audit**, whose amended-rounds dict contains the directory name
`R399_what_estimand_does_the_second_corpus_admit`. **The instrument matched a round's NAME and I
would have reported it as a corpus RESULT.** Tightened to require a data path in a value.

## Controls
- **Positive** — 13 artifacts record the home corpus, so a zero would have been a measurement. **PASS.**
- **Negative** — an invented corpus token appears in 0 artifacts. **PASS.**

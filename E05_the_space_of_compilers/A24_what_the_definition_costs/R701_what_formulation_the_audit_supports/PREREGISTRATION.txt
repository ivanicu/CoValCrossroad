R701 · PRE-REGISTRATION — labelled PRODUCTION with a measured component.

WHY THIS ROUND. The drift audit returns FIVE consecutive corpus rounds at the tail, and all seven of
the object thread's findings are landed in STATEMENT.md. The arc has produced a thorough CRITIQUE of
the existing definition and has never written a FORMULATION informed by it — which is what the
standing task asks for.

⚠ THIS IS PRODUCTION, NOT A FRONTIER ACTION, AND IT IS LABELLED SO. Writing a formulation executes a
  decision the audit already made; it does not separate live worlds. §0 permits production when it
  traces to gated decisions, and every constraint below traces to a committed round.

THE MEASURED COMPONENT — so the round is failable and not just prose:
POINT A     of the 5 clause-positions the current statement carries (①, ②, ③, ④, size), how many
            survive the audit's constraints UNCHANGED?
POINT A     1          INTERVAL A [0, 5]
POINT B     the formulation the audit supports has 3 clauses.
INTERVAL B  [1, 6]
DIRECTIONAL (separately failable) FEWER clause-positions survive than the current statement asserts
            as live — i.e. the audit is subtractive.
KILL        if every current clause survives unchanged, the audit produced no formulation change and
            this round must say so rather than manufacture one. That is live: ③ already survived
            §4's falsifier (R688), and ② survived as a real boundary (R693's residual).
CONSTRAINTS THE FORMULATION MUST RESPECT, each traced to a committed round:
  C1 (R689)  the release ships ONE core; the card gives the INSTANCE's size distribution, not the
             CATEGORY's. So no clause may name a k.
  C2 (R696)  ② IS an A2 threshold, so no clause may be justified by agreement with A2.
  C3 (R694)  95.2% of the definition's discriminating power is recoverable from (family, k) — two
             parameters we chose. The residual is the sham distinction.
  C4 (R688)  ③ (provenance) survives §4's falsifier on the population reachable here.
  C5 (R683/R685) the ③ separation is judge-dependent, on n=1, so any clause naming it needs a scope
             condition and that condition rests on a single verdict pair.
CONTROLS
  POSITIVE  a clause the audit explicitly killed (a named k) must be scored as NOT surviving.
  g=0       a clause the audit explicitly preserved (③'s provenance test) must be scored as
             surviving — the scorer must return both.
  NEGATIVE  a clause not mentioned by any constraint is UNSCORED, never counted as surviving.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  "survives the constraints" is a judgement I am making against rounds I ran.
            It is not a measurement of the world; it is a bookkeeping of my own audit against my own
            statement, and it is named as that.

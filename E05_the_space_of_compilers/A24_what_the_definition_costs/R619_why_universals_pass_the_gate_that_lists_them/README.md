# R619 · The fix I was about to build changes nothing, and my alarm was the wrong world

**Decision this makes safe:** whether to scope the gate's provenance discharge. **No — measured at
`+0` flagged lines at every window.** The real gap is vocabulary, and it is **87× larger**.

| class, over **368** NEXT lines | n | share |
|---|---|---|
| flagged by the live gate | 112 | 30.4% |
| **VOCAB** — a quantifier with **no artifact noun** in window | **87** | **23.6%** |
| **AMNESTY** — would flag, discharged by a citation anywhere | **1** | **0.3%** |
| clean | 168 | 45.7% |

## ⛔⛔ The verdict string printed "B BLANKET AMNESTY" on **n = 1**
**My own §4 row, committed live in the same script that was auditing closing sentences.** The branch
was `elif not amnesty:` → else **B**, so **any nonzero count promotes the alarming world**. One line
in 368 is not "blanket".

⭐ **And the deeper defect: I wrote THREE worlds and coded TWO.** World C (*both, amnesty dominant*)
had no branch, so the code could not express the answer the data actually gives — **A dominates, B is
real and negligible.** *A world set is only as good as the branch structure that reads it, and a
two-branch `== 0` divider silently deletes the middle of a three-world design.*

**Corrected verdict: A VOCABULARY GAP, with B present at 1 line and not load-bearing.**

## ⛔ Check #218 — the closing line was wrong twice, and both were checkable in one read
1. *"the gate flags bare counts and lets universals through"* — **FALSE.** `QUANT` already contains
   `every|all|none|nothing|never|only`. **The gate lists universals**; they pass for a different
   reason.
2. *"four have now passed it"* — I counted **quantifier errors**, not universals. **Pre-registered as
   4, measured at 1 → REFUTED.**

**And #212 was FLAGGED by the gate all along** — so it was never a miss. Only **#217** is a genuine
gate miss, and it is `VOCAB`: **`axis` is not in the artifact-noun list.**

## The specification curve — the whole reason not to build the fix
| discharge window | flagged | vs live gate | known-bad still flagged |
|---|---|---|---|
| 40 | 112 | **+0** | PASS |
| 80 | 112 | **+0** | PASS |
| 160 | 112 | **+0** | PASS |
| ∞ *(= live gate)* | 112 | — | PASS |

**Four cells, one answer: the scoping fix is inert.** ⭐ *A curve that is flat across every
defensible choice is the cheapest possible kill of a planned change — and it cost less than building
the change would have.*

## Controls
| control | returned |
|---|---|
| **positive** — 3 known-false NEXT lines from real history | **all 3 still flagged by the live gate** — PASS |
| **g=0** — `QUANT` emptied | **0 hits** — the detector can return nothing — PASS |
| **placebo** — a quantifier token that does not occur | **0 lines** — PASS |

**MULTIPLICITY:** 2 classes × 368 lines + 4 windows + 3 controls. All reported.

**IMPOSSIBLE, named:** *"this amnestied line is FALSE"* needs a reader. The class size is an
**EXPOSURE, never a defect count** — and 87 `VOCAB` lines are likewise **candidates**, not errors.

## The sentence I can no longer write
> *"the gate lets universals through, so it needs to be extended to cover them."*

It covers them. **One line in 368 escapes by discharge; 87 escape because the noun beside the
quantifier is not in a hand-written list** — and *that* list is the object, not the quantifier set.

## NEXT
`ARTIFACT` is a hand-written vocabulary and **87 lines sit outside it**, which is the same
open-vocabulary failure R594–R596 measured on the `world` field: **an untyped string rule over prose
cannot be made sound by adding words to it.** Instead of widening the list, sample the 87 and measure
what share quantify over project artifacts **at all** — if most are ordinary English (*"never
stop"*, *"fully determined"*), the vocabulary gap is a correct rejection and the gate is closer to
right than this round assumed.

PRE-REGISTERED BEFORE ANY CODE WAS WRITTEN FOR R658 — 2026-08-05, committed with the round.

QUANTITY: across every A24 round, take every NAME used as a glob base that the evaluator fails to
resolve. What fraction of those names are bound ONLY by a syntactic form the evaluator does not
handle at all (`with ... as`, a comprehension target, a tuple-unpack, a walrus, an AugAssign)?

POINT ESTIMATE : 20%
INTERVAL       : [5%, 45%]
DIRECTIONAL PREDICTION, which is the real content and is separately failable:
  the missing FORMS are NOT the blocker. The dominant category will be names bound by a form the
  evaluator DOES handle (plain Assign / For) whose right-hand side simply does not resolve --
  because it comes from a parameter, a call, or runtime data. If that holds, extending the
  evaluator to new syntactic forms buys little and the residual is closer to structural.
KILL           : if unhandled FORMS account for >50% of unresolved names, this prediction is
                 RETRACTED and the residual is an evaluator gap after all.

⚠ ON MY OWN FORECAST RECORD. Three consecutive forecasts in this arc were over-estimates:
  -25, -7, -5. Under a symmetric null 3/3 same-signed has p = 0.125. I am deliberately NOT
  shading this estimate downward to compensate: n=3 licenses no correction, and applying an
  unearned adjustment would make the next miss uninterpretable -- I would no longer know whether
  the estimate or the adjustment was wrong. The estimate is what I actually believe.

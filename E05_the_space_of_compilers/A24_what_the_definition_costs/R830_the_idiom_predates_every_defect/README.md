# R830 · the idiom predates every defect

**The decision this made safe:** whether the six collapsed controls need an *educational* remedy or
a *mechanical* one. **Mechanical.** The correct idiom was in use **55 indices before the earliest
defect**, so it was available and did not prevent it. `assurance/a_control_that_cannot_fail.py` is
the right remedy and this line closes.

Design in `PREREGISTRATION.txt`, committed before `run.py` existed. `run.py` committed before it ran.

## The framing my own previous NEXT proposed, corrected before anything ran

R829 closed by asking whether the six defective rounds are **disjoint** from the correct-idiom ones,
*"because a defect co-occurring with a missing idiom is a habit rather than an accident."*

⛔ **That needs variation across AUTHORS, and this corpus has one.** The only real axis is **TIME**,
and the R-index is a time index because indices are assigned in sequence. The answerable question is
whether the idiom **arrived after** the defects (learning) or was **already in use** (not learning).

## Result

| | |
|---|---|
| corpus | **822** R-indexed rounds, R1…R830 |
| rounds using `f() == f()` | **22 rows — n_eff = 13 clusters** |
| the six | R332 · R361 · R436 · R672 · R731 · R746 |
| **idiom rounds BEFORE the earliest defect** | **R255, R277 → 2**, by **55 indices** |
| idiom rounds after the latest defect | **0** |
| positive (synthetic separated) · negative (identical) · g=0 (straddle) | SEPARATED · null · null |
| two-seed | byte-identical |

**W-ALWAYS-THERE.** An idiom already in use at R255 cannot be learned at R332.

⚠ **Partly a derivation, labelled.** R-indices are assigned in sequence, so the *ordering* is free.
What is **not** forced is whether the two sets separate along it — every idiom round could have
fallen above R746. It could have come out otherwise, so it is a cheap measurement, not a theorem.

## ⛔ A null computed, rejected, and kept

A permutation drawing 22 rounds at random from the 822 gives **p = 0.0002** for the idiom rounds
falling inside the defects' span. **INADMISSIBLE**: 14 of the 22 are the contiguous arc R679–R708,
so the draw counts one arc as fourteen independent units. **n_eff is clusters, not rows.**

It is carried **inside the artifact** rather than deleted — a null that was computed and rejected is
evidence about the design, and it vanishes if it is only dropped.

⚠ I guessed n_eff ≈ 6 before measuring; **it is 13**, because adjacency has to be counted against the
indices that actually exist. A gap in the numbering is not evidence of separation.

## What is not claimed

**Availability, never intent.** Why a given round used one form and not the other is not in the
repository; it would need the authoring context.

## NEXT

The line closes. The gate exists, its controls pass, and the six are recorded. What is *not* settled
is whether a mechanical gate placed at commit time would have fired on any of the six **at the moment
they were written** — the gate scans committed source, and a round is committed after it runs.

R673 · PRE-REGISTRATION — written before reading the gate's commit walk.
What I have read: its regexes (QUANT / ARTIFACT / PROVENANCE) and its docstring. Not its walk, not
its commit list, not its extraction of the NEXT paragraph.

THE GAP        R672 measured 249 flagged commit bodies with an ad-hoc script; the gate's own freeze
               holds 167 and the gate PASSES. If 82 flagged shas were genuinely outside the freeze
               the gate would have to be failing. It is not. So the two walks disagree.

POINT          >=70% of the 82-instance gap is attributable to the NEXT-PARAGRAPH EXTRACTION RULE
               differing between the gate and my ad-hoc script -- not to the commit range.
INTERVAL       [40%, 100%]
DIRECTIONAL    (separately failable) the GATE's extraction yields FEWER qualifying paragraphs than
               my ad-hoc regex did -- i.e. the gate is the more conservative instrument, and my 249
               is the inflated number.
KILL           if the two walks cover commit counts differing by more than 20, the RANGE is the
               explanation, the extraction hypothesis dies, and 249-vs-167 says nothing about
               either regex.
CONTROLS
  POSITIVE   the two walks must agree exactly on at least one sha's verdict, or they are not
             measuring the same object and no attribution is possible.
  NEGATIVE   a sha KNOWN to be in the freeze must be flagged by both walks.
  PLACEBO    running the gate's own extraction twice must give a byte-identical set.
NOT ANSWERED  which extraction is CORRECT. This round attributes the gap; it does not adjudicate it.

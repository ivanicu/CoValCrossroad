PRE-REGISTERED BEFORE ANY CODE WAS WRITTEN FOR R657 — written 2026-08-05, committed with the round.

QUANTITY: of R656's 14 FUNCTION-LOCAL rounds, how many become DECIDED once PathEval also binds
assignments made inside a function?

POINT ESTIMATE : 11
INTERVAL       : [8, 14]
REASONING      : 13 of 14 bind exactly one local name. 7 bind `root`, which in this corpus is
                 almost always `Path(__file__).resolve().parents[N]` and resolves. 3 bind `d`,
                 which is usually a FOR-LOOP variable rather than an assignment, and a loop
                 variable needs its iterable resolved -- so those are the doubtful ones. RES,
                 work, wt, p are plain assignments and should resolve.
FAILURE MODE   : the estimate is high if `root` is itself built from a parameter, or if `d` is a
                 loop variable I have mis-modelled as an assignment.
KILL           : if the measured count falls OUTSIDE [8, 14], the FORECASTING PROCEDURE has failed,
                 not merely the number -- and the report must say so rather than quoting the new
                 number as though the method were sound. My last two forecasts here were 25->0
                 and 11->4, both outside any interval I would have drawn.

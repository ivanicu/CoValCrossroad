# R625 · An invented decimal "anchors" 36% of the time — the instrument was the object all along

**Decision this makes safe:** how to read four rounds of provenance work. **Against a measured floor
they never had.**

| band | distinct rounded values | **an INVENTED decimal matches** (3 seeds × 4000) |
|---|---|---|
| **4 dp** *(operating precision)* | 5,862 | **36.12% · 37.60% · 35.15%** |
| 3 dp | 2,641 | 91.88% · 92.47% · 91.67% |
| 2 dp | 1,523 | **100.00%** ×3 |

**23,823 persisted numeric scalars over a 4-digit space.** *"This value appears in an artifact"* is
true of **more than a third of numbers nobody ever measured.**

## ⭐⭐ What this rescopes, and it is most of the arc
- **R622's `T2 anchorable 79.0%`** must be read against a **36% floor** — the excess is **43 points,
  not 79.** The tier was reported as though the floor were zero.
- **R624's pass rates** (91.1% section, 96.3% document) run on this same matching test, so a large
  share of "pass" was **collision, not provenance**.
- **R625's own F1/F2 decomposition is VOID** — 60/52 at bands where the null is 36–92%.
- **R621's laundering demonstration STANDS**: that was an exact string written into the anchor, not a
  coincidence.

⭐ **§1 G2 says a zero from an instrument never shown to return non-zero is silence. The mirror had
no entry: a HIT from an instrument never shown to return a MISS.** Four rounds built on a matching
test whose false-positive rate took **one minute and three seeds** to measure — and nobody asked,
because the test kept returning the answer that let the next round begin.

## ⛔ Two controls failed, twice, and both times they were right
| | v1 (2–6 dp sweep) | v2 (band tied to the literal) |
|---|---|---|
| **positive** — a fabricated decimal must be `F4` | ⛔ `F1` | ⛔ `F1` |
| **placebo** — a decimal in no artifact must be `F4` | ⛔ `F1` | ⛔ `F1` |
| g=0 — a value from its own cited round | PASS | PASS |
| negative — a value from another round | PASS | PASS (`F2`) |

**v1 loosened precision to admit legitimate rounding and thereby made the test unable to fail** —
§4's *a search is an instrument*, with the collision built by me. **v2's repair was correct and
insufficient**, because the corpus is large enough that even 4 dp collides. *Two failures with the
same signature are not a bug to patch; they are the instrument reporting its own resolution.*

## ⛔ And the flag set I promised to read was never 82
Recomputed here: **112**, because R624 counted string-form value positions and this round counts
parsed numerics. **My closing line called 82 "the smallest honest flag set this project has
produced" — an uncomputed superlative attached to an instrument-specific number.**

## Verdict
**The F-decomposition is `UNVERIFIED`** — its controls failed, so F1/F2/F4 are not reportable.
**The null is a measured result**, seeded and replicated 3×, and it is what the round delivers.

**MULTIPLICITY:** 112 pairs × 3 classes + 3 bands × 3 seeds × 4000 draws + 4 controls. All reported.

**IMPOSSIBLE, named:** the null uses **uniform** draws; the document's decimals are not uniform
(they cluster near 0–1 and on common statistics), so **36% is a floor for a uniform invention and the
true collision rate for realistic values is probably higher, not lower.** Correctness still requires
re-executing the round that produced each number.

## ⛔ Check #224
*"the smallest honest flag set"* and *"the **only** thing standing between this arc and a provenance
claim"* — **two uncomputed superlatives in one sentence**, eighth and ninth in fifteen closing lines,
in the sentence a later round acts on.

## The sentence I can no longer write
> *"79% of the definition's values are traceable to a persisted artifact."*

**36 of those points are available to numbers that were never measured at all.**

## NEXT
The instrument's resolution is now known, so the question it can actually answer is narrow and
worth stating exactly: **a match is informative only where the value is rare in the corpus.** Compute,
per decimal on the two documents, **how many distinct rounds persist that value** — a value carried by
one round is evidence; a value carried by forty is a coincidence with a citation. That converts
anchoring from a boolean into a likelihood ratio, and it is computable from what is already on disk.

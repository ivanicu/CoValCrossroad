# R589 · The shared values are a real bridge — and what kind is unverified

**Decision this makes safe:** the oldest document is not disjoint from the newest.

**WORLD B.**

| | |
|---|---|
| `STATEMENT` distinct decimals | **93** |
| `FORMULATION` distinct decimals | **732** |
| **observed overlap** | **24** |
| null (400 digit-shuffles) | **mean 10.64, sd 2.40, range 4–18** |
| draws ≥ observed | **0 / 400** |

**The overlap is ~5.6 sd above the null and no shuffle reached it.** ⚠ **`p < 0.0025`, not `p = 0`
— a permutation p is floored at `1/(N+1) = 1/401`** *(G3)*, and reporting `0.000` would claim
resolution the design does not have.

**The null preserves the integer part, the decimal length and the digit multiset, and destroys value
identity.** So it is a null about *these* numbers' shapes, not about numbers in general.

## ⭐⭐⭐ What this does and does not settle
**Settled: FORMULATION's values recur in STATEMENT despite the two sharing ZERO cited rounds**
*(R588)*. The oldest document is **not** disjoint from the newest.

**UNVERIFIED — and this is the consequential fork:**
- **(a) replication** — later rounds re-measured the same quantities and got the same values, so
  FORMULATION's findings held.
- **(b) attribution drift** — values were carried forward and re-cited to later rounds, so the
  newest document's citations point at rounds that did not produce those numbers.

⚠ **(b) is not hypothetical here.** R585 already found one label carrying values from two different
rounds. **The null establishes that the bridge exists; it says nothing about which direction the
numbers travelled.**

## Controls
- **Positive** — both sets non-empty with a non-zero overlap, so the comparison exists. **PASS.**
- **Negative** — the null has non-zero spread, so it can place the observation; **a degenerate null
  admits no threshold.** **PASS.**
- ⭐ **The branch was written after the controls — third round running.**

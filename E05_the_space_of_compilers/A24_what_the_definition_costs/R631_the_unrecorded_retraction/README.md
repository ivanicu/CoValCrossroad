# R631 · The definition's own limit lived only in the document no gate reads

**Decision this makes safe:** whether clause ③'s testability bound is governed. **It was not, and
neither was the retraction inside it.** Both are now recorded.

| fact | measured |
|---|---|
| `FORMULATION.md` says *"This also retracts R335"* | **True** |
| `RETRACTIONS.md` **records** that retraction | **False** — 1 paragraph *names* R335, **0** record it |
| R335's artifact still says | **`W-DECIDABLE`** — a settled verdict, superseded in prose only |
| the gated pair states any bound on ③'s testability | **False** — **0 of 4** specific phrases |

**World C**, on the three-world set R630 made the default.

## ⛔⛔ Both PASSes that REFUTED me were instrument artifacts
v1 reported *"the retraction is in the ledger"* and *"the gated pair states the bound"* — **both
false**, and I had already written the refuted hypothesis into the report as established.

- The ledger match found **my own entry 578, written one round earlier**, which *names* the register
  while classifying it LIVE. **A citation is not a recording.** ⭐ **The retraction ledger is a
  population my own rounds write to — so a round scanning it for its own subject contaminates itself
  within one round.** Same class as R601/R604, **new vector.**
- The bound search's alternation `(testab|bound)` matched **the bare English word "bound"**.
  **A search whose broadest alternative is an ordinary word is not an instrument.**

⭐⭐ **A lenient instrument that REFUTES you is as dangerous as one that confirms you — and it feels
safer, because disagreeing with yourself reads as rigour.** The correction survived only because the
same scrutiny went to a PASS that contradicted me as to one that would have flattered me.

## The bound, now in both gated documents
- **Route 1 · PERFORMANCE — REFUTED.** Sensitivity **1.000**, specificity **0.886**, but
  `corr(slope, A2) = +0.934`; the four false positives are exactly the admitted set; `oracle_k4`, the
  *maximally* leaky arm, sits **3.25 sd BELOW** its quality prediction. **No residual leak signal**,
  and the confound is structural — fitting on labels is what makes an arm better.
- **Route 2 · SELECTION — one rule family only.** **0.866** held-out-by-arm, **0.510 — chance —
  held out by MECHANISM.**
- ⛔ **Route 1's refutation retracts R335**, now recorded as entry 581.

## Controls
| control | returned |
|---|---|
| **positive** — a round named in the ledger is found there | PASS |
| **g=0** — a fabricated round (R996) is not | PASS |
| **negative** — the phrase family **is** found in `FORMULATION.md`, where it demonstrably is | PASS — so a miss in the gated pair is the document, not the search |
| **placebo** — a phrase occurring nowhere | **0 hits** in all three documents |

**MULTIPLICITY:** 3 facts × 3 documents + 4 controls. All reported.

**IMPOSSIBLE, named:** whether the ③b bound is **correct** needs re-running R336–R338, which this
site cannot do. **What is decidable is whether it is RECORDED where the project's own rules say a
retraction and a limit must live.** ⚠ And a *mention* is not a *recording* — which is precisely the
distinction v1 got wrong.

## ⛔ Check #230 — the checks catch, they do not prevent
*"outside every gate"* **repeats check #228's exact overstatement, one round after recording it.**
⭐ *Recording a correction did not stop it recurring in the very next closing line* — which is why
the ledger outranks the resolution to be careful.

## The sentence I can no longer write
> *"clause ③ is half the live definition and its limit is stated somewhere."*

**It was stated in exactly one place, and that place is outside every instrument the project owns.**

## NEXT
R630's status instrument asked only the ledger, and this round shows in-document supersession is a
second channel it cannot see. **Re-run R630's classification with both channels** — ledger membership
*and* an in-document "retracts R###" scan across `FORMULATION.md` — because if other UNSETTLED or
LIVE findings are superseded in prose the same way, the ledger has been overstating what stands by
more than one entry, and that count is what decides whether this is a fix or a sweep.

# R644 · No third object exists, and none can be built — the specification is the deliverable

**Decision this makes safe:** whether clause ② can ever be tested cross-object here. **Not on this
disk.** R618's five-field specification **is** the deliverable, and the arc can close on it.

| file | keys | ① ② ③ ④ ⑤ | total |
|---|---|---|---|
| `conversation_rubrics.jsonl` | 6 | ✓ · ✓ ✓ ✓ | **4/5** |
| `utterances.jsonl` *(second release)* | 14 | ✓ ✓ ✓ · · | 3/5 |
| `merged_comparisons_annotators.jsonl` | 11 | ✓ · ✓ · · | 2/5 |
| `annotators.jsonl` | 9 | ✓ · ✓ · · | 2/5 |
| `comparisons.jsonl` | 6 | ✓ ✓ · · · | 2/5 |
| **`metadata.jsonl`** *(85 MB, opened by no round)* | 10 | ✓ · · · · | **1/5** |
| **HOME RELEASE** (union) | — | ✓ ✓ ✓ ✓ ✓ | **5/5** |
| **JOIN of every non-home file** | — | ✓ ✓ ✓ · · | **3/5** |

⭐⭐ **The pool and the core exist in exactly one file and nowhere else.** Joining all six non-home
files still yields **3/5** — the two missing fields are missing from the *disk*, not from any
particular file. **The object is not merely un-found; it is not constructible from what is here.**

## ⚠ And the measurement refutes my own framing, one command later
Going to the object, I wrote that *"the arc has spent forty rounds on two files while six sit in the
same directory"* — an implicit criticism. **The 85 MB file no round has ever opened carries a single
field.** ⭐ *The neglect I was about to report does not exist, and I had already written the sentence.*

## Controls
| control | returned |
|---|---|
| **positive** — the home release, the object the definition was written from | **5/5** — PASS |
| **negative** — the second release must score **exactly 3/5, missing pool and core** | **PASS** — reproduces R618 |
| **g=0** — a file with no keys | **0/5** — PASS |
| **placebo** — a key no file uses | **0 files** — PASS |

⭐ **The negative control is the load-bearing one**: it requires the instrument to reproduce a
*published* number, so drift would show as a different score rather than as silence.

**MULTIPLICITY:** 6 files × 5 fields + the union + the join + 4 controls. Full table printed.

**IMPOSSIBLE, named and unchanged from R618:** **key presence is NECESSARY, NOT SUFFICIENT.** A file
can carry all five and still be useless — **R602 measured the second corpus as disjoint in content**
(exact overlap 0, token-Jaccard at the shuffled floor), and **no schema check can see that.** This
screens out impossibilities; **it cannot certify a site.**

## ⛔ Check #245
*"The production work is **done**"* — the four inert sites still carry the old code; the
**corrective** work is done, the **preventive** installation is not. *"The arc's **oldest** open
item"* — never enumerated.

## The sentence I can no longer write
> *"check whether any object on disk satisfies the five fields — if one does, the definition becomes
> testable cross-object."*

**None does, and no join of them does either.** The conditional's antecedent is false on this disk.

## NEXT
The arc's central question now has a closed answer with a scope: **`② ∧ ③` is evaluable on exactly one
object, and the five-field specification states what a second would have to provide.** The remaining
work is not another measurement but a **decision**: either accept that the definition is
single-object-bound and say so in the claim table where every row currently reads as unconditional,
or find an object off this disk. **Check first whether the claim table already carries that scope
qualifier** — because R601 measured every claim row as home-release-only and *not saying so*, and if
that is still true the honest close is one edit, not one experiment.

R679 · PRE-REGISTRATION — written after grepping WHICH lines exist, before resolving any of them.

⚠ DISCLOSURE. Locating the population printed 4 matching lines and I read them, including L192
  `| extension here | 5† arms (①∧②∧④) | 0† |`. So "at least one row prints 5 for a non-③ set" is an
  OBSERVATION, not a forecast, and no prediction is scored on it. What is blind: the full population
  once widened to every asserted extension size, how each row RESOLVES via its cited round, and how
  many distinct sets sit behind each printed number.

ESTIMAND A  over every STATEMENT.md line asserting an extension SIZE, how many resolve — via the
            round each cites, through R678's producer map — to the ③ extension R294.admitted?
ESTIMAND B  how many DISTINCT sets sit behind the single printed number "5"?
POINT A     6 of the widened population resolve to R294.admitted
INTERVAL A  [2, 12]
POINT B     2 distinct sets behind "5"
INTERVAL B  [1, 3]
DIRECTIONAL (separately failable) at least one line resolves to a set that is NOT a ③ extension
            while printing the same number as one that is.
KILL        if every line resolves to the same set, there is no collision in the DELIVERABLE and the
            finding stays confined to the artifact corpus — R676's census — which would materially
            weaken it.
CONTROLS
  POSITIVE  L127 lists its five members explicitly; it must resolve to R294.admitted.
  g=0       a line asserting no extension must resolve to NOTHING — if everything resolves, the
            resolver is matching rather than locating.
  NEGATIVE  a synthetic line citing a round id that does not exist -> unresolved.
  PLACEBO   the resolution run twice is identical.
IDENTIFICATION LIMIT  a line's cited round is taken from its own `(R###)` markers. A line whose
            number came from a round it does not cite is attributed wrongly, and that is exactly the
            defect R674 measured corpus-wide at 47.5%. Reported as a bound, not corrected.

# R720 · premise or contrast — the rule R719 proposed would have killed the last standing block wrongly

**Of the `3` blocks citing a retracted literal, `1` uses it as a PREMISE (R688 — "the 3 published
arms") and `1` as a CONTRAST: the lineage block names `R442.published_five` **to say its own
five-member set is a different object**. R719's naive rule yields a residue of `0`; the role-aware
rule yields `1`. ⭐ **A block that cites a retracted literal to say it is NOT that does not rest on
it.** ⚠ And it owes a **gloss correction**.**

## check #322 — it holds, and its rule is too coarse

✓ `blind_spot_blocks` is in R719's artifact; the lineage block carries 62 rounds without the
retracting one.

⛔ But *"a retraction anywhere reaches every block citing the retracted literal"* would have retracted
the last standing block **for the wrong reason**. The lineage block's "number 5" is **③'s
extension** — `coval_core, topw_k3, topw_k4, topw_k6, topw_k8` — a *different* set, and it names
`published_five` precisely to distinguish itself: *"Five other five-member arm sets are committed in
this corpus and denote other objects"*.

## the three citing blocks

| role | gloss | asserts | block |
|---|---|---|---|
| **RETRACTION** | — | no | `THE RELEASE SHIPS ONE CORE GENERATOR AND 986 INSTANCES` |
| **PREMISE** | — | yes | `§4's FALSIFIER AGAINST ③ — UNEVALUABLE` *(downgraded by R719)* |
| **CONTRAST** | **STALE** | yes | `LINEAGE OF THE EXTENSION — what supports the number 5` |

## what residue each rule produces

| rule | residue |
|---|---|
| naive (any citation kills) | **0** |
| **role-aware (PREMISE kills)** | **1** |
| role-aware + gloss recorded | **1**, with **1** gloss correction owed |

**The naive rule over-kills by 1.**

## ⭐ the corrected residue

**`⭐ LINEAGE OF THE EXTENSION — what supports the number 5` (R676–R682).**

⚠ **It owes a gloss correction**: it repeats *"CoVal's publication list"* — the exact description
R689 retracted — without citing R689. **That is a defect in the PROSE, not in the CLAIM**, and
conflating the two is precisely the over-kill this round exists to prevent.

## controls — 6 PASS, 0 FAIL, two runs byte-identical

| control | returned |
|---|---|
| **POSITIVE** | R688 → **PREMISE**, lineage → **CONTRAST** — the reading separates two independently stateable roles |
| **g=0** | blocks citing no retracted literal → `role=None`, never a default |
| **NEGATIVE** | the block **recording** the retraction reads as `RETRACTION` — the retraction is not counted as a victim of itself |
| **SHAM** | contrast markers removed → the lineage block flips to **PREMISE**, so the markers are the ingredient |
| PLACEBO / UNIT | identical runs differ by 0 · instrument unit ≠ claim unit |

## ⛔ my residue replaced R718's criterion instead of adding to it

First version: `asserts AND role != PREMISE` → **residue 3**, counting R701's and R704's blocks —
which **R718 had already found amended by later rounds**. The registered point (1) would have landed
inside its interval **for the wrong reason**.

**A new criterion is a CONJUNCT, never a substitute.** With both ANDed, the residue is **1**.

## registered vs observed

| | registered | observed |
|---|---|---|
| **A** blocks citing a retracted literal | 3 [1, 6] | **3** |
| **B** of those, PREMISE | 1 [0, 3] | **1** |
| **C** corrected residue | 1 [0, 4] | **1** |
| directional | the two rules disagree | **HOLDS**, by 1 |

## limits

- **The role reading is a READING**, positive-controlled against two independently stateable blocks —
  not a measurement.
- ⚠ **Unattacked is still not true.** R719 showed a block can stand for thirty rounds on a withdrawn
  set.

## impossible here

| criterion | what it would require |
|---|---|
| deciding whether the surviving claim is true | an attack, which is what a later round is for |
| cross-release | a second release |

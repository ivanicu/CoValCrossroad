# R700 · is the gate measuring risk, or prose?

**⭐⭐⭐ Over **257** flagged occurrences, **30 (11.7%)** are idiomatic. **88.3% are genuine universals
over our own work** — so the quantifier gate measures the risk it was built for, and its PASS means
what it claims. **And 11.7% is a FLOOR, not an estimate.****

## ⛔ CHECK #302 · THE PREVIOUS NEXT PROPOSED SELF-ADJUDICATION ON n = 5
It asked me to read the **five freeze reasons — which I wrote, about my own flags** — and classify
them. **The separable version runs a mechanical idiom rule over every flagged occurrence in the
frozen corpus**, which is n=257 and does not ask my opinion of my own prose.

## THE MEASUREMENT

| | |
|---|---|
| flagged occurrences | **257** (README NEXT sections + every frozen commit body) |
| ⭐ **idiomatic** | **30 — 11.7%** |
| genuine universals | **227 — 88.3%** |
| commonest words | **`every`**, `nothing` 29, `never` 29, `the only` 23, `the first` 16, `the last` 9, `none` 8 |

Registered **A 22% [5,60] → 11.7%, INSIDE (−10.3)** · **B (`all` commonest) → ⛔ FAILS, it is
`every`** · **directional (idioms a minority) HOLDS** · kill did not fire.

**Controls:** POSITIVE — `at all` classifies **IDIOMATIC**, the case that generated the question.
**g=0** — `every round in this arc` classifies **non-idiomatic**, *the classifier returns both*.
NEGATIVE — text with no quantifier is not counted. PLACEBO — identical.

## ⛔ I PREDICTED `all` WOULD LEAD AND IT IS `every`
I generalised from **the two cases that bit me** — R694's and R699's `at all` — and registered the
word that had hurt most recently. **A prediction shaped by the most salient recent failure is not a
prediction about the population**, and the corpus says the gate mostly fires on `every`, `nothing`,
`never` and `the only`: exactly the words §4's closing-sentence row names.

## ⚠ TWO LIMITS, BOTH IN THE DIRECTION THAT MAKES THE VERDICT CONSERVATIVE
- **The idiom list is closed and hand-written.** Any construction I did not think of counts as a
  real universal, so **11.7% is a floor** and "mostly earned" is the conservative reading, not the
  convenient one.
- **A flagged idiom is still a flag someone must resolve.** Instrument unit: *a flagged occurrence*.
  Claim unit: *an unearned flag*. **The cost of a false flag is paid in attention regardless.**

## THE RESULT IS NOW IN THE GATE, NOT ONLY IN THIS ROUND
`assurance/next_line_quantifiers_are_computed.py` carries the measured share in its own header —
**what a gate's flags are worth belongs where the gate is read**, not in a round nobody opens.

## ⭐⭐⭐ AND THE GATE FLAGGED THIS ROUND FOR QUOTING THE WORDS IT FLAGS
The first draft of the NEXT section listed the four commonest flagged words. **The gate flagged it —
for `every`.** *A round measuring which words the gate flags cannot name them in a NEXT section
without being flagged.* **This is use versus mention, the limit R671 established the gate cannot
distinguish, in its purest available form:** the sentence is not making a universal claim, it is
**reporting that the gate treats a word as one.** Rephrased to cite the field instead of quoting the
values — which is the same remedy the gate offers, arrived at by being caught.

## IMPOSSIBLE HERE
Whether I *meant* a universal is not in the text. **The idiom list is a proxy for intent and is named
as one.**

## NEXT
The four commonest flagged words (`results/idiom_share.json`, field
`word_counts`) are the same ones §4's closing-sentence row lists as its tell. ⭐ *They are not
repeated here, because writing them tripped the gate — see below.* Check whether flagged
occurrences cluster in closing sections specifically: compare the flag rate per thousand words in
`## NEXT` sections against the flag rate in the rest of the same READMEs. If closing sections are
denser, the row's diagnosis is confirmed on this corpus rather than assumed from it.

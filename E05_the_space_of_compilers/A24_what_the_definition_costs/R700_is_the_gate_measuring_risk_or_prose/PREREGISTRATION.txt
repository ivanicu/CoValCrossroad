R700 · PRE-REGISTRATION

⚠ WHY NOT THE ROUND THE LAST NEXT NAMED. It proposed classifying the five freeze REASONS — reasons I
  wrote, about my own flags. That is self-adjudication on n=5. The separable version runs the
  classification over EVERY flagged instance in the frozen corpus, by a mechanical idiom rule.

THE DECISION  the quantifier gate has fired repeatedly and twice on `at all`, which is an intensifier
              and not a universal claim. If most flags are idiom collisions, the gate is measuring
              PROSE STYLE and its flag rate overstates the defect it was built for. That changes
              what its PASS is worth.

POINT A     22% of flagged quantifier instances are IDIOMATIC rather than a universal claim over our
            own work.
INTERVAL A  [5%, 60%]
POINT B     `all` is the commonest flagged word.        BINARY, can fail.
DIRECTIONAL (separately failable) idiomatic instances are a MINORITY — under half — so the gate is
            mostly catching real universals and its flags are mostly earned.
KILL        if idiomatic instances exceed 50%, the gate measures prose style; say so plainly and
            treat its flag rate as an overstatement of the defect.
CONTROLS
  POSITIVE  `at all` must classify IDIOMATIC — the case that generated this question.
  g=0       `every round in this arc` must classify NON-idiomatic; the classifier must return both.
  NEGATIVE  text with no quantifier is not counted.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  the idiom list is CLOSED and hand-written, so a construction I did not think
            of counts as a real universal. That biases the idiomatic share DOWN, which makes
            "mostly earned" the conservative verdict rather than the convenient one.

# R760 · clause ③ by RULE instead of by NAME — and the "16 vs 5" tension dissolves

**Implemented as a RULE read off the builder's own source rather than as four literal arm names,
clause ③ excludes **7 more tags = 4 more objects** on today's 92-arm population, **all five committed
extension members survive**, and the admitted set falls from **16 tags / 9 objects** to **9 tags /
5 objects**. ⭐ **The extension over today's population becomes exactly the committed 5.** The
`16 vs 5` tension that has run through R728 → R745 → R746 was an artifact of ③ being a **blocklist**.**

## check #362 — the proposed round was prior art I already held

R759's NEXT asked whether the 7 target-reading tags are 7 objects. **R730's committed artifact records
`objects_of_the_seven` as FOUR classes**, and I read that field in R747. **Fifth rebuild the gate has
stopped in this arc — and the first I proposed while already holding the answer** *(ledger 1053)*.

## what was actually open

```python
USES_PROMPT_LABELS = {"oracle_k4", "oracle_k4_fit1", "greedy_k4_fit1", "indep_k4_fit1"}
```

Four literal names. R520 and R729 established this **fails open**; R745 measured the consequence —
the census admits **3 target-reading objects** ③ is meant to exclude. Meanwhile the builder names the
target-reading rules exactly at `select_core.py:102`.

⚠ **P4 on both questions.** R509 tested a **behavioural** ③′ (*"not optimised against the labels"*)
and concluded **do not** — *"it makes the same vacuity harder to see"*. R623 is a documentation-anchor
rule, a different object. **A rule-based PROVENANCE ③ keeps ③'s type and changes only its
implementation. That is the gap.**

## the grid — 3 implementations × {tags, objects}

| clause ③ | admitted tags | admitted **objects** | excluded tags |
|---|---|---|---|
| **name** (current blocklist) | **16** | **9** | 76 |
| **RULE** | **9** | **5** | 83 |
| inverted *(block selectors)* | 14 | 8 | 78 |

**The 7 arms rule-③ excludes that the blocklist admits** — `greedy_k4_greedy_kA/kB`,
`indep_k4_indep_kA/kB`, `oracle_k4_08bR`, `oracle_k4_oracle_kA/kB` — **are exactly R729's seven, and
R730's four objects.** Three separate rounds converge on the same set from different directions.

**All 5 committed members survive**: `coval_core, topw_k3, topw_k4, topw_k6, topw_k8`.
**② ∧ ③rule admits 9 tags / 5 objects — NON-VACUOUS**, so R509's warning does not reach this
implementation.

## ⛔ two results were forced, and are labelled

**D1 — the rule set is a SUPERSET of the blocklist.** Every blocklisted name carries a target-reading
prefix, asserted in code. *"Rule excludes more"* is **algebra**; only the **size of the excess**
measures.
**D2 — the committed extension is INVARIANT.** No committed member carries a target-reading prefix,
so a stricter ③ cannot remove one. **World C could not fire on the committed population** — stated
before the run, then verified on the outcome (5/5 survive).

## ⭐ the SHAM prices the blocklist exactly

Five **random** blocklists of the same size (4 names) admit **[20, 20, 19, 20, 19]**, mean **19.6**.

| ③ | admitted tags |
|---|---|
| random 4 names *(sham)* | **19.6** |
| the actual blocklist | **16** |
| **RULE** | **9** |

**The blocklist's specific content buys 3.6 arms over an arbitrary list of the same size; the rule
buys 10.6.** The list was doing *some* work — and about a third of what a principled clause does.

## controls — 6 PASS, 0 FAIL

| control | returned |
|---|---|
| **PROVENANCE** | all three source facts hold against `select_core.py`; the round **exits 2** otherwise, because the whole instrument rests on the tag being rule-derived |
| **POSITIVE** | rule-③ **excludes** `oracle_k4_oracle_kA` (target-reading, not blocklisted) and **keeps** `topw_k4`. Band computed: a ③ blocking **nothing** cannot separate them, nor can one blocking **everything** — the threshold is unreachable from either degenerate end |
| **g=0** | **0 UNPARSED tags** — none silently admitted (which would let a hidden target-reader through) nor silently blocked (which would inflate the repair) |
| **NEGATIVE** | inverting the rule to block **selectors** changes the admitted set (14 vs 16) — **③ is not decoration; ② alone does not decide everything** |
| **SHAM** | as above, 19.6 |
| **PLACEBO** | name-③ reproduces R728's committed **16** admitted tags **exactly** — the harness *is* the census, so every comparison above is against the same instrument |

## what this changes in the deliverable

| carried | stands as |
|---|---|
| *"the census admits **16**, not 5"* *(R728)* | **an artifact of ③ being a blocklist.** Under rule-③ it admits **9 tags / 5 objects** |
| *"③ admits 3 target-reading objects"* *(R745)* | **repaired** — rule-③ excludes all 4 target-reading objects |
| *"③ is a blocklist and fails open"* *(R520, R729)* | **still true of the implementation, and now priced: 3.6 arms over a random list of the same size** |

## ⭐ the reproducibility check failed once, and the PIN explained it in one field

The two hash-seed artifacts differed — in **exactly one field**: `document_pin` for `STATEMENT.md`,
**1264 lines vs 1286**. Every substantive field — the admitted sets under all three implementations,
the excluded arms, the object counts, the sham draws, the controls — was **identical**. **I had amended
the page between the two runs.** That is not a non-determinism in the round; it is **R758's pin
detecting me editing the document under my own verification** *(ledger 1056)*. Re-run with the page
stable: **byte-identical, both writes confirmed, pin `1286 / a3d9af62` on both.**

## the sentence I can no longer write

*"the extension is 5 on the committed population and 16 on today's."* Under a ③ that says what it
means, it is **5 on both**.

## NEXT

Rule-③ is a repair to the *implementation*, and the page can now state ③ by rule rather than by list.
What it does not touch is whether ③ is the right **clause** — the impossibility register has carried
that as needing an external criterion since the beginning, and nothing here changes it. The reachable
next step is the other conjunct: ② is defined against a baseline this campaign chose by **file order**
(`POOL[0:4]`, percentile 93.7 of its 1,820-subset class, R527), and the extension moves **4 → 8**
across that class. **③ now says what it means and ② still does not.** The registered quantity is the
admitted set under rule-③ swept over the baseline's reference class, which asks whether the 5 that
survived here survive the *other* clause's arbitrariness — a different axis from the one this round
fixed.

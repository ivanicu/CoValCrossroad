R709 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #311 ON THE LAST NEXT LINE — THE CITATION RESOLVES, THE SUPERLATIVE DOES NOT.
  ✓ STATEMENT.md:141-142 does say F1's scope "rests on ONE VERDICT PAIR (R683, R685)". Citation good.
  ⛔ "the weakest load-bearing claim in the deliverable since R685" is an UNVERIFIED SUPERLATIVE over
    my own work — I never ranked the deliverable's claims by strength. §4's closing-sentence failure,
    third time in this arc. Withdrawn: F1's scope is *a* claim resting on n=1, not demonstrably the
    weakest. This round tests the n=1, not the ranking.

⛔ AND §4's OWN REMEDY, APPLIED: count what the release CONTAINS against what the code CONSUMED.
  11 rounds in this repo carry BOTH judge keys. R685 examined 7. ⚠ The 4 it skipped are R683-R686 —
  the instrument rounds themselves — so that exclusion is SELF-INCLUSION CONTROL, correct and not a
  miss. World B is therefore nearly dead before the run and is registered anyway, because a world I
  expect to die is exactly the one I should not quietly drop.
  ⭐ THE LIVE GAP IS THE DEFINITION. R685 reduced 7 rounds to 1 pair by counting ONLY booleans and
    small-closed-set strings, stating that "continuous per-judge values differ by construction and
    are EXCLUDED". But F1's scope claim IS about a continuous quantity's SIGN across judges — "the
    separation does not hold at 0.8B". So the definition that produced n=1 excludes the very
    evidence type the claim is made of.

ESTIMAND
  The number of per-judge comparisons in this corpus that are INFORMATIVE about F1's scope claim,
  under three nested definitions, with the agreement rate at each:
    D1  R685's — booleans and small closed-set strings only
    D2  D1 + SIGN agreement on continuous per-judge numerics
    D3  D2 + ORDERING agreement on per-judge numeric vectors
  Nested by construction, so D1 <= D2 <= D3 is a DERIVATION and is not evidence; the counts are.

IDENTIFICATION
  Identified from artifacts on disk. ⚠ "informative about F1's scope" is operationalised as
  "carries a value under both judge keys that admits a same/different verdict"; that operationalisation
  is mine and is the round's main assumption, so every pair is listed by field name in the artifact.

SCOPE
  population : the 7 non-self rounds carrying both judge keys (R361 R362 R365 R477 R479 R481 R540)
  instrument : a JSON walk for dicts whose keys include both "0.8B" and "2B"
               instrument unit = A PER-JUDGE FIELD
               claim unit      = F1's SCOPE CONDITION
               ⚠ NOT EQUAL — a field that differs across judges does not by itself bear on whether
               the ③ SEPARATION holds; that link is asserted per field and is named, not assumed.
  baseline   : R685's committed n_pairs = 1
  regime     : this repository at HEAD

WORLDS
  A THE LIMIT IS REAL      D2 = D3 = 1 ⇒ the corpus really holds one pair; F1's scope stands
  B THE SEARCH MISSED IT   rounds outside R685's 7 hold pairs ⇒ under-powered by my search
  C THE DEFINITION MADE IT D2 > D1 ⇒ n=1 is a definitional artifact and F1's scope is under-powered
                           by a rule that excludes the evidence type the claim is made of

PREDICTION MATRIX          A        B        C
  D1 pairs                 1        1        1
  D2 pairs                 1       1..2     > 1
  extra rounds found       0       > 0       0

REGISTERED
  POINT A  pairs under D1 over the 7 rounds: 1, interval [0, 3]   (a replication of R685)
  POINT B  pairs under D2: 5, interval [1, 15]
  POINT C  agreement rate under D2 — the share where the two judges agree on SIGN: 0.60, [0.20,0.95]
  DIRECTIONAL (separately failable)  D2 pairs > D1 pairs.
  KILL (conditional — evaluated ONLY if POSITIVE recovers R685's pair and g=0 returns 0):
     D2 == D1 == 1 → world A, the limit is real and F1's scope condition stands as written;
     D2 > 1        → world C, and STATEMENT.md's "rests on one verdict pair" must be corrected.

CONTROLS
  POSITIVE     R685's own pair — `rank_resolved` in R361, {0.8B: False, 2B: True} — must be recovered
               by D1. A finder that cannot re-find a known-present object is not a finder.
  g=0          a synthetic artifact carrying both judge keys but no comparable value must yield 0
               pairs. The finder must not manufacture a pair from the keys alone.
  NEGATIVE     strip the judge keys from every artifact and re-run → 0 pairs. The world it excludes:
               "the finder responds to field NAMES rather than to the judge KEYING".
  SHAM         drop the judge-keying requirement and count same-named fields anywhere. Same walk
               minus the ingredient; it isolates what the keying is worth.
  PLACEBO      two identical runs differ by exactly 0.
  SELF-INCLUSION  R683-R686 excluded BY NAME and the exclusion COUNTED — it is the whole difference
               between 11 and 7 and would otherwise read as a search failure.
  UNIT         instrument unit != claim unit, asserted and carried into the verdict.

SPECIFICATION SWEEP
  definition ∈ {D1, D2, D3} × population ∈ {the 7, all 11 incl. self} = 6 cells, all reported.
MULTIPLICITY  6 cells; every cell printed with its pair list; none selected.
SEEDS  not applicable — the walk is deterministic; stated rather than faked.
ARTIFACT  results/pairs.json — every pair by round, field, values and verdict, so a later round can
          disagree with the operationalisation without re-deriving the walk.
IMPOSSIBLE
  a THIRD judge — the release ships two; no rate over 2 bounds anything about a third.
  construct validity of "informative about the separation" — needs a criterion outside this repo.

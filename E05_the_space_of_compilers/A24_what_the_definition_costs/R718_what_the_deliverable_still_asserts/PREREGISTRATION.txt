R718 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #320 ON R717's NEXT LINE — ITS COUNT IS WRONG BY ONE, AGAIN IN A CLASS THE GATE CANNOT CATCH.
  ⛔ "three consecutive rounds have ended in a bound" is FALSE. R715 produced a VALUE — a separating
    predicate at 0.0655 — and only R716 and R717 ended in bounds. It is TWO. Seventh false closing
    claim in this arc, and the third of the precedence/count species the quantifier gate cannot see.

⛔ AND ITS PROPOSAL NEEDS AMENDING BEFORE IT IS RUN.
  It asks me to count rounds that ended in a bound versus a value. §0.2 forbids exactly that as a
  product: "never lead with the ledger, never end on it", and a count of my own retractions is the
  activity metric that section opens by forbidding. The admissible question is the one §0.2 demands
  instead: WHAT STANDS. So the object is the DELIVERABLE, not the 440 artifacts.

ESTIMAND
  For each claim block on STATEMENT.md: (i) whether it ASSERTS something or RECORDS a withdrawal,
  and (ii) whether a LATER round has amended it — computed as "a round cited inside the block's body
  is numbered higher than the highest round cited in its heading".
  Then the production statement: how many blocks assert something no later round has amended.

IDENTIFICATION
  (ii) is exactly computable from the file's own citations. ⚠ (i) is read from the heading's marker
  (⭐ asserts, ⛔ withdraws, ⚠ qualifies), which is a CONVENTION I wrote, so it is validated against
  blocks whose status I can state independently, and is otherwise a reading rather than a measurement.

SCOPE
  population : the claim blocks of E05/STATEMENT.md at HEAD
  instrument : citation-number comparison between a block's heading and its body
               instrument unit = A CLAIM BLOCK
               claim unit      = WHAT THE CAMPAIGN HAS PRODUCED
               ⚠ NOT EQUAL — a block standing unamended is not thereby true; it is unattacked, and
               this arc has shown four blocks were wrong while standing.
  baseline   : the block count itself, and the marker convention
  regime     : this repository at HEAD

WORLDS
  A PRODUCTION       most blocks assert and stand unamended ⇒ the arc has a residue
  B LEDGER           most blocks record withdrawals ⇒ §0.2's failure: the audit trail IS the product
  C CHURN            most asserting blocks carry a later amendment ⇒ the statement is unstable and
                     nothing has settled

PREDICTION MATRIX          A         B         C
  asserting blocks        most      few       most
  amended blocks          few       any       most

REGISTERED
  POINT A  claim blocks on the statement: 12, interval [8, 20]
  POINT B  blocks whose heading ASSERTS (⭐) rather than withdraws (⛔) or qualifies (⚠): 4,
           interval [2, 10]
  POINT C  asserting blocks carrying a LATER round's amendment in the body: 2, interval [0, 4]
  DIRECTIONAL (separately failable)  more blocks are withdrawals-or-qualifications than assertions —
           i.e. this arc's statement records more correction than assertion.
  KILL (conditional — evaluated ONLY if the POSITIVE fires and g=0 returns nothing):
     asserting blocks that stand unamended == 0 → world B, and §0.2's charge is proven against this
       campaign: the ledger is the product;
     otherwise → name them, because that list IS the residue and is what §0.2 asks be reported FIRST.

CONTROLS
  POSITIVE   two blocks whose status I can state independently — R701's three-clause formulation
             ASSERTS, R696's A2-circularity block WITHDRAWS — must be classified accordingly.
  g=0        a synthetic block with no citation at all must yield NO amendment verdict, not a silent
             "unamended" pass. Absence of citations is UNKNOWN, never clean.
  NEGATIVE   scramble which body belongs to which heading; the amendment count must change. The world
             it excludes: "every block cites a later round somewhere, so the test is free".
  SHAM       run the same amendment test on DEFINITION.md, which is an append-only record and should
             show far fewer heading/body citation inversions.
  PLACEBO    two identical runs differ by exactly 0.
  UNIT       instrument unit != claim unit, asserted and carried into the verdict — and the verdict
             must state that standing unamended is NOT truth.

SPECIFICATION SWEEP
  document ∈ {STATEMENT.md, DEFINITION.md} × marker source ∈ {heading only, heading+first body line}
  = 4 cells, all reported.
MULTIPLICITY  4 cells; counts are exact, so no p-values are computed and none are implied.
SEEDS  used only for the negative control's scramble; 3 streams.
ARTIFACT  results/standing.json — every block with its heading, its cited rounds, and its verdict.
IMPOSSIBLE
  whether an unamended block is TRUE — that is the campaign's whole question and no self-audit can
    answer it; four blocks in this arc were wrong while standing.
  cross-release.

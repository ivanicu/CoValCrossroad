PRE-REGISTERED BEFORE ANY CODE WAS WRITTEN FOR R661 — 2026-08-05, committed with the round.

CONTEXT: R660 measured that 53 of 290 rounds declaring an IMPOSSIBLE register (18.3%) are named by
a wall-entry, and its NEXT proposed RECENCY as the rival. That rival has its arrow backwards: a
ledger entry can only name a round that ALREADY EXISTED, so an OLD round has had MORE chances to be
named than a new one. Exposure runs the other way.

QUANTITY: rho = Spearman correlation between a round's id and its EXPOSURE-CORRECTED naming rate,
where exposure = the number of wall-entries written after that round existed.

POINT |rho|      : 0.20
INTERVAL         : [0.00, 0.60]

DIRECTIONAL PREDICTION, the real content and separately failable:
  the exposure-corrected profile is NOT MONOTONE in round age. Neither recency nor exposure explains
  the naming rate; the variation is about the rounds themselves. Pre-run evidence: the raw mention
  distribution peaks at R450-499 (27) while the most recent full block R550-599 has 3, the second
  lowest -- a middle peak is not what either rival predicts.
KILL             : if |rho| exceeds 0.60 AND the profile is monotone, the directional prediction is
                   RETRACTED and the naming rate is an artifact of when a round was written.

⚠ AND rho NEEDS ITS OWN FLOOR. R660's whole finding was a share reported without one. A permutation
  null over the round->entry assignment is computed here, >= 5 seeds, and |rho| is read against that
  spread rather than against zero.

⚠ FORECAST RECORD: six pre-registered magnitudes -- four over, two under; six directional -- five
  held, one retracted (R660, on a metric that turned out to carry no information). Not shading.

R703 · PRE-REGISTRATION

⚠ THE PREVIOUS NEXT'S EXAMPLE IS WRONG, AND THE GAUGE TEST SAYS SO FOR FREE.
  It proposed `random_k4_s0` as something F1 admits that a reader would refuse. `random_k4_s0` is
  label-free, so F1 does admit it — but it FAILS F2, so the CONJUNCTION refuses it. Naming an object
  one clause admits is uninformative when another clause catches it.

THE SHARP FORM  §4 warns that a clause excluding nothing the others already exclude is "untested
  decoration". R519 ran exactly this on the old numbering and retired ① and ④ because each dropped
  ZERO. ⭐ The same test has never been run on F1/F2/F3.

ESTIMAND    for each clause, how many objects does it exclude that the OTHER TWO admit — its UNIQUE
            exclusions?
POINT A     3 of 3 clauses have at least one unique exclusion.
INTERVAL A  [0, 3]
POINT B     F3's unique exclusions number 3 (`topw_k1` below the floor; `topw_k6`, `topw_k8` above
            the repaired ceiling).
INTERVAL B  [0, 10]
DIRECTIONAL (separately failable) every clause is load-bearing — none drops zero uniquely.
KILL        if any clause has ZERO unique exclusions it is decoration and must be retired exactly as
            ① and ④ were. That is live: F2's boundary is 95.2% recoverable from parameters we chose
            (R694), so it is the candidate.
CONTROLS
  POSITIVE  the method must REPRODUCE R519's known result — clause ① drops 0 of ②'s passers. If it
            cannot recover a published finding, its zeros are silence.
  g=0       a clause known to exclude something (F3's floor, which excludes `topw_k1`) must return
            non-zero — the method must return both.
  NEGATIVE  an arm outside the ledger is UNSCORED, never counted as excluded.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  F1 and F2 are read from R360's committed clause verdicts (③ and ② as
            implemented); F3 is computed from the `k` field. So this tests the clauses AS THE LEDGER
            IMPLEMENTS THEM, not as R701's prose states them — and where prose and code differ, this
            measures the code.

# R628 · The bound, written as four predictions and only committed because they held

> ## Provenance cannot be INFERRED. It must be DECLARED, and then checked.

**Decision this makes safe:** what the assurance suite may be said to establish. **A declared
binding, not a measured number.**

| intervention | predicted | observed | |
|---|---|---|---|
| a value cited to a round whose verdict is **UNVERIFIED** | CAUGHT | **CAUGHT** — `statement_provenance` | ✓ |
| a value **laundered** through `DEFINITION.md`, then cited to a settled round | NOT CAUGHT | **NOT CAUGHT** | ✓ |
| a **real** artifact value asserted as a **different quantity** in a section citing its round | NOT CAUGHT | **NOT CAUGHT** | ✓ |
| a **drifted** value for a label the gate re-derives | CAUGHT | **CAUGHT** | ✓ |

**A register entry is a claim about an instrument, so it was written as a prediction and tested —
not asserted.** All four held; the block is committed.

## ⛔ Check #227 — the eleventh uncomputed universal, false *pessimistically*
*"Every mechanical route to provenance has now failed"* — **R621's exact-string intervention worked**,
and `derive()` re-derives 119 values and would break on drift. **What failed is narrower: routes that
INFER provenance from value coincidence.** ⭐ *So the bound is not "provenance cannot be mechanised"
but "provenance cannot be inferred" — a different sentence with a different consequence for the next
site.* Second instance this arc of a closing line **manufacturing** a fault rather than excusing one.

## ⭐ The suite caught me twice while I was writing the register about it
1. The new block's decimals had no anchor in `DEFINITION.md` → **`statement_provenance` failed**, which
   is exactly the behaviour line 1 claims. Fixed by appending the matching record.
2. It then failed again: **I cited R625 without declaring that R625's verdict is `UNVERIFIED`.** Its
   *decomposition* failed its controls; **the null is the measured part.** Both citing sites now say
   so. *A register written about an instrument is the best possible test case for it.*

## ⚠ Mechanism correction inside the register itself
Line 4's drift was caught by **transitive anchoring** — the statement's copy became orphaned — **not by
re-derivation.** So **a drift in a `DEFINITION.md` value that `STATEMENT.md` does not also carry is
untested by this design.** The register says this rather than implying re-derivation is the guard.

## Controls
| control | returned |
|---|---|
| **positive** — line 1 must be caught | **CAUGHT** — so every NOT CAUGHT is a measurement, not silence |
| **placebo** — a no-op trailing newline | **0 verdict changes** |
| **negative** — the tree must return to all-pass after every arm | PASS, checked after each |

**MULTIPLICITY:** 4 interventions × 6 gates = 24 cells + 3 controls. All reported.

**IMPOSSIBLE, named — and it is in the register, not a footnote:** **completeness is not testable from
the register's own lines.** A defect class no line names is invisible to a test derived from the
lines. What is claimed is that these four behaviours are what they say — **not that they exhaust the
ways a number can be wrong.**

## The sentence I can no longer write
> *"the assurance suite establishes that the deliverable's numbers are real."*

**It establishes that a declared binding holds.** Two of four interventions pass through it untouched,
and both were predicted to.

## NEXT
The register now states what the suite does; `FORMULATION.md` is still outside all six gates (R621
measured it flipping zero, and that finding stands independently of the collision floor since it was
an exact-string intervention). **Ask what `FORMULATION.md` asserts that `STATEMENT.md` does not** —
if the answer is nothing, it is a duplicate and should be archived under L81 rather than gated; if it
asserts something, that content is currently ungoverned and the arc has been auditing the wrong file.

PRE-REGISTERED BEFORE ANY CODE WAS WRITTEN FOR R660 — 2026-08-05, committed with the round.

QUANTITY: of R659's 36 wall-entries that NAME a round, how many name a round that (i) exists,
(ii) declares an IMPOSSIBLE register in its own run.py docstring, AND (iii) whose register the
entry can plausibly be about -- i.e. the join R659's NEXT assumed was already available.

POINT ESTIMATE : 12
INTERVAL       : [4, 25]

DIRECTIONAL PREDICTION, the real content and separately failable:
  MOST named entries do NOT reach the named round's own IMPOSSIBLE register. A ledger entry names
  the round where the ERROR WAS FOUND, not the round where the LIMIT WAS DECLARED, and those are
  usually different rounds -- often the finder is the LATER round. So R659's NEXT inference
  ("36 name a round, SO each overturned wall is traceable to where it was declared") is FALSE, and
  the per-wall rate it proposed is not computable from this join alone.
KILL           : if >= 50% of the 36 do reach a declaring round's own register, the directional
                 prediction is RETRACTED and the traceability R659 assumed was real.

⚠ MY FORECAST RECORD: five pre-registered magnitudes, four over (-25, -7, -5, -16.8pts) and one
  under (+29); five directional predictions, five held. I am again NOT shading the magnitude --
  the sign flipped at n=5, which is precisely what an unearned correction would have got wrong.

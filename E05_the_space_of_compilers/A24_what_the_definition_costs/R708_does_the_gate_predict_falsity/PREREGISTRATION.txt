R708 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #310 ON R707's NEXT LINE — THE CITATION RESOLVES, THE PROPOSED METHOD DOES NOT.
  ✓ `chance_share_by_decile` IS in results/length.json, 10 entries, range 0.61–0.99. The first
    citation in this thread that resolves; R706's did not.
  ⛔ But its method is INADMISSIBLE. It proposes I "judge them by hand against the rule the gate
    claims to enforce". I WROTE that rule and I wrote the gate, so my hand-judgement correlates with
    the gate BY CONSTRUCTION — it would measure my consistency, not the gate's validity. Self-review
    is void, not weak. Independent judges are unavailable this session.
  ⭐ THE SUBSTITUTE, AND IT IS BETTER THAN THE ORIGINAL PROPOSAL. This project already produces a
    verdict on NEXT lines by a process that NEVER CONSULTS THE GATE: every round opens with
    `CHECK #N ON R___'s NEXT LINE`, and records whether the line HELD or was FALSE. 110 numbered
    checks exist (#192–#309); 55 name their target round; 33 carry a classifiable headline verdict.
    That is criterion validity — the row §2 usually marks impossible — available in partial form.
  ⚠ AND ITS EXACT SCOPE, STATED BEFORE THE RUN: these labels are NOT independent of ME. They are
    independent of the GATE, which is the confound that matters here and the only one this design
    controls. A truly external standard remains impossible and is named in the register.

ESTIMAND
  The gate's CRITERION VALIDITY on the only ground truth this project has:
    SENSITIVITY  P(flagged | the NEXT line was later recorded FALSE)
    FALSE RATE   P(flagged | the NEXT line was later recorded to HOLD)
    GAP          sensitivity − false rate, with exact binomial intervals
  and, because n is small and that is the likely answer, the design's own MDE for the gap.

IDENTIFICATION
  Identified only where a check names its target round AND carries a classifiable verdict. Rounds
  outside that set are UNLABELLED and are counted, never imputed. ⚠ Partially identified ⇒ the
  answer is reported as an interval, never a point.

SCOPE
  population : target rounds of checks #192–#309 with a parsable verdict (expected ~33)
  instrument : `flagged()` imported UNCHANGED + R706's paragraph-initial extractor, applied to the
               TARGET round's own commit body
               instrument unit = A NEXT PARAGRAPH
               claim unit      = THE GATE'S PREDICTIVE VALIDITY
               ⚠ NOT EQUAL — a per-line flag is not validity until it is cross-tabulated against an
               outcome the flag did not produce, which is what this round does.
  baseline   : the corpus-wide flag rate, 0.2772 (R707), and a label-permutation null
  regime     : this repository at HEAD

WORLDS
  A VALID       gap positive and outside its null ⇒ the gate flags lines that later turn out false
  B INERT       gap inside the null ⇒ the gate's flags carry no information about later falsity;
                combined with R707's 0.758 chance share, the gate is ceremony
  C INVERTED    gap negative and outside the null ⇒ it flags TRUE lines more, worse than useless
  D UNRESOLVED  |gap| < MDE ⇒ this corpus cannot answer it, and the MDE is the finding

PREDICTION MATRIX          A        B        C        D
  gap                    > 0      ~ 0      < 0     any, small
  |gap| vs MDE          above    below    above    below
  what it licenses      keep    rebuild  invert   more data

REGISTERED
  POINT A  SENSITIVITY P(flagged | later FALSE): 0.35, interval [0.10, 0.75]
  POINT B  GAP sensitivity − false rate: +0.10, interval [−0.25, +0.50]
  POINT C  the design's MDE for the gap at 80% power at the observed cell sizes: 0.35, [0.15, 0.80]
  DIRECTIONAL (separately failable)  sensitivity > the corpus base rate 0.2772.
  KILL (conditional — evaluated ONLY if the CLASSIFIER's positive control fires and its g=0 abstains):
     |gap| < MDE                       → world D, UNRESOLVED, and the MDE is reported as the finding;
     gap ≤ 0 and outside the null      → world C, and the gate is reported as inverted.

CONTROLS
  POSITIVE (classifier)  it must label #307, #308, #309 FALSE and #279, #303 HOLDS — verdicts whose
                         content I can state independently of the classifier. A classifier that
                         cannot reproduce known verdicts is not reading verdicts.
  g=0      (classifier)  headlines with every verdict word stripped must ABSTAIN, not guess. A
                         classifier that still labels is pattern-matching something else.
  NEGATIVE               permute which verdict is paired with which NEXT line, preserving counts →
                         the gap must fall inside its own null. The world it excludes: "the gate's
                         flags track SOMETHING about these lines other than the pairing."
  SHAM                   replace the gate's flag with an independent coin at the corpus base rate
                         0.2772 — the same operation minus the ingredient, count- and rate-matched.
                         Its gap distribution IS the null the observed gap must beat.
  PLACEBO                two identical runs differ by exactly 0.
  NOISE FLOOR            the sham gap's spread over ≥2000 draws, measured.
  UNIT                   instrument unit ≠ claim unit, asserted and carried into the verdict.

SPECIFICATION SWEEP
  label source ∈ {headline verdict, ⛔-in-block} × extractor ∈ {commit body, README ## NEXT}
  × window ∈ {60 live, whole paragraph} = 8 cells, all reported.
MULTIPLICITY  8 cells; BH q=0.10 over all of them; non-survivors named.
SEEDS  3 permutation streams; the seed flag is verified to change the draws.
ARTIFACT  results/validity.json — carrying the 2×2, every per-round row, the MDE curve and the sham
          distribution AS FIELDS (check #309 was exactly the failure of not doing that).
IMPOSSIBLE
  construct validity by an EXTERNAL standard — needs a judge who did not write the rule; unavailable
    this session and not substitutable by me.
  cross-release — the NEXT convention, the check protocol and the verdict vocabulary are all ours.

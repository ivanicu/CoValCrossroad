R688 · PRE-REGISTRATION

⚠ WHY NOT THE ROUND R687's NEXT NAMED. It proposed checking the 6 structured judge fields — a fifth
  consecutive round about the CORPUS. A drift audit over R672–R687 returns 4 object headlines of 16,
  and the last SIX are all corpus. R676 caught this exact drift once and R664 before it. The judge
  fields keep; the object does not.

⚠ DISCLOSURE. R442's committed artifact holds `overlap_impl_published = ['coval_core','topw_k4']`
  and `published_five`, and I read both while running the prior-art gate. So "③ excludes three arms
  CoVal published" is an OBSERVATION from a committed artifact, not a forecast. No prediction is
  scored on it.

THE OBJECT QUESTION, WHICH IS §4's FALSIFIER FOR A DEFINITION
  "Name an admissible object this clause EXCLUDES. If the excluded object is one your own benchmark
  ACCEPTS, the clause is false." ③'s extension is {coval_core, topw_k3, topw_k4, topw_k6, topw_k8};
  CoVal's published five is {coval_core, topabs_k4, topvar_k4, topw_k4, topwvar_k4}. Three published
  arms — topabs_k4, topvar_k4, topwvar_k4 — are outside ③'s extension.

WHAT IS BLIND, AND IS REGISTERED
POINT A     of the 3 published-but-excluded arms, 2 PASS clause ② (so ③ alone excludes them).
INTERVAL A  [0, 3]  ⚠ and I am naming that this interval spans the whole range, which ledger 803 says
            is unfailable. It is reported as uninformative; POINT A and the DIRECTIONAL carry the
            content, and the directional can fail.
POINT B     their mean A2 sits within 0.02 of the extension members' mean A2.
INTERVAL B  [-0.05, +0.05]
DIRECTIONAL (separately failable) at least one published-but-excluded arm passes ② — i.e. the
            exclusion is done by ③ ALONE, which is what makes it a falsifier for ③ rather than a
            behavioural rejection.
KILL        if NONE of the three passes ②, then ② already excluded them, ③ is not the excluder, and
            the §4 falsifier does NOT fire against ③. Say so plainly.
CONTROLS
  POSITIVE  coval_core is KNOWN to pass ②; the ②-reader must return true for it.
  g=0       an arm KNOWN to fail ② must return false — the reader must return both values.
  NEGATIVE  an arm name absent from the corpus -> UNKNOWN, never false.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  "CoVal published it" is read from R442's `published_five`, which is one round's
            record of the release. If that list is wrong, this round is wrong with it.

R689 · PRE-REGISTRATION

⚠ DISCLOSURE, AND IT IS LARGE. Going to the object (door ①) before writing this file, I ran greps
  that already showed: `data/DATASET_CARD.md` exists; it names `coval_core` and none of
  `topabs_k4 topvar_k4 topw_k4 topwvar_k4`; and those names are generated by our own
  `corebench/select_core.py` and `rule_sweep.py`. So the HEADLINE is an observation, not a forecast,
  and no prediction is scored on it. What is registered below is the part I have not read.

⛔ WHY R688's NEXT IS NOT RUN AS WRITTEN, AND WHY ITS PREMISE IS RETRACTED
  It proposed reading "CoVal's release manifest" for the four arms ③ removes. There is no arm
  manifest: the release ships DATA, and `data/DATASET_CARD.md` describes it. The card names one
  core. R442's `PUBLISHED_FIVE` is a HARD-CODED LITERAL at `run.py:107` with no cited source, and
  four of its five members are our own constructions.
  ⚠ AND THE IMPOSSIBILITY REGISTER WAS WRONG: R688 wrote that settling this "needs the release's own
  text", which has been on disk at `data/DATASET_CARD.md` the whole time. §4's "a wall never
  checked" — a permanent limit that was a query nobody ran.

POINT A     of R442's five, 1 appears in the card.        INTERVAL A [1, 2]  (NOT [0,5] — ledger 803
            and 808 both say a range spanning the population is unfailable; this one can fail.)
POINT B     the card STATES the core's size distribution in words.   BINARY, and it can fail.
POINT C     the stated modal size is 4.                  INTERVAL C [2, 6]
DIRECTIONAL (separately failable) the card gives a size RANGE rather than a single number, so
            STATEMENT.md's "no number is named" is compatible with the release while its k-sweep
            was answering a question the documentation had already answered.
KILL        if the card names ≥3 of R442's five, `PUBLISHED_FIVE` is defensible and the retraction
            does not stand.
CONTROLS
  POSITIVE  a term KNOWN to be in the card (`rubric`) must be found — a grep is an instrument.
  g=0       a term known ABSENT (`topabs_k4`) must return 0, and the positive proves the instrument
            can return non-zero.
  NEGATIVE  a nonsense token returns 0.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  "absent from the card" is NOT "not published" — the card documents the DATA and
            does not enumerate comparison arms at all. The load-bearing evidence is the pair: absent
            from the card AND generated by our own corebench source.

# R689 · the release ships ONE core

**⭐⭐⭐ The release's own documentation names **1** of R442's `PUBLISHED_FIVE` — `coval_core`. The
other four are generated by our own `corebench` source. So a definition tested against *"the
published five"* was tested against **one released object and four we built ourselves**.**

## ⛔ TWO DEFECTS IN R688's CLOSING LINE, ONE OF THEM ITS PREMISE
1. **Citation** — it cites *"R519's admitted set"* for the four arms ③ removes. **`R519.admitted` is
   the FIVE.** The four are `R404.admitted_2 − R404.admitted_2a`. *Fourth citation defect in this
   arc; the numbers were right every time — **it is the pointer that rots**.*
2. **Premise** — it proposed reading *"CoVal's release manifest"*. **There is none.** The release
   ships data, documented by **`data/DATASET_CARD.md` — 27 KB, on disk the whole time** — while R688
   wrote into its **impossibility register** that settling this *"needs the release's own text"*.
   ⭐ **§4's "a wall never checked": a permanent limit that was a query nobody ran.**

## THE EVIDENCE IS A PAIR, NOT A GREP

| arm | in the card | provenance |
|---|---|---|
| **`coval_core`** | **named** | the release |
| `topabs_k4` `topvar_k4` `topw_k4` `topwvar_k4` | **0** | our `corebench/select_core.py`, `rule_sweep.py`, `dimension_curve.py` … |
| `greedy_k4_fit1` `indep_k4_fit1` `oracle_k4` `oracle_k4_fit1` | **0** | our `generate_core.py`, `learned_core.py`, `compare.py` … |

⚠ **"Absent from the card" is NOT "not published"** — the card documents the *data* and enumerates no
comparison arms. **The load-bearing evidence is the pair: absent from the card AND generated by our
own source.**

**Controls (a grep is an instrument):** POSITIVE — `rubric` → non-zero, *the instrument can return
non-zero*. **g=0** — `topabs_k4` → 0. NEGATIVE — a nonsense token → 0. PLACEBO — identical.

**Registered A 1 [1,2] → 1, error 0.** Kill (card names ≥3) did not fire.

## ⭐ R688's VERDICT SURVIVES, FOR A BETTER REASON
The one core the release actually publishes — `coval_core` — **is in ③'s extension.** So ③ does not
exclude the released core at all. **§4's falsifier still does not fire, and now on the right
population.**

## ⭐⭐ AND THE CARD STATES THE CORE'S SIZE
> *"up to four rubric items … Most prompts end up with **four** core rubric items (about **95%**),
> with the remainder having **two or three**."*

Registered **B (the card states it) → HOLDS** · **C modal 4 [2,6] → 4, error 0** · **directional (a
range, not a single number) → HOLDS.**

⚠ **This does not overturn R441.** The card gives **the released core's** size distribution — a fact
about **the instance**. R441 asked whether the *definition* can resolve a `k` among constructed arms,
which is a fact about **the category**. **They are different questions, and the gap between them is
exactly §4's row: describing the instance and defining the category are indistinguishable from the
inside.** The card supplies the instance's number; it does not supply the category's.

## IMPOSSIBLE HERE
Whether CoVal *intended* any of our constructed arms as a baseline is not in the data. The card
documents the release, not our comparisons.

## NEXT
`published_five` is retracted as a description of CoVal (`results/release_says.json`, field
`n_published_named_in_card`), and R442 hard-codes it at `run.py:107` with no cited source. Sweep the
corpus for other hard-coded sets asserting a property of the RELEASE rather than of our arms — the
`token_boundaries` helper plus the literal classifier from R680 will find them — and check each
against `data/DATASET_CARD.md`. One mislabelled literal propagated through five rounds here.

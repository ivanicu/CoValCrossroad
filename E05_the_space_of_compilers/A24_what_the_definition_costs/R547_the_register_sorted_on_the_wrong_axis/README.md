# R547 · The register sorted on effort while reading as reachability

**Decision this makes safe:** whether the register's ordering — which its own header calls **the
deliverable** — can be acted on.

**Estimand:** whether `source_rubric_item_ids` (row 1, ranked cheapest) is reachable from this site.

| probe | data/ | corebench/ |
|---|---|---|
| **Positive** — `annotator_id` | **5 files** | — |
| **Positive** — `ranking_blocks` | **4 files** | — |
| **Negative** — invented field | **0** | 0 |
| ⭐ **`source_rubric_item_ids`** | **0** | **0** |

**WORLD B.** The field exists in neither tree. **Row 1 requires the publisher to add it to an
already-shipped release**, and R509 measured only **6.6%** of its content as verbatim-reconstructible
here — so it cannot be rebuilt either.

⭐⭐⭐ **Ranked cheapest: true in keystrokes, false in availability.** §2 says the register doubles
as the specification for the next site, which makes **agency** the load-bearing axis.

| group | rows |
|---|---|
| **on this site** | 2 · **3+4 nested** *(R546)*, needing a code edit first *(R544/R545)* |
| **needs the publisher** | **1** |
| **needs another site** | 5, 6 |
| **needs a decision, not data** | 7 |

**The effort ordering is kept as the secondary key** — annotating beats rewriting.

⚠ **The provenance gate blocked the first commit of this change**, because `STATEMENT.md` cited
`R547` before the round existed. **Exactly its unit**, caught before the citation shipped.

R717 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #319 ON R716's NEXT LINE — ITS NUMBERS HOLD AND ITS COMPARISON IS STRUCTURALLY BIASED.
  ✓ 942 against 44 confirmed from R714's k distribution; and no round has split provenance by k,
    so the question is genuinely unasked.
  ⛔⛔ BUT THE STATISTIC IS m/k AND THE SPLIT IS BY k. Splitting a ratio by its own DENOMINATOR is
    conditioning on a component of the statistic. The groups do not even share a support:
    k=4 admits {0, ¼, ½, ¾, 1}, k=3 admits {0, ⅓, ⅔, 1}, k=2 admits {0, ½, 1}.
  ⭐ DERIVATION, before any data: at the SAME match count m=1 the share is 0.2500 at k=4, 0.3333 at
    k=3 and 0.5000 at k=2. **Identical match counts FORCE a higher share in the smaller-k group.**
    So the comparison R716 proposed is biased, and its bias has a known SIGN.

ESTIMAND
  (i)  the DIRECTION of the structural bias, derived before the run (higher share at smaller k for
       equal counts) and compared against the observed direction;
  (ii) the group difference on the UNBIASED statistic — the raw match count, which does not divide
       by the split variable;
  (iii) the MDE at the ACTUAL imbalance, 942 against 44, because R716's even-split 0.040 cannot be
       assumed to carry over to a group of 44.

IDENTIFICATION
  (i) is a DERIVATION and is labelled. (ii) and (iii) are measured by permutation and planted shift.
  ⚠ The count is unbiased with respect to THIS split and is not thereby the right statistic — it has
    its own scope, and R716 measured its MDE as 5–7× worse than the share's.

SCOPE
  population : the 986 conversation rubrics, split 942 (k=4) against 44 (k∈{2,3})
  instrument : permutation over group labels + a planted-shift power curve at the true imbalance
               instrument unit = A CORE INSTANCE
               claim unit      = WHETHER THE k-GROUPS DIFFER IN PROVENANCE
               ⚠ NOT EQUAL — a difference in a statistic computed FROM k cannot by itself be a
               difference in provenance BETWEEN k-groups, and that is the whole round.
  baseline   : the structural bias's own direction, and a label-permutation null
  regime     : this repository at HEAD, alpha 0.05, power 0.80

WORLDS
  A UNREADABLE      the MDE at 942/44 exceeds the observed difference ⇒ the question R716 proposed
                    cannot be answered here, and the honest output is the bound
  B REAL AND AGAINST THE BIAS  the count difference survives its null and runs opposite to the
                    structural bias ⇒ a real difference, understated by the biased statistic
  C CONFOUNDED      the share difference survives but the count difference does not ⇒ what was seen
                    is the quantisation, exactly as the derivation predicts

PREDICTION MATRIX             A          B          C
  MDE vs observed          above      below      below
  count difference          any      survives    null
  share difference          any      survives   survives

REGISTERED
  POINT A  [DERIVED] the bias direction: equal counts give a HIGHER share at SMALLER k. And the
           observed share difference's SIGN: k=4 above k<4, i.e. AGAINST the bias.
  POINT B  the MDE for the share at the true 942/44 imbalance, 80% power: 0.10, interval [0.02,0.40]
  POINT C  the count difference (k=4 minus k<4) against its permutation null: SURVIVES or not —
           predicted NOT to survive, interval on the difference [0.00, 0.30]
  DIRECTIONAL (separately failable)  the MDE at 942/44 is at least 2× the even-split MDE of 0.040,
           i.e. R716's figure does NOT carry over.
  KILL (conditional — evaluated ONLY if POSITIVE recovers a planted shift and g=0 sits at alpha):
     MDE >= the observed difference → world A, report the bound and refuse the comparison;
     count difference survives its null → world B;
     share survives and count does not → world C, and the difference is quantisation.

CONTROLS
  POSITIVE   plant a 0.20 shift into the 44-member group; the test must detect it at >= 80%. FLOOR
             (no shift) and CEILING (maximal shift) computed and the target required to lie between.
  g=0        a planted shift of exactly 0 must reject at approximately alpha.
  NEGATIVE   permute the group labels at the true 942/44 sizes. The world it excludes: "any 44-member
             subset of these 986 differs from the rest".
  SHAM       the same comparison with the ingredient removed — split on a variable UNRELATED to the
             statistic (conversation-id parity) at the same 942/44 imbalance. It must return a
             difference inside its null.
  PLACEBO    two identical runs differ by exactly 0.
  UNIT       instrument unit != claim unit, asserted and carried into the verdict.

SPECIFICATION SWEEP
  statistic ∈ {share m/k, raw count m} × split ∈ {k=4 vs k<4, parity sham, random 942/44} = 6 cells,
  all reported with their observed difference, null interval and verdict.
MULTIPLICITY  6 cells; BH q=0.10 over the whole grid; non-survivors named.
SEEDS  3 permutation streams; the seed flag is verified to change the draws.
ARTIFACT  results/split_by_k.json — the supports, the bias derivation, the MDE curve and the sweep.
IMPOSSIBLE
  removing the bias by choosing a better ratio — every share with k in its denominator inherits it;
    only a statistic that does not divide by the split variable avoids it, which is the count.
  cross-release — one release.

R714 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #316 ON R713's NEXT LINE — THE FOUR ROUNDS EXIST, AND THE LINE'S PREMISE IS ABOUT TO BREAK.
  ✓ R696, R711, R712, R713 all exist with artifacts, and the five pricings are theirs.
  ⛔ AND ASKING ITS QUESTION — "can a definition be attacked by a site that built the objects it is
    defined over" — SENT ME TO LOOK FOR OBJECTS THE SITE DID NOT BUILD, and there are 986 of them,
    on disk, in the release, untouched by this entire arc: `data/conversation_rubrics.jsonl` carries
    a per-conversation `coval_core` for each of 986 conversations.

⛔⛔ THE CLAIM THIS BREAKS IS ON THE DELIVERABLE.
  STATEMENT.md carries "THE RELEASE SHIPS ONE CORE" (R689), and the impossibility register has used
  "one released core" as a hard limit in round after round. ⭐ At the ARM level that is true — one
  core GENERATOR. At the OBJECT level the release ships 986 core INSTANCES. That is the
  instrument-unit-versus-claim-unit distinction this arc has flagged in eleven rounds, sitting
  unexamined in the deliverable's own headline.

ESTIMAND
  (i)  COUNT — how many core INSTANCES the release ships, and the k-distribution over them.
  (ii) UNIT — for each of the three clauses, whether it is a predicate over a core GENERATOR or over
       a core INSTANCE. A formulation whose clauses mix units cannot be applied to a single object.
  (iii) PORTABILITY — the first application of any clause to objects this site did not build:
       which of the three can be evaluated on the 986, and what each returns.

IDENTIFICATION
  (i) and (iii) are exactly computable from the release file. ⚠ (ii) is a READING of each clause's
  own wording, not a measurement; it is argued and labelled, never counted.
  ⚠ And (iii) is only PARTIALLY identified: a clause needing per-arm scores cannot be evaluated on a
  rubric that ships no score. Those are reported as NOT EVALUABLE, never as passing.

SCOPE
  population : the 986 conversation rubrics in data/conversation_rubrics.jsonl
  instrument : direct field reads — the criteria list length per rubric
               instrument unit = A CORE INSTANCE
               claim unit      = THE FORMULATION'S APPLICABILITY
               ⚠ NOT EQUAL, and the gap IS the round: a clause that classifies instances says
               nothing about a clause that classifies generators.
  baseline   : the DATASET_CARD's own statement, "up to four, ~95% are four"
  regime     : this repository at HEAD

WORLDS
  A ONE CORE STANDS      the 986 are not cores in the sense the formulation defines, and R689's
                         claim survives as written
  B UNIT CONFUSION       the 986 ARE core instances and R689's claim is true only at the arm level,
                         so the deliverable must state its unit
  C THE CLAUSES MIX UNITS  F1/F2 are generator predicates and F3 an instance predicate, so the
                         three-clause formulation cannot be applied to any single object as written

PREDICTION MATRIX               A          B          C
  986 carry a criteria list    no         yes        yes
  F3 evaluable on them         no         yes        yes
  clauses share one unit       yes        yes        NO

REGISTERED
  POINT A  core instances in the release: 986, interval [1, 1000]
  POINT B  the share of instances with 1 < k <= 4, i.e. F3's admission rate on objects we did not
           build: 1.00, interval [0.80, 1.00]
  POINT C  the share with k exactly 4, against the card's "~95%": 0.955, interval [0.90, 0.99]
  DIRECTIONAL (separately failable)  at least one of the three clauses is NOT evaluable on the 986,
           because it needs a per-arm score the rubric file does not carry.
  KILL (conditional — evaluated ONLY if the POSITIVE fires and the g=0 returns nothing):
     the 986 carry no criteria list -> world A, R689 stands as written;
     they do -> world B at minimum, and STATEMENT.md must state the UNIT of "one core".

CONTROLS
  POSITIVE   the card's own claim — "up to four, ~95% are four" — must be recovered from the file.
             A reader that cannot reproduce the card's published statistic is not reading the field
             the card describes.
  g=0        a field that does not exist (`coval_core_NONEXISTENT`) must yield 0 instances, not a
             silent empty pass.
  NEGATIVE   read `coval_full` instead — the same file, the other field. Its k runs far above 4, so
             F3 must REJECT most of it. A clause that admits both fields is not reading size.
  SHAM       apply F3's predicate with the bound removed (any k >= 1) — the operation minus the
             ingredient. It must admit both fields, showing the bound is what does the work.
  PLACEBO    two identical reads differ by exactly 0.
  UNIT       instrument unit != claim unit, asserted and carried into the verdict — and this round
             is ABOUT that distinction, so the verdict must state it twice: once as a control and
             once as the finding.

SPECIFICATION SWEEP
  field ∈ {coval_core, coval_full} × bound ∈ {1<k<=4 (F3), k>=1 (sham), k==4 (the instance's value)}
  = 6 cells, all reported.
MULTIPLICITY  6 cells; the counts are exact, so no p-values are computed and none are implied.
SEEDS  not applicable — the read is deterministic; stated rather than faked.
ARTIFACT  results/instances.json — the k-distribution per field, the per-clause unit reading, and
          the evaluability verdict per clause, AS FIELDS.
IMPOSSIBLE
  evaluating F1 and F2 on the 986 — they need a provenance record and a per-arm score the rubric
    file does not carry. Named, and reported as NOT EVALUABLE rather than as passing.
  cross-release — 986 instances from ONE release is still one release.

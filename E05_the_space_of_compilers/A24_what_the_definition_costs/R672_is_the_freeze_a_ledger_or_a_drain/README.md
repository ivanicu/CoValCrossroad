# R672 · is the freeze a LEDGER or a DRAIN?

**⭐⭐⭐ ONE retirement in 26 transitions, and the PROVENANCE escape used ONCE in 250 opportunities.
The freeze only fills, and the gate it serves is in practice a prohibition on quantifiers rather
than a requirement to cite them.**

## WHAT WAS ASKED
R671's NEXT line named two unchecked quantities about the baseline the quantifier gate carries.
A baseline that only accumulates is hard to tell from suppression, and nobody had looked.

## A · THE FREEZE'S OWN HISTORY  (exact set arithmetic, `results/freeze_history.json`)

| | |
|---|---|
| committed versions | 27 |
| first → last | 123 → 167 (**+44**) |
| transitions | 26 |
| ⭐ transitions that retired anything | **1 (3.8%)** — and it is the FIRST one, `7be0a92→301a6c1` |
| since that transition | **35 added, 0 retired** |

**Controls, computed before the verdict branch.** POSITIVE: retirement is *detectable* — one occurred,
so this is a measured absence rather than an instrument that has never returned non-zero (P5's ★
rule turned on my own instrument). NEGATIVE: the known R670 transition registers `+10, retired 0`,
not as a retirement. PLACEBO: a version against itself returns `0/0`.

⚠ **NO FORECAST ROW.** Part A was measured before this round's directory held anything but a name.
Scoring a prediction against an answer I had already read is not a forecast, so the record stays at
nine. Written into `PREREGISTRATION.txt` rather than back-dated.

## B · HOW CHEAP IS THE REPAIR?  (registered blind)

**17 of 34 (50.0%)** frozen README sections already contain a citable `R###` or `assurance/`
reference somewhere in the section — registered **45%, interval [25%,70%] → INSIDE, error +5.0 pts**.

⚠ **UPPER BOUND, not a promise.** A reference *present in the section* is not proof it *sources that
quantifier*; that is a judgement about prose and it is not made here. The instrument unit is A
SECTION; the claim unit is A CHEAPLY REPAIRABLE SECTION. **Not equal — hence the bound.**

## ⭐ THE THING NEITHER HALF WAS LOOKING FOR

The directional row required a matched quantity, so both populations were re-measured under the
gate's own rule and window:

| population | quantified + artifact | accepted via PROVENANCE | flagged |
|---|---|---|---|
| commit-body `NEXT` paragraphs | 250 | **1 (0.4%)** | 249 |
| README `## NEXT` sections | 34 | **0 (0.0%)** | 34 |

**The escape hatch the gate defines has been taken once in 284 opportunities.** So the gate does not
function as *"cite the artifact your quantifier came from"*. It functions as *"do not write
quantifiers near artifact words"*, and everything already written flows into the freeze — which is
exactly why the freeze only fills.

⚠ **AND THE DIRECTIONAL PREDICTION IS DEGENERATE.** Registered: README's accepted-share is LOWER
than the commit body's. Observed 0.0% < 0.4%, so it reads HOLDS — **on a difference of one instance.**
Reporting `HOLDS` without that sentence would be a false pass: the prediction assumed two rates that
could differ, and what was measured is that **both are floor.**

⚠ **UNVERIFIED, and named rather than smoothed:** 249 commit bodies flag here against 167 entries in
the freeze. The two populations are not reconciled — the gate walks a commit range, this
re-measurement walks all of `git log`. Which is correct is not established by this round.

## NEXT
The freeze holds 167 shas and 34 README rounds while the escape it offers has been used once
(`results/freeze_history.json`, matched table above). **Reconcile the 249-vs-167 gap first** — run
the gate's own walk and this round's walk over the same commit list and diff the sha sets, because
until that is closed, every count of "how much is frozen" in this arc is quoting one of two numbers
without knowing which. A gap of 82 is too large to be a rounding difference and it has not been
explained.

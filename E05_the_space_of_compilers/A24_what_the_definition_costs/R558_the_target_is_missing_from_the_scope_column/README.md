# R558 · The scope column named every axis but the one that empties the definition

**Decision this makes safe:** how the claim set's counts may be read.

**WORLD B.** The claim table names **judge / prompts / arms in 10 of 10** rows and the **target in
0 of 10**. Meanwhile R288 — 968 prompts, six agreement targets — returns **four distinct admitted
sets**:

| target | admits |
|---|---|
| `A2·annot`, `A2·consensus` — every number on the page | `coval_core`, `topw_k4` |
| `A1·annot`, `A1·consensus` | **∅ — the definition is EMPTY** |
| `tau·mean` | `coval_core` |
| `top1·mean` | `topw_k4` — **the released core is NOT admitted** |

⭐⭐⭐ **The extension counts have been readable as unconditional.** Two of six defensible targets
empty the definition; a third excludes the only object anyone calls a core.

**Register row 6 is two questions wearing one row.** ① construct validity — needs an external
standard, genuinely another site. ② target robustness — **measured here, artifact on disk
throughout**, and priced at "another site" anyway.

## Controls
- **Positive** — the parse finds a known scope axis in **10 of 10** rows, so the zero for `target`
  is a measurement and not silence. **PASS.**
- **Negative** — an invented scope token matches **0** rows. **PASS.**

**Fix:** one scope note above the table, stated **once**. A scope restated per row drifts, and the
copy is never the one that gets corrected.

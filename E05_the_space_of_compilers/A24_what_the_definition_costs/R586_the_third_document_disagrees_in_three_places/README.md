# R586 · UNVERIFIED — the instrument does not transfer to the third document

**Decision this makes safe:** none. **Both comparisons fail, for different reasons.**

| pair | shared labels | verdict |
|---|---|---|
| **STATEMENT ∩ FORMULATION** | **0** | **UNVERIFIED** — no comparison exists; a zero is silence |
| **DEFINITION ∩ FORMULATION** | **1** | **UNVERIFIED** — its one "disagreement" is a prefix collision |

## The collision, resolved by reading both sites
> `DEFINITION`: `| `coval_core` | 4 | **+0.0726** | 0.0031 |` — an **advantage** column
> `FORMULATION`: `| `coval_core` | 4 | 0.5665 | +0.0738 |` — an **A2 score** column

**Two different quantities sharing the 26-character prefix `coval core 4`.** ⭐ **Exactly the false
positive my own caveat predicted one round earlier — and it would have been reported as a
cross-document contradiction had I not read both sites.**

## ⭐⭐⭐ The real finding: the instrument does not transfer
It worked between STATEMENT and DEFINITION — **24 shared labels, 1 genuine disagreement** *(R585)*.
Against FORMULATION it connects **1 label out of ~2,000 decimals**. **The documents do not share
label phrasing**, so a prefix method has nothing to join on. **A method that succeeded on one pair
of files is not a method for the corpus.**

## ⛔ And my branch printed a verdict over a failing control — third time this session
The positive control required shared labels in **both** pairs. It printed **FAIL**. The script
printed **WORLD B** from the other pair anyway. **R562 caught this, R582 caught this, and it happened
again here.** ⚠ **This is no longer a slip; it is the shape of how I write conclusions** — the branch
gets written while I am thinking about the finding, and the control is read as a status line rather
than as an input.

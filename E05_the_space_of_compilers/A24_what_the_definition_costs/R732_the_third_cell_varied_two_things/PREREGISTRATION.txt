R732 · the third cell varied two things
written BEFORE run.py. P4 RUN FIRST and it changed the round.

PRIOR ART, AND IT ANSWERS THE QUESTION I WAS GOING TO ASK
   R731 closed by proposing that oracle_k4_08bR's margin collapse might be a JUDGE change, and
   pointed at R424/R426. R424 already established it: the _08b and _08bR families agree with each
   other and BOTH disagree with the default emitter. R426 corrected R424's "not on disk" conclusion,
   which was a filter bug in R424's own candidate loop, but the foreignness stands.
   ⛔ SO R731 PUT A FOREIGN-JUDGE OBJECT IN THE SAME COLUMN AS DEFAULT-JUDGE OBJECTS and read a
   number off the comparison. That is mixing instruments, and I did it while writing a round about
   units.

DISCLOSED, NOT BLIND
   I have already measured, inline: oracle_k4 and oracle_k4_08bR select IDENTICAL criteria on 86 of
   968 shared prompts (0.0888), their sat grids have identical cell keys (15488 each), and per-cell
   satisfaction agreement is 549/15488 = 0.0354. I also ran a distinct-VALUE containment probe that
   returned 0.96 and was the WRONG INSTRUMENT -- the satisfaction alphabet is small and shared by
   construction, so it cannot see an emitter difference. That failure is reported, not hidden.

ESTIMAND
   How many factors differ between oracle_k4 and oracle_k4_08bR, and does any object on disk permit
   separating them? Specifically: is there an arm carrying oracle_k4_08bR's CRITERIA scored by the
   DEFAULT emitter, or oracle_k4's criteria scored by the 08b emitter -- the 2x2 that would identify
   the judge effect apart from the selection effect?

IDENTIFICATION
   ⚠ PARTIAL, AND THAT IS THE POINT. The margin gap between the two objects confounds at least two
   factors: which criteria were selected (they agree on 8.88% of prompts) and which emitter scored
   them (R424). Neither is identified from their difference alone. The round therefore reports a
   DECOMPOSITION ONLY IF the 2x2 exists, and otherwise returns the gap as UNIDENTIFIED with the
   missing cell named -- never as a judge effect and never as a construction effect.

SCOPE
   population : today's 93 tags · instrument core_*.json criteria identity + sat cell-key overlap ·
                baseline the default-emitter arms · regime this tree_sha

WORLDS
   W-CONFOUNDED  two or more factors differ and no 2x2 exists. R731's third cell is UNVERIFIED and
                 its "construction does not determine behaviour" reading must be withdrawn -- not
                 because it is false, but because the cell that produced it cannot support it.
   W-SEPARABLE   a 2x2 exists on disk. Then the judge effect is estimable and R731's reading can be
                 repaired rather than withdrawn.
   prediction matrix
      no arm carries 08bR's criteria under the default emitter -> CONFOUNDED 0.90  SEPARABLE 0.05
      such an arm exists                                       -> CONFOUNDED 0.05  SEPARABLE 0.90

REGISTERED POINTS (failable; the first is disclosed as non-blind)
   A  share of prompts where oracle_k4 and oracle_k4_08bR select identical criteria = 0.09 [0.00, 1.00]
   B  factors that differ between the two objects (selection, emitter)              = 2    [0, 3]
   C  arms on disk carrying 08bR's criteria under a DEFAULT-emitter score           = 0    [0, 93]
   DIRECTIONAL (separately failable): R731's third cell is UNVERIFIED rather than evidence.
   Fails if C > 0, because then the gap is decomposable and the cell can be repaired.

KILL (CONDITIONAL -- evaluated only if the POSITIVE and NEGATIVE controls both fire)
   if positive_fires and negative_collapses:
       C > 0  -> W-SEPARABLE: build the decomposition, do not withdraw
       else   -> W-CONFOUNDED: withdraw R731's third-cell reading to UNVERIFIED and say which
                 experiment would settle it
   else:
       verdict = UNVERIFIED   # never OVERTURNED, never CONFIRMED

POSITIVE CTRL   two tags R730 proved are the SAME OBJECT -- topw_k4 and topw_k4_detA -- must return
                criteria identity 1.0000 and per-cell agreement 1.0000. threshold computed:
                floor (a known-different pair) < t 1.0 <= ceiling 1.0. An identity instrument that
                cannot see a known identity licenses nothing about a difference.
g=0             an arm against itself -> 1.0 on both measures, and the known-different pair
                (oracle_k4 vs topw_k4) must NOT return 1.0. The control must not fire on nothing.
NEGATIVE CTRL   permute the criteria across prompts within one arm and require identity to collapse.
                excluded world: "the comparison is insensitive to WHICH criteria, only to how many".
SHAM            compare the two arms on their PROMPT SETS alone, with the criteria removed -- the
                ingredient absent, not inverted -- and show it returns 1.0, i.e. the prompt sets are
                identical and carry none of the signal.
PLACEBO         an alias pair against itself -> agreement exactly 1.0.
NOISE FLOOR     across all same-object tag pairs from R730's partition, the distribution of
                criteria identity; anything below that floor is a real difference, not rounding.
MULTIPLICITY    93 tags x 2 measures against oracle_k4_08bR + all same-object pairs, all reported.
SPECIFICATION   comparison unit (criteria set per prompt, criteria multiset, sat cell value) x
                reference (oracle_k4, the 08b family, the default family)
SEEDS           deterministic; permutation control 3 seeds; two hash seeds byte-identical
ARTIFACT        results/r732_confounded_cell.json with tree_sha
IMPOSSIBLE      the judge effect alone -> needs the missing 2x2 cell, i.e. a scoring run of one
                arm's criteria under the other emitter, which is a NEW MEASUREMENT this site cannot
                perform from artifacts · independently replicated -> a second implementer

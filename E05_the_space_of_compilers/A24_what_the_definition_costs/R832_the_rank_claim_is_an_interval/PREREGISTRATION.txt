R832 · R831's rank claim is an interval, and the direction has to survive both ends
registered 2026-08-06, BEFORE execution.

WHAT I PUBLISHED ONE ROUND AGO, AND WHY IT OVERREACHED
  R831's README says: "the best label-free substantive arm this site contains sits at rank 50 of 93".
  ⛔ THE INSTRUMENT'S UNIT IS `③-ADMITTED`. THE SENTENCE'S UNIT IS `label-free`. They are not equal.
  ③'s partition returns 11 UNKNOWN over R831's 93 arms, and UNKNOWN is not "reads labels" -- it is
  "selector_of returned None because the arm name carries no known selector prefix". Those 11
  include `generic` (rank 21) and `gen` (rank 27), arms whose construction plausibly reads no human
  labels at all. So the sentence quantified over a population the partition never classified.
  This is §4's instrument-unit-vs-claim-unit, for the THIRD time in this session.

⛔ AND P4 STOPPED THE ROUND I FIRST WANTED TO RUN. My instinct was to DECIDE the 11 by record, the
   way R475 decided `coval_core` from the dataset card. DEFINITION.md already records the decision
   NOT to: "7 arms have provenance the source cannot classify (coval_core, gen, generic, promptecho,
   shams) and are returned UNKNOWN, never silently admitted". That is a deliberate three-valued
   discipline, committed, and re-opening it is not this round's business.

ESTIMAND
  The rank of the best substantive non-label-reading arm, as an INTERVAL over the two readings of
  UNKNOWN. Named before the method.
     lower end = unknown-as-ADMITTED   upper end = unknown-as-EXCLUDED
  ⚠ THIS IS A DERIVATION from R831's committed ranks plus ③'s committed partition. It is labelled
  as one. Nothing is re-measured; what changes is the SCOPE the number is reported under.

THE ROBUSTNESS QUESTION, WHICH IS THE ONLY PART THAT COULD COME OUT OTHERWISE
  Does W-SELF-DEFEATING survive at BOTH ends? Report, under each reading, how many of the top 8 and
  top 16 arms are ③-EXCLUDED. If the top of the ordering stops being dominated by label-readers
  under unknown-as-admitted, R831's world is scoped to one reading and must be said so.

WORLDS
  W-DIRECTION-SURVIVES  the top of the ordering is label-readers under BOTH readings. R831's world
                        stands; only its rank NUMBER becomes an interval.
  W-READING-DEPENDENT   under unknown-as-admitted the top is no longer dominated by label-readers.
                        R831 is scoped to unknown-as-excluded and the README must say so.

POSITIVE CONTROL  a synthetic ordering whose top 8 are all admitted must report 0 excluded in the
                  top 8; one whose top 8 are all excluded must report 8. Both required.
NEGATIVE CTRL/g=0 the partition recomputed from source twice and compared -- the producer invoked on
                  both sides, not `x - x`.
KILL              CONDITIONAL. Evaluated only if both positive controls behave and g=0 is null.
                  Otherwise UNVERIFIED and the README correction is made WITHOUT a world claim.
MULTIPLICITY      top-8 and top-16 under two readings = 4 cells, all reported.
SCOPE             population: R831's 93 arms. instrument: clause3_as_written.partition + R831's
                  committed ranks. baseline: none needed -- this is a re-scoping, not a test.
ARTIFACT          results/r832_interval.json with source hash.
IMPOSSIBLE        deciding the 11 UNKNOWNs. That needs a provenance RECORD per arm, and the
                  deliverable has already ruled that they do not have one here.

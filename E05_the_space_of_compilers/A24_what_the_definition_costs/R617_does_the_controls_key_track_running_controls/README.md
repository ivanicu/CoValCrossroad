# R617 · The `controls` key is empty form — and the arc's last "real loss" is retracted

**Decision this makes safe:** what the ~430 boundary changed. **What artifacts RECORD. Nothing
measured shows a change in what rounds DID.**

| | |
|---|---|
| rounds writing a **`controls` key** | **0.1462** |
| rounds whose **README discusses controls** | **0.9006** |
| rounds whose **`.py` carries control-shaped names** | **0.8596** |
| agreement key ↔ README prose | **0.2339** |
| agreement key ↔ code names | **0.2865** |

**The pre-registered kill (min agreement ≥ 0.60) fired BEFORE the contrast was read.** The `controls`
key does **not** track controls being run — **90% of rounds discuss controls and 86% have
control-shaped code, while 15% write the key.**

## ⭐⭐⭐ This retracts R611's one unmirrored loss
R611 found `has_controls` falling **0.8788 → 0.3269** and called it *"a genuine loss, unmirrored"* —
the single substantive casualty the whole R605–R617 arc had identified. **It is a schema change too.**

| boundary effect | status after this round |
|---|---|
| provenance keys vanish | **replaced** by `world` (R611); complement exact, swap unestablished (R612) |
| `controls` key falls | ⛔ **schema habit — controls kept being run** |

**So the boundary is entirely about recording, and the arc's substantive casualty count is zero.**

## ⛔ Check #216 — and the third instance of one pattern
R616 closed calling `has_controls` *"the axis closest to the definition's evidential quality"*. **It is
a JSON key.** And it is the third consecutive closing line to promote a key-presence measure:

| check | I called it | it measures |
|---|---|---|
| #214 | "verdict class" | first token in a six-member set |
| #215 | "legibility" | first token in a six-member set |
| #216 | "evidential quality" | a key named `controls` |

⭐ **So this round did what none of the three did: tested the CONSTRUCT before the contrast.** *Three
rounds asserted what a key means; the first one to check found it means almost nothing.*

## Controls
| control | returned |
|---|---|
| **positive** — a round with key+signal and one with neither | agreement **1.0000** |
| **negative** — key shuffled across rounds | **0.2456 / 0.1754 / 0.2105** vs observed **0.2339** — shuffling moves it, so the observed value is not just the marginal |
| **placebo** — a key every round has | agreement **0.9006** = the signal's **own rate**, not 1 — the statistic is not saturating |
| **KILL, evaluated first** | min agreement **0.2339** < 0.60 → world C, contrast not read as evidence |

**The contrast was computed and printed anyway** — Δ = **−0.1623**, null [−0.1623, +0.1642] — **and
explicitly marked NOT READ AS EVIDENCE**, because a number about a schema habit is not a number about
controls.

**IMPOSSIBLE, named:** README prose and code identifiers are **proxies too**. Agreement bounds the
construct **from above**, and nothing here shows any round's controls were **correct** — only that
something control-shaped exists.

## The sentence I can no longer write
> *"`has_controls` falling at the boundary was a genuine loss of controls."*

**It was a key falling out of a schema while 86–90% of rounds went on running and describing controls.**

## NEXT
Every axis of this arc now measures **artifact form**, and the construct check just showed form and
substance can diverge by 6× on a single field. **The one substantive question left is the definition
itself**: `② ∧ ③` was measured on the home release, and R603 established the second file cannot carry
②'s baseline. **Ask what a THIRD object would have to provide for ② to be evaluable on it** — that is a
specification, not an audit, and it converts thirteen rounds of corpus measurement into a requirement
the next site can be checked against.

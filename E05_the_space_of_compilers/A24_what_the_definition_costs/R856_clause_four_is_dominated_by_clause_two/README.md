# R856 · why does clause ④ exclude NOTHING? — because ② is strictly harder

**Arc A24.** ⚠ **The arithmetic trap fired on this round's own headline and demoted it to a
derivation. The mechanism it exposed is the finding.**

## ⛔ THE TRAP, RUN FIRST

The plan was to recompute the conjunction on the 99-arm space and lay it beside the published
extension (**1 arm, on 42**). Intersecting the two committed 99-arm satisfier sets gives
**\|②∧④′\| = 29 = \|②\|** exactly — i.e. **② ⊆ ④′**. *Could that have come out otherwise?*

| | |
|---|---:|
| ②'s prompt-blind comparator `genericpool16`, even half | **0.5404** |
| ④′'s response-only bar, even half | **0.4820** |
| **gap** | **+0.0584** |

⭐ **The comparator sits well ABOVE the bar, so an arm resolvably beating 0.5404 resolvably beats
0.4820. ②⇒④′ is largely FORCED, and the 29 is `1+1=2`.** **Labelled a DERIVATION, not evidence.**

## ⭐⭐ THE MECHANISM IS THE FINDING — and it explains a number that has sat unexplained

**Clause ④ is DOMINATED by clause ②.** ④ can only bind on arms that ② has already rejected, so **in
the conjunction it contributes nothing** — not because it is decoration, but because it is
strictly weaker.

⭐⭐⭐ **That explains R440's committed `excluded_by_4 = 0 of 42`**, which this file has carried as an
observation since it was measured. **④ excludes nothing because everything ④ would exclude, ②
already excluded.** A mechanism, where there was a coincidence.

## ⚠ CONTROLS — and what stands in for them honestly

| check | result |
|---|---|
| **KILL** the two artifacts' arm lists must be **identical** | **True · PASS** (99 vs 99) |
| the arithmetic-trap check | **FIRED** — and demoted the headline |

⚠ **There is no statistical control here and pretending otherwise would be theatre**: (a) is set
arithmetic over committed artifacts and (b) is two means. **The arm-list identity check CAN fail** —
intersecting different populations is exactly entry 1371's error — and it is the real control.

## ⚠ WHAT IS NOT COMPUTED, AND WHY

**The full conjunction on 99 arms.** Clause ③ is a **provenance** property, measured by R360/R444
only on **42** arms. Classifying 99 arms' provenance **from their names** would be *"a label is not a
description."* **Named as impossible here rather than approximated** — which is what entry 1375
established one round ago and what my own NEXT then asked me to do anyway.

## STRUCTURALLY IMPOSSIBLE HERE
| criterion | what it would require |
|---|---|
| the conjunction on 99 arms | clause ③'s provenance re-measured on that space, from sources |
| construct validated | an external gold standard for corehood |
| cross-release | a second release |

⚠ **N/A with what each would require — never "planned".**

⭐ **The round's value: a number the file had carried for many rounds as a fact now has a reason, and
this round's own headline was demoted by the check that the standard opens with.**

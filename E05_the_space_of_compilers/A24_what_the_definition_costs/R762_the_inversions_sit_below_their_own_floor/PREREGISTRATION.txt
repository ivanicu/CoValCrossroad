R762 · R761's inversions were never resolved, and its counterexample has no interval
WRITTEN BEFORE ANY CODE FOR THIS ROUND. 2026-08-05.

--------------------------------------------------------------------------------
WHY THIS ROUND EXISTS -- IT ATTACKS THE ROUND I PUBLISHED ONE COMMIT AGO
--------------------------------------------------------------------------------
R761's NEXT asked what separates `oracle_k4_08bR` from `oracle_k4` -- "same rule, same target
access, 0.0634 less A2".

⛔ CHECK #364, GAUGE TEST, ZERO COMPUTE: THE TWO ARMS ARE NOT THE SAME OBJECT.
   `select_core.py:75` makes `--tag-suffix` MANDATORY when `--full-npz` is not the default, and
   `corebench/rebuild_selection_08b.sh` records the invocation:
       frozen() --full-npz 0.8B --select-npz 2B --tag-suffix _08b
       rerun()  --full-npz 0.8B                 --tag-suffix _08bR
   So `_08bR` was SELECTED from a different source table (`sat08_full.npz`, Qwen3.5-0.8B-Base, named
   by R426). R416 measured the consequence: the `_08b`/`_08bR` pairs scored DIFFERENT CRITERIA, and
   for `oracle_k4` **91.1% of prompts changed selection**. My NEXT compared across instruments and
   called the difference a property of target-reading.

⛔ AND THE SAME CHECK REACHES BACK INTO R761's OWN HEADLINE.
   R415 measured, for exactly this arm pair, a run-to-run shift in mean A2 of **0.116489**. R761's
   "0.0634 gap" is **54% of a floor already committed on the same two files**. And all four of
   R761's inversions occur at |ΔA2| <= **0.0017**.
   ⇒ R761 COUNTED INVERSIONS WITHOUT A RESOLUTION ON ΔA2. Its preregistration never asked for one.
   ⇒ R761 REPORTED rob = 0.9401 vs 1.0000 AS A DIFFERENCE WITH NO INTERVAL ON rob AT ALL.

⚠ This is an ATTACK, so it is a full round (§3). It is most dangerous if it succeeds, because it
   retracts something I published, and a false retraction is as permanent as a false acquittal.

--------------------------------------------------------------------------------
ESTIMAND (before method)
--------------------------------------------------------------------------------
E1  the RESOLUTION of a between-arm ΔA2 in this design: the paired MDE of (x_a - x_b) over the 968
    prompts, per pair. Reported as a distribution over pairs, not one number.
E2  R761's inversion count RESTRICTED to pairs whose ΔA2 clears its own resolution -- and the whole
    curve over the floor, including floor = 0 (R761's uncorrected 4).
E3  an INTERVAL on rob, by an outer bootstrap over prompts, for the arms R761's World C turns on:
    does rob(`oracle_k4_08bR`) = 0.9401 separate from 1.0000, and from rob(`coval_core`) = 0.9978?

--------------------------------------------------------------------------------
IDENTIFICATION
--------------------------------------------------------------------------------
E1, E2 IDENTIFIED and exact.
E3 PARTIALLY IDENTIFIED. rob is a share over a COMPLETE ENUMERATION of 1,820 references, so it has
   no sampling error in the class; all of its variability comes from the 968 prompts. The honest
   instrument is a NESTED bootstrap (outer prompt resample x inner CI bootstrap), and its cost is
   controlled by reducing the inner draws -- which is itself validated, not assumed (see POSITIVE-2).
   ⇒ E3 is reported as a BOUND on separability, never as a point difference.

--------------------------------------------------------------------------------
SCOPE
--------------------------------------------------------------------------------
population  R761's 27 arms, unchanged, so the two rounds are comparable
instrument  R294's estimator; outer bootstrap over prompt ids for E3
baseline    all 1,820 size-4 subsets, unchanged
regime      first release, home judge, 968 prompts, this tree_sha
⚠ `oracle_k4_08bR` is SELECTED under a foreign source table. Every statement about it is
  cross-instrument and is worded that way or not made.

--------------------------------------------------------------------------------
⛔ THE ARITHMETIC, DERIVED BEFORE MEASURING
--------------------------------------------------------------------------------
D1  A FLOOR CAN ONLY REMOVE INVERSIONS. Filtering pairs to those with resolved ΔA2 is a subset
    operation, so the count is MONOTONE NON-INCREASING in the floor. "Fewer inversions after
    filtering" is ALGEBRA. The measurement is WHETHER IT REACHES ZERO, and at what floor -- and the
    sham is a random subset of the same size, which is what makes the drop attributable.
D2  R761's THREE 08bR INVERSIONS ARE THE SAME COMPARISON THREE TIMES. `topw_k3`, `topw_k4`, `topw_k6`
    lie within 0.0010 of each other, so one arm inverting against all three is ONE event with three
    labels, not three. R761's "4 of 351" therefore overstates its own numerator; the honest count of
    INDEPENDENT inversion events is at most 2.

STRONGEST CONFOUND, written BEFORE the run: if the resolved-inversion count reaches 0, that could be
because the floor is so large it removes nearly every pair -- a filter that keeps nothing cannot
invert. CONTROL IN THE SAME ITERATION: report the SURVIVING PAIR COUNT beside every floor, and the
sham's inversion count on a random subset of identical size.

--------------------------------------------------------------------------------
WORLDS
--------------------------------------------------------------------------------
A · R761 FOUND NOISE. Resolved inversions = 0 at the design's own MDE, while the sham at the same
    subset size retains inversions. rob is a rank statistic of A2 WHEREVER A2 IS RESOLVED, and
    R761's WORLD B'/C reading is retracted to WORLD B.
B · R761's RESIDUAL IS REAL. At least one inversion survives at the design's own MDE, so rob carries
    information A2 does not, and R761 stands as published.
C · THE FLOOR EATS EVERYTHING. Almost no pair is resolved, so neither R761 nor this round can speak;
    the correct output is a bound on how many pairs this design could ever order.

PREDICTION MATRIX
  A -> resolved inversions 0, surviving pairs >> 4, sham keeps >0
  B -> resolved inversions >= 1 at the paired MDE
  C -> surviving pairs < 20 of 351, at which point no ordering claim is admissible either way

--------------------------------------------------------------------------------
KILL -- CONDITIONAL, gated on the controls
--------------------------------------------------------------------------------
if POSITIVE-1 fires (a planted ΔA2 above the MDE is counted as resolved and one below is not)
   and POSITIVE-2 fires (the reduced-inner-draw rob reproduces the full-draw rob within 0.005)
   and g=0 fires (a planted ZERO ΔA2 is never resolved):
        resolved inversions == 0 and surviving pairs >= 50 -> WORLD A. R761's E2 verdict is
                                                              RETRACTED to WORLD B on the page.
        resolved inversions >= 1                           -> WORLD B. R761 stands.
        surviving pairs < 20                               -> WORLD C. Bound only.
else:
        UNVERIFIED. Never OVERTURNED, never CONFIRMED.

⚠ AND A SEPARATE, SEPARATELY-FAILABLE VERDICT ON E3, because it decides a DIFFERENT sentence:
        rob CI for `oracle_k4_08bR` excludes 1.0000 -> R761's WORLD C counterexample STANDS
        it includes 1.0000                          -> the counterexample is UNRESOLVED and the
                                                       causal retraction R761 made loses its case

--------------------------------------------------------------------------------
CONTROLS
--------------------------------------------------------------------------------
PROVENANCE  reproduce R761's committed rob vector exactly (tolerance 0) before anything else. This
            round attacks R761, so it must first BE R761. EXIT 2 otherwise.
POSITIVE-1  plant ΔA2 at 2x the pair's own MDE -> must read RESOLVED; at 0.5x -> must read
            UNRESOLVED. BAND, COMPUTED: floor = a rule calling nothing resolved (0 at both),
            ceiling = a rule calling everything resolved (both). Threshold "resolved at 2x and not
            at 0.5x" is unreachable from either degenerate end.
POSITIVE-2  the reduced inner-draw estimator must reproduce the full 1200-draw rob within 0.005 on
            all 27 arms. Without this the E3 interval is an artifact of the cost saving.
g=0         a planted ΔA2 of exactly 0 must never be called resolved, over all 351 pairs.
NEGATIVE    destroy the per-prompt pairing between the two arms of a pair (permute one side, 200
            draws) and report the resolved-pair COUNT as a distribution. World excluded: "the MDE is
            driven by the pairing, so the floor is an artifact of correlated arms."
SHAM        ingredient ABSENT, not inverted. Ingredient = RESOLUTION FILTERING. Remove a RANDOM
            subset of pairs of exactly the size the floor removes, 200 draws, and report the
            inversion-count distribution. If a random subset of the same size also removes every
            inversion, the floor is not what did it.
PLACEBO     arm pairs with |ΔA2| > 0.05 must invert at 0 for every floor, including floor 0.
NOISE       the two floors are DIFFERENT OBJECTS and both are reported: (i) the paired MDE of ΔA2,
            (ii) R415's committed run-to-run re-selection shift 0.116489. Neither is "the" floor.
UNIT        instrument unit = an ARM PAIR (351); claim unit = an INDEPENDENT INVERSION EVENT (D2
            says these are not equal). Both reported, never conflated.

--------------------------------------------------------------------------------
MULTIPLICITY
--------------------------------------------------------------------------------
351 pairs x the floor curve {0, 0.5x MDE, 1x MDE, 2x MDE, R415's 0.116489} = the whole grid, with
surviving-pair counts printed at every level, survivors and non-survivors alike.

--------------------------------------------------------------------------------
IMPOSSIBLE HERE -- each with what it would require
--------------------------------------------------------------------------------
what makes `_08bR` differ from `_08b`      the 0.8B source table's own construction, which R426
                                           located but whose BUILDER is still off-repository
whether the foreign judge is worse or
  merely different                         an external criterion for judge quality
independently replicated                   a second team; the session prompt forbids agents
cross-release                              a second release

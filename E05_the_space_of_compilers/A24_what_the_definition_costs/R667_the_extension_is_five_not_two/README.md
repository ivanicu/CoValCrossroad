# R667 · The extension is FIVE, not two — and nothing in `② ∧ ③` distinguishes k

**Decision this makes safe:** what the definition actually admits. **`{coval_core, topw_k3, topw_k4,
topw_k6, topw_k8}` — the instance plus FOUR top-weight arms at four different k.**

## The three committed answers

| source | `② ∧ ③` | set |
|---|---:|---|
| **R360** `clause23_admits` | **5** | coval_core, topw_k3, topw_k4, topw_k6, topw_k8 |
| **R442** `ext_impl` | **5** | *identical* |
| R527 curve ∩ R442 removal — **R665's route** | 2 | coval_core, topw_k4 |

**The gap is exactly `{topw_k3, topw_k6, topw_k8}` — arms R527's percentile curve NEVER SWEEPS.**
R527 ranges over **8 arms of R360's 42**.

> ⛔ **R665's "the definition admits exactly two objects" is RETRACTED.** It was computed through a
> curve whose pool excludes three of the five members.

## ⭐ And the object-level consequence is sharper, not softer

The definition admits **top-weight arms at k = 3, 4, 6 and 8**. **Nothing in `② ∧ ③` distinguishes
k at all.** R665 framed `topw_k4` as *one* witness that the clauses are too weak; there are **four**,
and they differ only in a parameter the definition never mentions.

⚠ **This is a DISAGREEMENT, not an adjudication.** Which arm pool the definition should quantify over
is a choice **the release does not make**. Both numbers and the cause are reported; neither is
declared correct.

## Controls

| control | returned |
|---|---|
| **positive** — R360 and R442, independent rounds ~80 apart | **identical 5-element sets** — PASS |
| **negative** — R527's swept set must be a **strict** subset of R360's arm space | **8 ⊂ 42** — PASS, *pool restriction can explain the gap* |
| **placebo** — an arm in no artifact | **in none** — PASS |

**MULTIPLICITY:** 3 artifacts × 1 extension + 3 controls; **every arm in every set printed.**

## ⛔⛔⛔ Check #268 — and a pattern that is now measured

| claim | truth |
|---|---|
| *"the held-out bracket is **the only** unresolved cell"* | **budget-0 is `[2, 3]`** — also a bracket, on the round's own printed table. |
| *"pinning it needs the per-arm scores, **which the corpus has not surfaced**"* | **The gate was not run.** It finds **21 artifacts** carrying a per-arm map, two of which answer the question outright. |

⭐⭐⭐ **Third consecutive round in which data I asserted missing was already committed** — R664 (from
R527), R665 (from R442/R440), R667 (from R360/R442). **I assert absence at exactly the point where
running the gate is one command.** *That is a measured pattern, not an anecdote.*

**IMPOSSIBLE, named:** **which** pool the definition should quantify over is a choice the release
does not make. Adjudicating the 5-vs-2 disagreement needs a statement from the release about its own
arm space, **and no such statement exists.**

## The sentence I can no longer write

> *"the definition admits exactly two objects: the instance and one baseline."*

**It admits five, four of which are the same construction at four values of k.**

## NEXT

**`topw_k3, k4, k6, k8` are admitted and `topw_k1, topw_k2, topw_k12` are not** — so *something*
in the definition already discriminates k, but only at the ends. **Measure where the k-boundary
falls and what draws it**, because a definition that admits k ∈ {3,4,6,8} and rejects k ∈ {1,2,12}
is not silent about k — it encodes an unstated band, and **an unstated band is a clause nobody
wrote.** That is a claim about the definition's *content*, and it is the first one in this arc that
would add a clause rather than retire one.

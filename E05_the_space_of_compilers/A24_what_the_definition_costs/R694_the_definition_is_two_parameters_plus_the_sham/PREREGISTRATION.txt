R694 · PRE-REGISTRATION

⛔ WHY R693's PROPOSED ROUND IS NOT RUN — A GAUGE TEST KILLED IT AT ZERO COMPUTE.
  It proposed fitting (family, k) jointly and reading the accuracy. Over 42 arms there are 24
  distinct (family, k) cells and **12 are SINGLETONS**. A memorising fit errs only on the minority
  of MIXED cells, so its accuracy is FORCED by cell cardinality, not by ② tracking the parameters.
  "Could this have come out otherwise?" No. Reporting it as evidence is the arithmetic trap.

⚠ AND MY OWN GAUGE TEST HAD AN ARITHMETIC ERROR, CAUGHT BECAUSE THE NUMBER WAS ABSURD. I computed
  errors as `sum(min(counts))`, which charges an error to every PURE cell; it printed 4.8% for a
  memorising fit, which cannot score below the 78.6% floor. Correct: `len(cell) - max(counts)`.

⚠ DISCLOSURE. The gauge test already showed 2 mixed cells for ②, both sham pairs. That is an
  OBSERVATION and no forecast is scored on it.

WHAT IS BLIND, AND IS REGISTERED — the same structure for the OTHER clause readings.
POINT A     ②∧③ (the full definition) has 2 mixed (family,k) cells.   INTERVAL A [0, 6]
POINT B     ③ alone has 1 mixed cell.                                  INTERVAL B [0, 6]
DIRECTIONAL (separately failable) ②∧③ has NO MORE mixed cells than ② alone — adding ③ creates no
            new structure that the two parameters cannot reach.
KILL        if ②∧③ has ZERO mixed cells, the full definition is entirely determined by two
            parameters we chose and the sham separation is LOST. That is the darker outcome and it
            is live.
CONTROLS
  POSITIVE  a synthetic cell holding both labels must be detected as MIXED.
  g=0       a pure cell must NOT be detected as mixed — the detector returns both values.
  NEGATIVE  an empty cell must not be counted.
  PLACEBO   run twice identical.
  ARITHMETIC the memorising fit's accuracy must be >= the majority floor for every clause reading;
            below the floor means the error formula is wrong again.
IDENTIFICATION LIMIT  (family, k) is OUR parameterisation of arms we built. A mixed cell shows the
            clause is not reducible to those two parameters; it does not show what the clause IS.

# R654 · 93 rounds measure a population that grows under them, and 71 of their artifacts cannot say over what

**Decision this makes safe:** whether the four number-movements in this arc were four lapses or one
missing mechanism. **One mechanism. Re-running a corpus-dependent round is a NEW MEASUREMENT, not a
reproduction — and I proved it by destroying a result an hour ago.**

## The census, with its scope

| | n | % |
|---|---:|---:|
| **CORPUS-DEPENDENT** — globs at or above the collection directory | **93** | 28.3% |
| OWN-SCOPE — globs only its own directory | 21 | 6.4% |
| NO-GLOB | 190 | 57.8% |
| UNRESOLVED-BASE — a glob whose base I could not resolve | 25 | 7.6% |

**Of the 93:** 83 record a count · **13 identify the tree** · **71 record a count and cannot say
over what (85.5%)**.

Corpus-wide: **168 rounds stamp the SOURCE, 38 stamp the TREE — the method is recorded 4.4× more
often than the population it ran over.**

## ⛔⛔⛔ Verifying a closing line destroyed a committed result — and that is the finding

1. R651's artifact persists `sites_mine: 355, sites_published: 354` — **the distinct-key counts its
   own multiset repair exists to reject** — while the control that ran and passed compared
   **364 to 364**. The report was right; **the artifact recorded different numbers**, and this round
   read the artifact and quoted 355 vs 354 as the replication.
2. Re-running R651 to regenerate a truthful artifact returned **`mine 369 vs published 364`** and
   wrote **`census_replicated: FALSE`** over a passing result.
3. Cause: **R652 and R653 joined the corpus since R651 ran, and R651's population IS the corpus.**
4. Artifact restored from git; the code annotation kept.

> **An artifact is a measurement at a sha. Re-running the code is not reproduction — it is asking
> the same question of a different object.**

## ⛔ Check #255 — one clause of R653's NEXT was false

> *"4 is small enough that the answer is exact rather than a bound, **which nothing else in this arc
> has been**."*

**Five exact results precede it, all mine, all in the four immediately preceding rounds:** R649's
head-vs-tail over every over-cap file (4, exhaustive, 0 flips) · R651's census replication · R651's
join addressability (0 ambiguous) · R653's vacuous guard (0 over 59) · R653's kill check (0).
*§4's exact tell — a quantifier over my own work.*

✓ The other two clauses hold: all 4 CALLEE sites carry the callee-return reason and all 4 are R652
D1 members. **The label was overloaded in the code** (two branches write `CALLEE`) but the other
branch fired 0 times — verified rather than assumed, *because an overloaded label is how a true
sentence becomes false later.*

## ⛔⛔ And this round's own first stamp detector returned 0, with a sentence built on it

v1 printed **"the corpus stamps its METHOD and not its POPULATION"** while its source-hash detector
returned **0 across 93 rounds**. That claim *requires* the method count to be non-zero.

**§P5: a zero from an instrument never shown to return non-zero is silence, not an acquittal.** The
detector looked for `src` / `source_hash` / `src_sha`; the corpus writes **`source_sha` (96)** and
**`source_sha256` (73)**. Both detectors now carry positive controls that must fire before any stamp
number prints.

⭐ **And the tree stamp hides under a misleading key.** 38 rounds call `git rev-parse HEAD` and
persist it — as `head`, `revision`, and in one era under **`src`**, which reads as *source* and is
in fact the **commit**. **A key name is not a type.**

## Controls

| control | returned |
|---|---|
| **positive** — three rounds whose counts were *observed* to move (R636, R650 900→902, R651 364→369 **today**) | **3/3 CORPUS-DEPENDENT** — PASS |
| **positive** — SOURCE-stamp detector over all rounds | **168** — PASS *(v1 returned 0 on this same corpus)* |
| **positive** — TREE-stamp detector over all rounds | **38** — PASS |
| **negative** — a round globbing only its own `results/` | **OWN-SCOPE** — PASS, *not every glob is corpus-dependent* |
| **placebo** — a module with no glob | **NO-GLOB** — PASS, distinguishable from UNRESOLVED-BASE |
| **g=0** — an empty program | **NO-GLOB** — PASS |

⭐ **The positive control is a fact, not a guess:** all three known movers were watched moving,
one of them during this round.

**MULTIPLICITY:** 1 classifier × 329 rounds + 6 controls + a stamp audit over 93 artifacts; all five
classes printed, including the 25 UNRESOLVED-BASE that are neither a yes nor a no.

**IMPOSSIBLE, named:** whether a corpus-dependent round's **conclusion** would change needs
re-running it at today's tree — **destructive, as demonstrated** — and would require a pinned
worktree per round. So 93 is an **UPPER bound** on rounds whose numbers move and a lower bound on
nothing.

## The sentence I can no longer write

> *"re-run the round to check the number."*

For 93 rounds that is a different measurement, and for 71 of them **nothing in the artifact says
which corpus produced the number in the first place.**

## NEXT

**The remedy is one line in a round's artifact dict and it already exists in 38 rounds: persist
`git rev-parse HEAD` beside every count.** But the 25 **UNRESOLVED-BASE** rounds are the ones to
size first — they are neither corpus-dependent nor safe, and my classifier declined to say which.
**Resolve their glob bases by binding the enclosing function's parameters** (R653 showed 16 of 59
such bindings are statically supplied at every call site), because the census's headline of 93 is an
undercount by an unknown amount until those 25 are decided, and an unknown-sized undercount is the
one thing a census may not have.

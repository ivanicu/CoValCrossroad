R713 — PRE-REGISTERED BEFORE ANY CODE.  written 2026-08-05.

CHECK #315 ON R712's NEXT LINE — TWO DEFECTS, ONE OF THEM WORK ALREADY DONE.
  ✓ p = 0.0003 and ceiling 7 confirmed; the four unique exclusions are indeed oracle and
    label-fitted arms.
  ⛔ "the ONE THING IN THE DELIVERABLE whose count beats its own null" — an UNVERIFIED SUPERLATIVE,
    the FIFTH in this arc. It is true within R712's own 9-cell grid; it is unchecked against every
    other claim on the statement. Withdrawn to: the only cell in R712's grid at its observed size.
  ⛔ "recomputed under the REPAIRED size clause rather than the one R360 committed" — R712 ALREADY
    used the repaired clause (1 < k <= 4). The NEXT line proposed work the round that wrote it had
    done. A closing sentence can also be wrong by asking for what is already on disk.

⭐ AND THE GAUGE TEST, RUN FIRST PER §3's LADDER, AT ZERO COMPUTE.
  F1's exclusion set is EXACTLY {greedy_k4_fit1, indep_k4_fit1, oracle_k4, oracle_k4_fit1}, and the
  name predicate /oracle|_fit1/ returns EXACTLY that set — 4 of 4, no false positive, no false
  negative — and F1 excludes NOTHING ELSE in the 42-arm ledger. ⛔ So F1's p = 0.0003 may be
  measuring that we built four label-reading arms AND a clause that excludes label-reading arms.

ESTIMAND
  (i)  NAME-REPRODUCIBILITY — for each clause, whether a single name predicate reproduces its
       verdict set EXACTLY, and if not, how many arms it misses.
  (ii) The admissibility of R712's null FOR EACH CLAUSE: a uniform random admission is a meaningful
       null for a clause whose exclusions are DISCOVERED and meaningless for one whose exclusions are
       STIPULATED by construction.

IDENTIFICATION
  (i) is exactly computable. (ii) is NOT a measurement — it is a judgement about what a null means,
  and it is argued from (i) rather than measured. Labelled as such: this round measures
  reproducibility and REASONS about admissibility; it does not test admissibility.

SCOPE
  population : the 42 arms of R360's ledger, three clauses
  instrument : exact set comparison between a clause's verdict set and a name predicate's
               instrument unit = AN ARM
               claim unit      = WHETHER A NULL IS ADMISSIBLE FOR A CLAUSE
               ⚠ NOT EQUAL, and the gap is the round's main limit — exact name agreement is
               evidence about construction, not proof of it.
  baseline   : the same test applied to F2 and F3
  regime     : this repository at HEAD

WORLDS
  A STIPULATED   F1's set is exactly name-reproducible and the others' are not ⇒ R712's null is
                 inadmissible for F1, and its "only one above chance" must be re-read as "the most
                 CONSTRUCTED", not "the most informative"
  B DISCOVERED   the name predicate misses arms ⇒ F1's verdict carries content beyond the name and
                 R712's reading stands
  C ALL STIPULATED  every clause is name-reproducible ⇒ the whole three-clause comparison is a
                 comparison of naming conventions

PREDICTION MATRIX             A          B          C
  F1 exact name match        yes        no         yes
  F2/F3 exact name match     no         any        yes
  R712's F1 reading      re-read     stands    all void

REGISTERED
  POINT A  F1: name-predicate misses, in arms: 0, interval [0, 2]
  POINT B  F2: the smallest miss count achievable by any single family-name predicate: 4, [0, 20]
  POINT C  F3: same for the size clause: 0, interval [0, 5]  — F3 IS a k-predicate, so a k-name
           predicate reproducing it exactly is a DERIVATION and is labelled, not counted as evidence.
  DIRECTIONAL (separately failable)  F1's miss count is strictly lower than F2's.
  KILL (conditional — evaluated ONLY if the POSITIVE fires and g=0 fails to match):
     F1 miss = 0 and F2 miss > 0 → world A, and STATEMENT.md must record that F1's exclusions are
       stipulated and that a random-admission null does not price them;
     F1 miss > 0 → world B, R712's reading stands unchanged.

CONTROLS
  POSITIVE   a predicate KNOWN to be wrong for F1 — /topw/ — must MISS. A matcher that matches
             everything proves nothing.
  g=0        a randomly chosen 4-arm set must not reproduce F1's exclusions; report how many of 1000
             random 4-subsets do (the exact chance is 1/C(42,4) = 1/111930).
  NEGATIVE   scramble the arm NAMES against their verdicts, preserving both multisets, and re-run
             the name predicate → the exact match must vanish. The world it excludes: "any predicate
             over 42 strings can be made to fit 4 of them".
  SHAM       the same test on F2 and F3 — the operation minus the clause under study.
  PLACEBO    two identical runs differ by exactly 0.
  UNIT       instrument unit != claim unit, asserted and carried into the verdict; and the verdict
             must state that exact name agreement is EVIDENCE about construction, not PROOF.

SPECIFICATION SWEEP
  clause ∈ {F1, F2, F3} × predicate family ∈ {generator-name tokens, k-value, sham-suffix} = 9 cells,
  all reported with their miss counts.
MULTIPLICITY  9 cells; miss counts are exact, so no p-values are computed and none are implied.
SEEDS  3, for the g=0 random-subset count only.
ARTIFACT  results/stipulated.json — per-clause predicates, miss counts, the scrambled-name control.
IMPOSSIBLE
  proving construction — that the arms were BUILT to be excluded is a fact about our history, not
    about the ledger; exact name agreement is consistent with it and does not establish it.
  cross-release — 41 of 42 arms are ours, which is the whole problem.

# R472 · the register half-complies — 54 of 100 entries name no requirement

**The decision this round makes safe:** whether the impossibility register is a specification or a
list of excuses. **Half of each.** `W-HALF`.

## ⛔ My announced premise was false, and my first count was wrong

R471 closed: *"every entry there is already phrased as 'what it would require'."* A quantifier over my
own work — §4's weakest population. Counted:

| | n | share |
|---|---|---|
| names a requirement **explicitly** | **35** | 35% |
| **implies** one without the phrase | 11 | 11% |
| ⛔ **neither** | **54** | 54% |

**The skill's rule is *"mark them N/A with what each would require, never 'planned'."* The register
has been half-complying.** *Fortieth announced step checked; premise refuted.*

⛔ **And the first count was arithmetically impossible** — 52 with-requirement and 65 without, out of
100. The instrument appended continuation-line matches a second time. **Caught by the internal
inconsistency, not by reading the code**: 52 + 65 > 100 is visible without knowing anything about the
parser. ⭐ *A count whose parts exceed its whole is the cheapest self-check available, and it costs
one glance.*

## ⚠ And even the corrected number measures phrasing, not content

The classifier tests whether a **requirement phrase** appears. An entry like *"construct validity of
`chosen` — the release's own human choice"* names a requirement in substance and fails the pattern.
**So 35 is the tested figure, 46 the generous reading, and 54 a lower bound on the work.** §4's
unit-equality: the instrument's unit is *phrasing*, the claim's unit is *naming a requirement*.

## What was produced

`NEXT_SITE.md` — the specification, derived mechanically from the 46 convertible entries and ranked by
how often this campaign was blocked on each:

| n | requirement |
|---|---|
| 17 | a second release / corpus |
| 10 | a second or third judge |
| 6 | an external gold standard |
| 5 | a generator |
| 4 | more annotators ⚠ *already answered NO here (α = 0.208)* |
| 2 | an intervention on the mechanism |
| 1 | a second prompt-blind family with breadth |

⭐ **Three requirements this site can already specify precisely** — a second core in the same release,
a cross-space key, and construction provenance shipped with each object. **Each removes an entire
class of round rather than answering one question.**

## The answer to the question R471 asked

**Both.** The specification *can* be written — it is written — **and** the register was collecting
unavailability claims alongside requirements at a rate of roughly one in two. **The campaign's product
is a definition plus a partial experiment that would test it, and the shortfall is now a countable
worklist rather than an impression.**

Findings and their scope live in `DEFINITION.md`. `STATEMENT.md` is the residue; `NEXT_SITE.md` is the
specification.

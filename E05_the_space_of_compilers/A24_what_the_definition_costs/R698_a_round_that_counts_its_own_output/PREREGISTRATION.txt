R698 · PRE-REGISTRATION

⛔ THE DEFECT, FOUND WHILE CHECKING #300 AND DISCLOSED BEFORE ANY MEASUREMENT
  R697 reports three different populations depending on when it is read:
    my README                : 79 co-located, 44 after filter 1, 8 admissible
    its committed artifact   : 10 admissible, 32 dropped, 37 degenerate
    a fresh run today        : 81 co-located, 49 after filter 1, 12 admissible
  Two causes, both observed: ① I wrote the README from a PRE-PATCH run and never re-read the
  artifact; ② R697 sweeps `results/*.json` across the arc, so it INCLUDES ITS OWN OUTPUT and every
  artifact written after it. Its population grows every time anything is committed.
  ⭐ THE VERDICT (0 cells could not have resolved) IS THE SAME IN ALL THREE. Only the population
  counts move. That split is what this round measures.

POINT A     re-running R697 with its OWN artifact excluded reduces the admissible count by 2.
INTERVAL A  [0, 8]
POINT B     the verdict (0 could-not-resolve) is UNCHANGED under every exclusion.  BINARY.
DIRECTIONAL (separately failable) the population counts drift while the verdict is stable — i.e.
            self-inclusion moves the denominator and not the conclusion.
KILL        if the verdict CHANGES under any exclusion, R697's finding is VOID rather than merely
            mis-reported, and the retraction is of the conclusion and not the counts.
CONTROLS
  POSITIVE  excluding R697's own artifact MUST reduce the count — if it does not, self-inclusion is
            not the mechanism and the drift has another cause.
  g=0       two runs over an IDENTICAL corpus must give identical numbers; if not, the drift is
            nondeterminism rather than corpus growth.
  NEGATIVE  excluding a file that contains no co-located triple must change nothing.
  PLACEBO   run twice identical.
IDENTIFICATION LIMIT  this measures sensitivity to WHICH FILES ARE PRESENT. It cannot recover what
            the corpus looked like when R697 first ran; git could, but R697's artifact records its
            verdict and not its file list, which is the same gap R695 named.

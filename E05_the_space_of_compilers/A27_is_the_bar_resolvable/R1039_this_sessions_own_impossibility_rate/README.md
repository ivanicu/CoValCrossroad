# R1039 — this session's own `IMPOSSIBLE` lines, falsified by its own later rounds at **4 of 16**

**The decision this round makes safe:** how to read the impossibility claims this arc produced. **As
hypotheses, not limits** — they fell at **7.5×** the committed historical rate.

## ⛔ Why not R1038's proposed population

R1038's NEXT proposed re-reading the register's N/A entries against the release. **That population is
not identified** — R1029 established the requirement type was never stored and three instruments gave
three denominators (17 / 9 / 7). Re-running it repeats a measurement that already failed at G1.

⭐ **What is identified is my own.** Every round R1022–R1038 carries an `IMPOSSIBLE` block in a known
format, enumerable **with no classifier**. And the **numerator is committed text**, not judgement made
now: a line counts as falsified only when a **later round's own artifact** contradicts it.

## Result — ⭐ **World B**

| | |
|---|---|
| **raw proportion** | **4 of 16 = 0.2500** (binomial SE ±0.1083) |
| **R802's committed baseline** | **1 of 30 = 0.0333** — *same unit, directly comparable* |
| difference | **7.5×** |
| mean exposure | 8.06 later rounds |
| hazard *(diagnostic only)* | 0.0310 per round-of-exposure |

**The four:**

| round | its line | falsified by |
|---|---|---|
| **R1026** | a stricter prompt-blind comparator needs **building and scoring** | **R1033** — built one from pool16 subsets for **0** judge calls |
| **R1035** | which q is right needs an **external criterion** | **R1038** |
| **R1036** | whether the scale-free q is the right q needs an **external criterion** | **R1038** |
| **R1037** | which q is right **is not decided here** | **R1038** — the family is its own null |

## ⛔ All four share **one shape**, and that is what the count buys

**Each said the answer needed something OUTSIDE the release, and each was answered by an object
already INSIDE it** — pool16's subsets (R1033), the comparator family as its own null (R1038).
**That is not four mistakes. It is one habit.**

## Controls

- **POSITIVE** — the enumerator finds R1034's block (which names the 254-GFLOP closure): **True**.
- **NEGATIVE** — and does **not** invent one in a docstring that has none: **True**.
- **PLACEBO** — strict lateness: no round may falsify **its own** line: **True**.
- **EXPOSURE** — carried per entry, because **R999** recorded verbatim that *"eligibility is unequal:
  later rounds had fewer chances to be checked."* R1038's own line has exposure **0** and cannot fall
  here.

## ⚠ Two unit errors of my own, both in the first run

1. **The exposure adjustment is a HAZARD, not a proportion** — falsifications *per round of exposure*,
   unbounded above. The first run printed **0.5821 beside 0.0333** as though comparable. **That is a
   units mismatch — the same failure this arc has caught four times at other levels.** Only the raw
   proportion is now placed beside the baseline; the hazard is reported in its own units.
2. **R1039 was in its own population** at exposure 0 — it cannot be falsified and is being written
   now. Excluded; the denominator went 17 → 16.

## What this cannot say

**An unfalsified line is not a true one.** Absence of a later contradiction is exactly the *"unchecked
wall"* this arc keeps finding. **N/A** — attacking each remaining line is **one round each**, and this
round does not do it.

`run.py` · `results/own_impossibility_rate.json`

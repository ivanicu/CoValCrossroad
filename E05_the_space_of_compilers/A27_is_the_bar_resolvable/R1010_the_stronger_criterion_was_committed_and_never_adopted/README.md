# R1010 · the stronger criterion was committed at R921 and read by one round in ninety

**THE DECISION THIS MAKES SAFE.** Whether R1009's repair was a discovery. **It was not — it was an
adoption.** The measurement existed, correct and committed, and the arc built on the weaker route
anyway.

---

## ⛔ The correction first, because it is about my own last round

R921's artifact carries two fields:

```
admitted_by_at_least_one_legitimate : 28 arms   ← includes `generic`, `generic_reprov`
survives_all_legitimate             : 24 arms   ← R1009's repair, already computed AND already named
difference                          : ['generic', 'generic_reprov', 'greedy_k12_fit1', 'topw_k2']
```

**Both R1009's finding and its repair were on disk before it ran.** What R1009 genuinely adds, and
what it should have claimed:

| | |
|---|---|
| **the reading** | R921 framed this as *comparator-choice sensitivity*. That admitting a prompt-blind arm means **the definition admits its own null** is not in R921 |
| **the resolvability** | `lo = +0.0057`, with self-comparison and sham controls |
| **the cost at the conjunction** | 9 vs 12 under ②∧③; R921's 24/28 are clause-② alone |

## The measurement

Over the **68 round scripts written after R921**:

| field | rounds that READ it (tight) | loose mentions | in the statement |
|---|---:|---:|---|
| `legitimate_comparators` — the weaker, per-comparator route | **13** | 14 | no |
| **`survives_all_legitimate`** — the stronger criterion | **1** (R924 only) | 2 | **no** |
| `admitted_by_at_least_one_legitimate` | 1 | 2 | no |
| runtime-assembled absent field | **0** | **0** | no |

⭐⭐ **The defect was never that nobody computed it. It is that nobody adopted it.**

## Controls

| control | result |
|---|---|
| **POSITIVE** | `legitimate_comparators`, a field of the **same artifact**, returns **13** readers. Without this a zero for the other field would be silence, not a measurement |
| **NEGATIVE** | a **runtime-assembled** absent field returns 0 under both patterns and in the statement. Assembled from fragments, because writing an absent marker into the file is what puts it in the corpus |
| **PLACEBO** | loose ≥ tight for every field — a tight pattern cannot exceed its own superset |

**Noise floor: n/a**, labelled — an exact count over a fixed corpus. The uncertainty here is the
**tight/loose spread**, which is reported per field rather than collapsed.

## ⚠ What is not measured, and is not guessed

**WHY it was not adopted.** Intent is not in the record, and a reason invented here would be a
narrative. What is measurable is **that** it was not, and from which round.

**Whether a third prompt-blind comparator would shrink the extension** — R1009's NEXT asked for one.
**It is priced, not guessed:** R921's own artifact records *"a comparator that is not already a scored
arm costs 15,488 judge calls (R914); this round bounds what that would buy."* So the expensive move
was already priced and partly pre-answered — which is why this round asked the cheaper question
instead.

## Alternatives considered

**Build the third comparator, as R1009's NEXT proposed.** Refused for now: 15,488 judge calls for a
question R921 states it already bounded. The ladder says try the cheap rung first, and the cheap rung
turned out to hold the finding.

**Report the adoption gap without correcting R1009.** Refused — the correction *is* the finding's
first half, and a round that reports a gap while leaving its own overclaim standing is doing the
thing it is describing.

# R1014 · the instance's sham is an exact derangement — the text-only class is closed

**THE DECISION THIS MAKES SAFE.** How far R1013's withdrawal reaches. **All the way.** The released
core's sham is its own criterion sets, permuted with **no fixed point**, so every text-only property
is identical by construction.

---

## Where the criteria were

R1013 could not check the instance because `core_coval_core.json` is not on disk. ⭐ **The released
core is in the RELEASE, not in corebench's outputs:** `data/conversation_rubrics.jsonl`, key
`coval_core`, joined by `covalx.judge.load_join`. One line, and it closes R1013's scope gap.

## The result

| | |
|---|---|
| multiset of criterion **sets** identical | **True** — 0 only in the core, 0 only in the sham |
| prompts where the sham holds the core's **own** set | **0 of 968 (0.0%)** — a **derangement** |
| `size_dist` | `{2:1, 3:42, 4:925}` in **both** |
| `mean_chars` | **348.686** in both |
| `mean_words` | **49.5537** in both |
| `mean_unique_criteria` | **3.954545** in both |

⭐⭐⭐ **The sham is the released core's criterion sets, permuted with no prompt keeping its own.**
That is the cleanest possible misdirection: nothing is added, nothing removed, every set survives, and
not one of them stays where it belongs.

## What it closes

**Every property computable from the criteria text alone is identical between core and sham by
construction.** So **no text-only property can be definitional content for this object.** R1013's
withdrawal reaches the instance in full, and the class is **closed**, not merely doubted:

- size · size residual · size variability — **already withdrawn (R1013)**
- vocabulary · length — **now withdrawn**; R1013 had left them live because `gen`'s sham is inexact
- **within-set redundancy** — R990's finding is text-only, so it too is ruled out **as a clause**
  ⚠ (the *measurement* stands; what falls is its candidacy as definitional content)

## Controls

| control | result |
|---|---|
| **POSITIVE** | size recovered from the release reproduces R986's committed decomposition **exactly** — min 2, max 4, **43 off-cap = residual 43**. So this is the object R986 measured |
| **NEGATIVE** | a **runtime-assembled** key yields no criteria — so *"the multiset differs"* could not have come from a mis-keyed lookup returning empty |
| **PLACEBO** | the core against itself is identical on every property |
| **NOISE FLOOR** | **n/a**, labelled — exact set comparison, not an estimate |

## ⭐ What this leaves, positively

**A2 depends on the pairing and is not shared** — the core beats its own sham by **+0.0709 [+0.0615,
+0.0801]** (R1011). **Definitional content must read the criteria AND their own prompt together.**
That is exactly what clause ② does, and after this round it is **the only place content can live.**

## ⚠ Impossible here

**Why the sham was built as a derangement** — N/A. Intent is not in the record; what is measurable is
that the sets moved and none stayed.

## Alternatives considered

**Take R1013's withdrawal as already sufficient.** Refused: R1013 said in its own README that its
three pairs were *other arms* and that the instance's evidence was a single committed number. A
withdrawal resting on one number, when the object itself is one join away, is a wall left unchecked.

**Report "0 of 968" as merely a permutation.** Refused — it is a **derangement**, and the distinction
matters: a permutation with fixed points would leave some prompts holding their own criteria, and
those prompts would not be sham at all.

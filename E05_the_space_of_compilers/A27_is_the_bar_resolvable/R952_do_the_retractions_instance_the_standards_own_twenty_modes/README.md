# R952 · do the retractions instance the standard's own twenty modes?

**The decision this made safe.** Whether an EXTERNAL taxonomy — §4 of the realstat standard, read
from the skill file rather than from memory — can classify the ledger where the ledger cannot
classify itself.

**What it rests on.** At two shared tokens the mode coverage is 0.093000 of 1,149 entries, against a
decoy floor built from the ledger's own vocabulary at matched token counts. At the loosest threshold
the real taxonomy scores *below* its decoys. The top three modes carry a top3 share of 0.057000.

**The alternatives.** Reporting a single threshold — rejected; the sweep is the result, and it is
what shows the loosest cell is worse than chance.

**What it does not do.** Shared tokens are not shared mechanism, so coverage is bounded from above.
A control measured whether unmatched titles are disproportionately claim-statements: they are not.

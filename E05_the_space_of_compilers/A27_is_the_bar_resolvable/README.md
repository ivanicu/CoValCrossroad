# A27 — is the bar resolvable?

**The decision this arc must make safe:** *may `DEFINITION.md` quote a cut point and an admitted
count as numbers, or must both be intervals?*

R922 showed clause ② is a threshold on mean A2 whose cut is placed by the comparator —
`genericpool16` → 0.5514 (28 admitted), `generic` → 0.5593 (24 admitted). Whether that difference
is something this design can see, and how many arms sit close enough to the bar that their
admission is a coin, decides whether the deliverable is a set or a band.

**Per P16 this file is a table of contents only** — every finding, its interval and its scope live
in `E05/DEFINITION.md`.

| round | what it asked |
|---|---|
| `R923` run clause ② on its own comparators | does either legitimate comparator resolvably beat the other, and how many admitted arms sit inside the design's own resolution? |
| `R924` does clause ① exclude anything clause ② does not | is `size > 1` an independent requirement, or implied by the bar? |
| `R925` can any label-blind size-1 selector clear the bar | sweeping 4 orderings × 15 ranks × 2 coverage rules — does clause ① exclude an admissible core, or an inadmissible one? |
| `R926` the price of clause ③ across k | does label access decay with set size, or is the curve my sampling cap? |
| `R929` the point and its interval are different objects | are R141's out-of-interval point estimates an arithmetic slip, or an estimand mismatch? |
| `R930` does rebuilding the matching inside the replicate repair it | UNVERIFIED — the synthetic world never reproduced R141's symptom, so nothing transfers; by-product: the matcher under-fills strata silently |
| `R931` how much of the negative group R141 never matched | is the under-fill inert on the real corpus — and if so, what does explain the `raters`-only incoherence? |
| `R932` what the tied cuts cost R141 | how far do the 42 points move once the duplicated strata are deduped, and does `length` stay still? |
| `R933` does R141's headline survive the repaired estimator | with the tie fixed AND the matching rebuilt in-replicate, do the same cells still exclude zero? |
| `R934` the gate paired two different kills | is R235's flag-vs-interval contradiction real, or did the guard pair an interval from one kill with a boolean from another? |
| `R935` which gates pass on silence | how many assurance checks have neither a plant nor any finding on the corpus, so their green means nothing? |
| `R936` the restore must refuse to delete what it did not stash | does `attack_the_suite`'s restore destroy work created during its window, and does refusing to delete close it? |
| `R937` the object test R936 refused to claim | does a sentinel planted inside a LIVE hide survive the patched restore? |
| `R938` the `kept` branch has no control | does `repair_full` preserve work created during a hide, and is that property exercised by anything? |
| `R939` the PRESERVE channel and its mutation test | does the new control actually reject a broken `repair_full`, or only my inline variant? |
| `R940` R935's bound was two-sided and I stated one | does any `pass on silence` check actually plant? — **corrects R935: 11 → 10, 17% → 15%** |

## Rounds indexed 2026-08-07 — GENERATED FROM ARTIFACTS, not authored

⚠ Each row's text is READ from that round's own results JSON (its `world`/`verdict` and its own
scope note). Nothing here is written from memory, and nothing is a summary I composed: a table of
contents that paraphrases is a second account of the results, which P16 forbids. For the finding,
its interval and its caveat, open the round.

`every_round_reaches_the_readme` had been failing for 93 rounds across five arcs, and
had been failing unasked for a whole session. A round that ran and is never mentioned is a result
nobody has.

| round | as its artifact states it |
|---|---|
| `R923_run_clause_two_on_its_own_comparators` | world **A** · margins and cuts are in mean-A2 units; counts are ARMS |
| `R924_does_clause_one_exclude_anything_clause_two_does_not` | world **B** · A2 and margins are in agreement units; tie rate is a share of the 6 pairwise comparisons |
| `R925_can_any_label_blind_size_one_selector_clear_the_bar` | world **A** · A2 and gaps are in agreement units; counts are ARMS and CELLS |
| `R926_the_price_of_clause_three_across_k` | world **A** · A2 and gaps are in agreement units; counts are PROMPTS |
| `R929_the_point_and_its_interval_are_different_objects` | world **SOURCE_CONFIRMED_MECHANISM_OPEN** · distances and sds are in delta units; counts are CELL×STRATIFIER units |
| `R930_does_rebuilding_the_matching_inside_the_replicate_repair_it` | verdict **UNVERIFIED** |
| `R931_how_much_of_the_negative_group_R141_never_matched` | world **SHORTFALL_REFUTED_DUPLICATION_CONFIRMED** · counts are CRITERIA; shares are of sum(want) |
| `R932_what_the_tied_cuts_cost_R141` | world **A** · shifts are in delta_mean units; counts are CELLS |
| `R933_does_R141s_headline_survive_the_repaired_estimator` | world **A** · intervals are in delta units; counts are CELLS |
| `R934_the_gate_paired_two_different_kills` | world **A** · counts are PAIRS and NODES |
| `R935_which_gates_pass_on_silence` | world **A** · counts are CHECKS |
| `R936_the_restore_must_refuse_to_delete_what_it_did_not_stash` | world **A** · counts are RESTORE VARIANTS; the sentinel is one file |
| `R937_the_object_test_R936_refused_to_claim` | world **A** · one sentinel file |
| `R938_the_kept_branch_has_no_control` | world **A** · one canary file; counts are OCCURRENCES of `kept` |
| `R939_the_preserve_channel_and_its_mutation_test` | counts are CHANNELS and occurrences |
| `R940_R935s_bound_was_two_sided_and_I_stated_one` | world **A** · counts are CHECKS |
| `R941_did_R928s_fix_repair_a_second_harness` | world **B** · counts are VECTORS within a harness |
| `R942_the_harness_reads_a_channel_that_cannot_move` | world **A** · counts are CELLS (plant kind x normalisation state) |
| `R943_the_gate_sees_13_and_the_proxy_routes_number_26` | world **A** · counts are ROUNDS (directories with a run.py) |
| `R944_of_the_blind_15_how_many_declare_anyway` | world **A** · counts are ROUNDS |
| `R945_is_disclosure_caused_by_enforcement_or_by_practice` | world **A** · counts are ROUNDS |
| `R946_is_the_zero_silence_or_the_readers_blindness` | world **C** · counts are ROUNDS |
| `R947_do_the_statements_own_numbers_come_from_artifacts` | world **B** · counts are DISTINCT NUMERALS |
| `R948_does_the_cited_round_actually_hold_the_number` | world **A** · counts are DISTINCT NUMERALS |
| `R949_does_the_key_that_holds_it_mean_what_the_sentence_says` | world **C** · counts are (numeral, cited round) PAIRS |
| `R950_the_naming_convention_shipped_and_its_first_user_is_this_round` | counts are ATTACK VECTORS; the rate is over vectors, not rounds |
| `R951_do_the_1338_retractions_name_their_own_error_classes` | world **C** · counts are LEDGER ENTRIES |
| `R952_do_the_retractions_instance_the_standards_own_twenty_modes` | world **C** · counts are LEDGER ENTRIES |
| `R953_three_near_zeros_are_three_silences_until_each_probe_has_a_positive_control` | world **A** · counts are LEDGER ENTRIES |
| `R954_the_retraction_header_shipped_and_the_attack_found_my_floor` | counts are ATTACK VECTORS and LEDGER ENTRIES, never summed |
| `R955_does_the_definition_contradict_itself_between_statement_and_record` | world **A** · counts are NUMERAL-OCCURRENCE PAIRS and DISTINCT VALUE PAIRS, never summed |
| `R956_the_qualifier_rule_turns_the_read_into_something_checkable` | world **A** · counts are PAIR INSTANCES; the named list is DISTINCT value+qualifier triples, never sum |
